import{createHash}from"node:crypto";export function buildGovernanceAuditIndex({entries}){if(!entries.length)throw Error("index empty");entries=[...entries].sort((a,b)=>a.packetHash.localeCompare(b.packetHash));if(new Set(entries.map(x=>x.packetHash)).size!==entries.length)throw Error("duplicate");let b={kind:"governance-audit-index/v1",entries:Object.freeze(entries)};return Object.freeze({...b,indexHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
