import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceOnboardingCompletionReceipt({checklist,claimProof,firstReceiptProof,completedAt}){if(!checklist?.ready||!H.test(checklist.checklistHash||"")||!H.test(claimProof?.proofHash||"")||!H.test(firstReceiptProof?.proofHash||"")||new Set([checklist.participantId,claimProof.participantId,firstReceiptProof.participantId]).size!==1||firstReceiptProof.claimProofHash!==claimProof.proofHash||!Number.isFinite(Date.parse(completedAt))||Date.parse(completedAt)<Date.parse(firstReceiptProof.receivedAt))throw Error("onboarding completion invalid");let b={kind:"governance-onboarding-completion-receipt/v1",participantId:checklist.participantId,checklistHash:checklist.checklistHash,claimProofHash:claimProof.proofHash,firstReceiptProofHash:firstReceiptProof.proofHash,completedAt:new Date(completedAt).toISOString(),complete:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
