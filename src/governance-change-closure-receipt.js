import{createHash}from"node:crypto";export function buildGovernanceChangeClosureReceipt({changeHash,impactHash,planHash,resultHashes}){for(let h of[changeHash,impactHash,planHash,...resultHashes])if(!/^[a-f0-9]{64}$/.test(h||""))throw Error("receipt invalid");if(!resultHashes.length)throw Error("results missing");let b={kind:"governance-change-closure-receipt/v1",changeHash,impactHash,planHash,resultHashes:Object.freeze([...resultHashes].sort())};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
