import{createHash}from"node:crypto";export function buildGovernanceTasks1To749FullStack({protocolHash,stateHash,assuranceHash,archiveHash}){let hs=[protocolHash,stateHash,assuranceHash,archiveHash];if(hs.some(x=>!/^[a-f0-9]{64}$/.test(x||"")))throw Error("full stack invalid");let b={kind:"governance-tasks1-749-full-stack/v1",protocolHash,stateHash,assuranceHash,archiveHash,complete:true};return Object.freeze({...b,stackHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
