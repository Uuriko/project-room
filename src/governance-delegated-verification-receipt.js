import{createHash}from"node:crypto";export function buildGovernanceDelegatedVerificationReceipt({packetHash,executionHashes,authorityProofHash,passed}){if(![packetHash,authorityProofHash,...executionHashes].every(x=>/^[a-f0-9]{64}$/.test(x||""))||typeof passed!=="boolean")throw Error("receipt invalid");let b={kind:"governance-delegated-verification-receipt/v1",packetHash,executionHashes:Object.freeze([...new Set(executionHashes)].sort()),authorityProofHash,passed};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
