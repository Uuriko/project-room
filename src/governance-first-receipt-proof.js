import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceFirstReceiptProof({participantId,claimProofHash,resultHash,reviewed,receivedAt}){if(!/^[A-Za-z0-9._:@+-]{1,128}$/.test(participantId||"")||!H.test(claimProofHash||"")||!H.test(resultHash||"")||reviewed!==true||!Number.isFinite(Date.parse(receivedAt)))throw Error("first receipt invalid");let b={kind:"governance-first-receipt-proof/v1",participantId,claimProofHash,resultHash,reviewed:true,receivedAt:new Date(receivedAt).toISOString()};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
