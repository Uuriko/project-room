import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceRotationReceipt({assessment,continuityLink,rotatedAt}){if(!assessment?.rotate||!H.test(assessment.assessmentHash||"")||!H.test(continuityLink?.linkHash||"")||!Number.isFinite(Date.parse(rotatedAt))||Date.parse(rotatedAt)<Date.parse(assessment.assessedAt)||Date.parse(rotatedAt)<Date.parse(continuityLink.linkedAt))throw Error("rotation receipt invalid");let b={kind:"governance-rotation-receipt/v1",assessmentHash:assessment.assessmentHash,continuityLinkHash:continuityLink.linkHash,rotatedAt:new Date(rotatedAt).toISOString(),historyPreserved:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
