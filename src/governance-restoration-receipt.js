import{createHash}from"node:crypto";export function buildGovernanceRestorationReceipt({plan,restoredHash,restoredAt}){if(!/^[a-f0-9]{64}$/.test(plan?.planHash||"")||restoredHash!==plan.artifactHash||!Number.isFinite(Date.parse(restoredAt))||Date.parse(restoredAt)<Date.parse(plan.plannedAt))throw Error("restoration receipt invalid");let b={kind:"governance-restoration-receipt/v1",planHash:plan.planHash,restoredHash,restoredAt:new Date(restoredAt).toISOString(),restored:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
