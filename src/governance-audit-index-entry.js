import{createHash}from"node:crypto";const sev=["info","low","medium","high","critical"];export function buildGovernanceAuditIndexEntry({packetHash,categoryCounts,maximumSeverity,timeBucket}){if(!/^[a-f0-9]{64}$/.test(packetHash||"")||!categoryCounts||Object.values(categoryCounts).some(x=>!Number.isInteger(x)||x<0)||!sev.includes(maximumSeverity)||!/^[0-9]{4}-[0-9]{2}$/.test(timeBucket||""))throw Error("entry invalid");categoryCounts=Object.freeze(Object.fromEntries(Object.entries(categoryCounts).sort()));let b={kind:"governance-audit-index-entry/v1",packetHash,categoryCounts,maximumSeverity,timeBucket};return Object.freeze({...b,entryHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
