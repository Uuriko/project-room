import{createHash}from"node:crypto";export function buildGovernanceVerificationPacketReceipt({manifestHash,results}){if(!/^[a-f0-9]{64}$/.test(manifestHash||"")||!results.length)throw Error("receipt invalid");results=[...results].sort((a,b)=>a.resultHash.localeCompare(b.resultHash));if(new Set(results.map(x=>x.checkCode)).size!==results.length)throw Error("duplicate");let b={kind:"governance-verification-packet-receipt/v1",manifestHash,results:Object.freeze(results),verified:results.every(x=>x.status==="pass")};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
