import{createHash}from"node:crypto";export function buildGovernanceAssurancePolicyReceipt({expressionHash,evaluationHash,verificationPacketHash,passed}){for(let h of[expressionHash,evaluationHash,verificationPacketHash])if(!/^[a-f0-9]{64}$/.test(h||""))throw Error("receipt invalid");if(typeof passed!=="boolean")throw Error("passed invalid");let b={kind:"governance-assurance-policy-receipt/v1",expressionHash,evaluationHash,verificationPacketHash,passed};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
