import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;const LEVELS=new Set(["healthy","degraded","critical"]);export function buildGovernanceHealthMetricRecord({metric,value,unit,level,sourceHash,sampledAt}){if(!/^[a-z][a-z0-9._-]{0,63}$/.test(metric||"")||!Number.isFinite(value)||typeof unit!=="string"||!unit.trim()||!LEVELS.has(level)||!HASH.test(sourceHash||"")||!Number.isFinite(Date.parse(sampledAt)))throw Error("health metric invalid");let b={kind:"governance-health-metric-record/v1",metric,value,unit:unit.trim(),level,sourceHash,sampledAt:new Date(sampledAt).toISOString()};return Object.freeze({...b,recordHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
