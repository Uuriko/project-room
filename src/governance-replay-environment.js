import{createHash}from"node:crypto";export function buildGovernanceReplayEnvironment(x){let keys=["engine","version","fixtureHash","clock"];if(!x||Object.keys(x).some(k=>!keys.includes(k))||!x.engine||!x.version||!/^[a-f0-9]{64}$/.test(x.fixtureHash||"")||!Number.isFinite(Date.parse(x.clock)))throw Error("environment invalid");let b={kind:"governance-replay-environment/v1",...x};return Object.freeze({...b,environmentHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
