import{createHash}from"node:crypto";export function buildGovernancePortableComplianceReceipt({profileHash,translationProofHash,sourceVerificationHash,targetResultHash}){if(![profileHash,translationProofHash,sourceVerificationHash,targetResultHash].every(x=>/^[a-f0-9]{64}$/.test(x||"")))throw Error("receipt invalid");let b={kind:"governance-portable-compliance-receipt/v1",profileHash,translationProofHash,sourceVerificationHash,targetResultHash};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
