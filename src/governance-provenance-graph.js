import{createHash}from"node:crypto";export function buildGovernanceProvenanceGraph({nodes,edges}){nodes=[...nodes].sort((a,b)=>a.nodeHash.localeCompare(b.nodeHash));edges=[...edges].sort((a,b)=>a.edgeHash.localeCompare(b.edgeHash));if(new Set(nodes.map(x=>x.nodeHash)).size!==nodes.length||new Set(edges.map(x=>x.edgeHash)).size!==edges.length)throw Error("duplicate");let hs=new Set(nodes.map(x=>x.nodeHash));if(edges.some(x=>!hs.has(x.sourceNodeHash)||!hs.has(x.targetNodeHash)))throw Error("dangling");let b={kind:"governance-provenance-graph/v1",nodes:Object.freeze(nodes),edges:Object.freeze(edges)};return Object.freeze({...b,graphHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
