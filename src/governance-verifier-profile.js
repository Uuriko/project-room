import{createHash}from"node:crypto";export function buildGovernanceVerifierProfile({verifierId,affiliation,capabilities=[]}){if(!verifierId||!affiliation||!Array.isArray(capabilities)||new Set(capabilities).size!==capabilities.length||capabilities.some(x=>typeof x!=="string"||!x))throw Error("verifier profile invalid");let cs=[...capabilities].sort(),b={kind:"governance-verifier-profile/v1",verifierId,affiliation,capabilities:cs};b.capabilities=Object.freeze(cs);return Object.freeze({...b,profileHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
