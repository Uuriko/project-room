import{createHash}from"node:crypto";export function buildGovernanceReleaseSubject({releaseCode,sourceCommit,artifactHashes,buildEnvironmentHash}){if(!releaseCode||!/^[a-f0-9]{7,64}$/.test(sourceCommit||"")||!artifactHashes.length||![...artifactHashes,buildEnvironmentHash].every(x=>/^[a-f0-9]{64}$/.test(x||"")))throw Error("subject invalid");artifactHashes=Object.freeze([...new Set(artifactHashes)].sort());let b={kind:"governance-release-subject/v1",releaseCode,sourceCommit,artifactHashes,buildEnvironmentHash};return Object.freeze({...b,subjectHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
