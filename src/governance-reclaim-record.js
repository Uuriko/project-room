import{createHash}from"node:crypto";export function buildGovernanceReclaimRecord({taskId,expiredLeaseHash,strike,observedAt}){if(!taskId||!/^[a-f0-9]{64}$/.test(expiredLeaseHash||"")||![1,2].includes(strike)||!Number.isFinite(Date.parse(observedAt)))throw Error("reclaim invalid");let b={kind:"governance-reclaim-record/v1",taskId,expiredLeaseHash,strike,observedAt,released:strike===2};return Object.freeze({...b,reclaimHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
