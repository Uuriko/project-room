import{createHash}from"node:crypto";export function buildGovernanceExceptionRenewalLink({expiredLifecycleHash,replacementLifecycleHash,reasonCode,observedAt}){if(![expiredLifecycleHash,replacementLifecycleHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||expiredLifecycleHash===replacementLifecycleHash||!reasonCode||!Number.isFinite(Date.parse(observedAt)))throw Error("renewal invalid");let b={kind:"governance-exception-renewal-link/v1",expiredLifecycleHash,replacementLifecycleHash,reasonCode,observedAt};return Object.freeze({...b,renewalHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
