import{createHash}from"node:crypto";export function buildGovernanceBoundaryTransferAttestation({boundaryHash,sourceArtifactHash,projectionHash,transferClass,timeBucket}){if(![boundaryHash,sourceArtifactHash,projectionHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||sourceArtifactHash===projectionHash||!transferClass||!/^[0-9]{4}-[0-9]{2}$/.test(timeBucket||""))throw Error("transfer invalid");let b={kind:"governance-boundary-transfer-attestation/v1",boundaryHash,sourceArtifactHash,projectionHash,transferClass,timeBucket};return Object.freeze({...b,transferHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
