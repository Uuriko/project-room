import{createHash}from"node:crypto";export function buildGovernanceConformanceRequirement({code,version,evidenceClass,minimumOutcome}){if(!/^[A-Z][A-Z0-9_-]+$/.test(code||"")||!/^[0-9]+\.[0-9]+\.[0-9]+$/.test(version||"")||!evidenceClass||!minimumOutcome)throw Error("requirement invalid");let b={kind:"governance-conformance-requirement/v1",code,version,evidenceClass,minimumOutcome};return Object.freeze({...b,requirementHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
