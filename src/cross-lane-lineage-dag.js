const freeze=x=>{if(x&&typeof x==="object"){Object.values(x).forEach(freeze);Object.freeze(x)}return x};export function buildCrossLaneLineageDag({links}){if(!Array.isArray(links)||!links.length)throw Error("links invalid");let edges=links.map(x=>[x.sourceLane,x.targetLane]),nodes=[...new Set(edges.flat())].sort(),adj=Object.fromEntries(nodes.map(n=>[n,edges.filter(e=>e[0]===n).map(e=>e[1]).sort()])),seen=new Set(),stack=new Set();let visit=n=>{if(stack.has(n))throw Error("cycle invalid");if(seen.has(n))return;stack.add(n);adj[n].forEach(visit);stack.delete(n);seen.add(n)};nodes.forEach(visit);return freeze({kind:"cross-lane-lineage-dag/v1",nodes,edges:edges.sort(),adjacency:adj})}
