import{createHash}from"node:crypto";export function buildGovernanceRevalidationPlan({changeHash,targets}){if(!/^[a-f0-9]{64}$/.test(changeHash||"")||!Array.isArray(targets)||new Set(targets.map(x=>x.nodeHash)).size!==targets.length)throw Error("plan invalid");targets=[...targets].sort((a,b)=>(a.lane+a.stage+a.nodeHash).localeCompare(b.lane+b.stage+b.nodeHash));let b={kind:"governance-revalidation-plan/v1",changeHash,targets:Object.freeze(targets)};return Object.freeze({...b,planHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
