import{createHash}from"node:crypto";export function buildGovernancePollMeasurement({budget,polls,computeUnits,windowStartedAt,measuredAt}){if(!/^[a-f0-9]{64}$/.test(budget?.budgetHash||"")||![polls,computeUnits].every(Number.isInteger)||polls<0||computeUnits<0||![windowStartedAt,measuredAt].every(x=>Number.isFinite(Date.parse(x)))||Date.parse(measuredAt)<Date.parse(windowStartedAt))throw Error("poll measurement invalid");let b={kind:"governance-poll-measurement/v1",budgetHash:budget.budgetHash,polls,computeUnits,windowStartedAt:new Date(windowStartedAt).toISOString(),measuredAt:new Date(measuredAt).toISOString()};return Object.freeze({...b,measurementHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
