export function buildGovernanceDependencyImpactGraph({nodes,edges,changedNodeHash}){let adj=Object.fromEntries(nodes.map(x=>[x,[]]));for(let [a,b]of edges){if(!adj[a]||!adj[b])throw Error("edge invalid");adj[a].push(b)}let q=[changedNodeHash],seen=new Set;while(q.length){let n=q.shift();if(seen.has(n))continue;seen.add(n);q.push(...(adj[n]||[]))}return Object.freeze({kind:"governance-dependency-impact-graph/v1",changedNodeHash,affected:Object.freeze([...seen].sort())})}
