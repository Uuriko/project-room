import{createHash}from"node:crypto";export function buildGovernanceFederationVerificationReceipt({federationHash,coverageHash,memberCheckHashes,passed}){if(![federationHash,coverageHash,...memberCheckHashes].every(x=>/^[a-f0-9]{64}$/.test(x||""))||typeof passed!=="boolean")throw Error("receipt invalid");memberCheckHashes=Object.freeze([...new Set(memberCheckHashes)].sort());let b={kind:"governance-federation-verification-receipt/v1",federationHash,coverageHash,memberCheckHashes,passed};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
