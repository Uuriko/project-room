import{createHash}from"node:crypto";export function buildGovernanceProvenanceNode(x){let keys=["lane","stage","artifactHash"];if(!x||Object.keys(x).some(k=>!keys.includes(k)))throw Error("node invalid");if(!x.lane||!x.stage||!/^[a-f0-9]{64}$/.test(x.artifactHash||""))throw Error("node invalid");let b={kind:"governance-provenance-node/v1",...x};return Object.freeze({...b,nodeHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
