import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceLearningReceipt({promotion,verificationHash,learnedAt}){if(!H.test(promotion?.promotionHash||"")||!H.test(verificationHash||"")||!Number.isFinite(Date.parse(learnedAt))||Date.parse(learnedAt)<Date.parse(promotion.promotedAt))throw Error("learning receipt invalid");let b={kind:"governance-learning-receipt/v1",promotionHash:promotion.promotionHash,verificationHash,learnedAt:new Date(learnedAt).toISOString(),verified:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
