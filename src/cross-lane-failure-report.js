export function buildCrossLaneFailureReport({failures}){if(!Array.isArray(failures))throw Error("failures invalid");let xs=failures.map(x=>{for(let k of["lane","stage","category","hash"])if(typeof x[k]!=="string"||!x[k])throw Error(k+" invalid");if(!/^[a-f0-9]{64}$/.test(x.hash))throw Error("hash invalid");return Object.freeze({lane:x.lane,stage:x.stage,category:x.category,hash:x.hash})});xs=[...new Map(xs.map(x=>[JSON.stringify(x),x])).values()].sort((a,b)=>JSON.stringify(a).localeCompare(JSON.stringify(b)));return Object.freeze({kind:"cross-lane-failure-report/v1",failures:Object.freeze(xs),count:xs.length})}
