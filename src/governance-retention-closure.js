import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceRetentionClosure({assessment,deletionEvidenceHash,closedAt}){if(!assessment?.eligibleForDeletion||!H.test(assessment.assessmentHash||"")||!H.test(deletionEvidenceHash||"")||!Number.isFinite(Date.parse(closedAt))||Date.parse(closedAt)<Date.parse(assessment.assessedAt))throw Error("retention closure invalid");let b={kind:"governance-retention-closure/v1",assessmentHash:assessment.assessmentHash,deletionEvidenceHash,closedAt:new Date(closedAt).toISOString(),closed:true};return Object.freeze({...b,closureHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
