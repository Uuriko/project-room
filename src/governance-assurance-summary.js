export function buildGovernanceAssuranceSummary({lanes,stages,edges,outcomes,negativeBranches}){for(let x of[lanes,stages,edges,outcomes,negativeBranches])if(!Array.isArray(x))throw Error("summary invalid");let counts={lanes:new Set(lanes).size,stages:stages.length,edges:edges.length,outcomes:new Set(outcomes).size,negativeBranches:negativeBranches.length};return Object.freeze({kind:"governance-assurance-summary/v1",counts:Object.freeze(counts),complete:counts.lanes===25&&counts.stages===100&&counts.edges===24&&counts.outcomes===7&&counts.negativeBranches>0})}
