import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;export function buildGovernanceActionReceipt({decision,performedAt,resultHash}){if(!decision?.allowed||!HASH.test(decision.decisionHash||"")||!HASH.test(resultHash||"")||!Number.isFinite(Date.parse(performedAt))||Date.parse(performedAt)<Date.parse(decision.decidedAt))throw Error("action receipt invalid");let b={kind:"governance-action-receipt/v1",decisionHash:decision.decisionHash,action:decision.action,scope:decision.scope,requestHash:decision.requestHash,resultHash,performedAt:new Date(performedAt).toISOString()};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
