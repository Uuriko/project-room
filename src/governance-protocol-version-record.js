import{createHash}from"node:crypto";export function buildGovernanceProtocolVersionRecord({protocol,major,minor,schemaHash,releasedAt}){if(!/^[a-z][a-z0-9._-]{0,63}$/.test(protocol||"")||![major,minor].every(Number.isInteger)||major<1||minor<0||!/^[a-f0-9]{64}$/.test(schemaHash||"")||!Number.isFinite(Date.parse(releasedAt)))throw Error("protocol version invalid");let b={kind:"governance-protocol-version-record/v1",protocol,major,minor,schemaHash,releasedAt:new Date(releasedAt).toISOString()};return Object.freeze({...b,versionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
