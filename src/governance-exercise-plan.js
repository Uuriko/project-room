import{createHash}from"node:crypto";export function buildGovernanceExercisePlan({planId,scenario,expectedRecoveryMinutes,approvedScope}){if(!planId||!scenario||!Number.isInteger(expectedRecoveryMinutes)||expectedRecoveryMinutes<1||!approvedScope)throw Error("exercise plan invalid");let b={kind:"governance-exercise-plan/v1",planId,scenario,expectedRecoveryMinutes,approvedScope};return Object.freeze({...b,planHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
