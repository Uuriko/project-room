export function buildCrossLaneSupersessionReport({records,manifestHashes=[]}){if(!Array.isArray(records))throw Error("records invalid");let by=new Map;for(let x of records){if(by.has(x.priorHash)&&by.get(x.priorHash)!==x.replacementHash)throw Error("fork");by.set(x.priorHash,x.replacementHash)}for(let [a,b]of by){let seen=new Set([a]),n=b;while(by.has(n)){if(seen.has(n))throw Error("cycle");seen.add(n);n=by.get(n)}}let dangling=[...by.values()].filter(x=>!manifestHashes.includes(x)).sort();return Object.freeze({kind:"cross-lane-supersession-report/v1",dangling:Object.freeze(dangling),count:records.length})}
