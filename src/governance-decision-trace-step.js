import{createHash}from"node:crypto";const codes=["allowed","denied","deferred","escalated"];export function buildGovernanceDecisionTraceStep({lane,stage,decisionCode,authorityHash,inputCommitment}){if(!lane||!stage||!codes.includes(decisionCode)||![authorityHash,inputCommitment].every(x=>/^[a-f0-9]{64}$/.test(x||"")))throw Error("step invalid");let b={kind:"governance-decision-trace-step/v1",lane,stage,decisionCode,authorityHash,inputCommitment};return Object.freeze({...b,stepHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
