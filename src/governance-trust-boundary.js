import{createHash}from"node:crypto";export function buildGovernanceTrustBoundary({sourceZone,targetZone,purposeCode,policyHash}){if(!sourceZone||!targetZone||sourceZone===targetZone||!purposeCode||!/^[a-f0-9]{64}$/.test(policyHash||""))throw Error("boundary invalid");let b={kind:"governance-trust-boundary/v1",sourceZone,targetZone,purposeCode,policyHash};return Object.freeze({...b,boundaryHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
