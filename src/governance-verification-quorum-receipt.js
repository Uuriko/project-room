import{createHash}from"node:crypto";export function buildGovernanceVerificationQuorumReceipt({packetHash,authorityProofHashes,resultHash,outcome}){if(![packetHash,resultHash,...authorityProofHashes].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!["approved","rejected","incomplete"].includes(outcome))throw Error("receipt invalid");let b={kind:"governance-verification-quorum-receipt/v1",packetHash,authorityProofHashes:Object.freeze([...new Set(authorityProofHashes)].sort()),resultHash,outcome};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
