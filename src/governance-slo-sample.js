import{createHash}from"node:crypto";export function buildGovernanceSloSample({slo,values,windowStartedAt,windowEndedAt}){if(!/^[a-f0-9]{64}$/.test(slo?.sloHash||"")||!Array.isArray(values)||values.length<1||values.some(x=>!Number.isFinite(x)||x<0)||![windowStartedAt,windowEndedAt].every(x=>Number.isFinite(Date.parse(x)))||Date.parse(windowEndedAt)<=Date.parse(windowStartedAt)||Date.parse(windowEndedAt)-Date.parse(windowStartedAt)>slo.windowMinutes*6e4)throw Error("slo sample invalid");let vs=[...values].sort((a,b)=>a-b),b={kind:"governance-slo-sample/v1",sloHash:slo.sloHash,values:vs,windowStartedAt:new Date(windowStartedAt).toISOString(),windowEndedAt:new Date(windowEndedAt).toISOString()};b.values=Object.freeze(vs);return Object.freeze({...b,sampleHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
