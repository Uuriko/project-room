import{createHash}from"node:crypto";export function buildGovernanceTrainReceipt({train,proof,departedAt}){if(!proof?.ready||proof.trainHash!==train?.trainHash||!/^[a-f0-9]{64}$/.test(proof.proofHash||"")||!Number.isFinite(Date.parse(departedAt))||Date.parse(departedAt)<Date.parse(train.departureAt))throw Error("train receipt invalid");let b={kind:"governance-train-receipt/v1",trainHash:train.trainHash,proofHash:proof.proofHash,candidateHashes:proof.candidateHashes,departedAt:new Date(departedAt).toISOString(),departed:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
