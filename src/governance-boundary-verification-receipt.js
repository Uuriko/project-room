import{createHash}from"node:crypto";export function buildGovernanceBoundaryVerificationReceipt({transferHash,controlProofHash,verificationPacketHash,result}){if(![transferHash,controlProofHash,verificationPacketHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!["allow","deny"].includes(result))throw Error("receipt invalid");let b={kind:"governance-boundary-verification-receipt/v1",transferHash,controlProofHash,verificationPacketHash,result};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
