import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceFederationReceipt({manifest,claimProof,acceptedBy,acceptedAt}){if(!H.test(manifest?.manifestHash||"")||!H.test(claimProof?.proofHash||"")||claimProof.manifestHash!==manifest.manifestHash||acceptedBy!==claimProof.targetBoardId||!Number.isFinite(Date.parse(acceptedAt))||Date.parse(acceptedAt)<Date.parse(claimProof.claimedAt))throw Error("federation receipt invalid");let b={kind:"governance-federation-receipt/v1",manifestHash:manifest.manifestHash,claimProofHash:claimProof.proofHash,acceptedBy,acceptedAt:new Date(acceptedAt).toISOString(),accepted:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
