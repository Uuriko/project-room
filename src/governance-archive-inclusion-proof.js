import{createHash}from"node:crypto";export function buildGovernanceArchiveInclusionProof({manifest,artifactHash,index}){if(!/^[a-f0-9]{64}$/.test(manifest?.manifestHash||"")||!/^[a-f0-9]{64}$/.test(artifactHash||"")||!Number.isInteger(index)||index<0||manifest.artifactHashes[index]!==artifactHash)throw Error("archive inclusion invalid");let b={kind:"governance-archive-inclusion-proof/v1",manifestHash:manifest.manifestHash,artifactHash,index};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
