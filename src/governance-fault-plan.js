import{createHash}from"node:crypto";import{governanceFaults}from"./governance-fault-catalog.js";export function buildGovernanceFaultPlan({targetLane,targetStage,faults}){if(!targetLane||!targetStage||!Array.isArray(faults)||faults.some(x=>!governanceFaults.includes(x)))throw Error("plan invalid");faults=[...new Set(faults)].sort();let b={kind:"governance-fault-plan/v1",targetLane,targetStage,faults:Object.freeze(faults)};return Object.freeze({...b,planHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
