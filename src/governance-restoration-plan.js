import{createHash}from"node:crypto";export function buildGovernanceRestorationPlan({proof,target,plannedAt}){if(!/^[a-f0-9]{64}$/.test(proof?.proofHash||"")||!target||!Number.isFinite(Date.parse(plannedAt)))throw Error("restoration plan invalid");let b={kind:"governance-restoration-plan/v1",proofHash:proof.proofHash,artifactHash:proof.artifactHash,target,plannedAt:new Date(plannedAt).toISOString()};return Object.freeze({...b,planHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
