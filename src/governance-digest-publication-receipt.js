import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;export function buildGovernanceDigestPublicationReceipt({snapshotHash,channel,sourceHeadHash,publishedAt}){if(!HASH.test(snapshotHash||"")||!/^[a-z][a-z0-9._:-]{0,63}$/.test(channel||"")||!HASH.test(sourceHeadHash||"")||!Number.isFinite(Date.parse(publishedAt)))throw Error("digest publication receipt invalid");let b={kind:"governance-digest-publication-receipt/v1",snapshotHash,channel,sourceHeadHash,publishedAt:new Date(publishedAt).toISOString(),authority:false};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
