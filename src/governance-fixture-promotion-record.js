import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceFixturePromotionRecord({classification,fixtureId,fixtureHash,promotedAt}){if(!H.test(classification?.classificationHash||"")||classification.severity<2||!/^[A-Za-z0-9._:-]{1,96}$/.test(fixtureId||"")||!H.test(fixtureHash||"")||!Number.isFinite(Date.parse(promotedAt)))throw Error("fixture promotion invalid");let b={kind:"governance-fixture-promotion-record/v1",classificationHash:classification.classificationHash,fixtureId,fixtureHash,promotedAt:new Date(promotedAt).toISOString()};return Object.freeze({...b,promotionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
