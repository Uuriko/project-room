import{createHash}from"node:crypto";export function buildGovernanceEvidenceFederationMember({memberCode,profileHash,evidenceManifestHash,validityBucket,evidenceClasses}){if(!/^[A-Z][A-Z0-9_-]+$/.test(memberCode||"")||![profileHash,evidenceManifestHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!/^[0-9]{4}-[0-9]{2}$/.test(validityBucket||"")||!evidenceClasses?.length)throw Error("member invalid");evidenceClasses=Object.freeze([...new Set(evidenceClasses)].sort());let b={kind:"governance-evidence-federation-member/v1",memberCode,profileHash,evidenceManifestHash,validityBucket,evidenceClasses};return Object.freeze({...b,memberHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
