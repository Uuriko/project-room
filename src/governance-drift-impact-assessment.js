export function buildGovernanceDriftImpactAssessment({driftHash,requirements,controls,checks,severity}){if(!/^[a-f0-9]{64}$/.test(driftHash||"")||!["low","medium","high","critical"].includes(severity))throw Error("impact invalid");let sort=x=>Object.freeze([...new Set(x)].sort());return Object.freeze({kind:"governance-drift-impact-assessment/v1",driftHash,requirements:sort(requirements),controls:sort(controls),checks:sort(checks),severity})}
