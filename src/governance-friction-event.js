import{createHash}from"node:crypto";const K=new Set(["retry","blocker","correction","abandonment"]);export function buildGovernanceFrictionEvent({eventId,kind,workflow,sourceHash,occurredAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(eventId||"")||!K.has(kind)||!/^[a-z][a-z0-9._:-]{0,95}$/.test(workflow||"")||!/^[a-f0-9]{64}$/.test(sourceHash||"")||!Number.isFinite(Date.parse(occurredAt)))throw Error("friction event invalid");let b={kind:"governance-friction-event/v1",eventId,frictionKind:kind,workflow,sourceHash,occurredAt:new Date(occurredAt).toISOString()};return Object.freeze({...b,eventHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
