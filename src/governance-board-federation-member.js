import{createHash}from"node:crypto";export function buildGovernanceBoardFederationMember({boardId,stateHash,generationHash,observedAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(boardId||"")||![stateHash,generationHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!Number.isFinite(Date.parse(observedAt)))throw Error("federation member invalid");let b={kind:"governance-board-federation-member/v1",boardId,stateHash,generationHash,observedAt:new Date(observedAt).toISOString()};return Object.freeze({...b,memberHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
