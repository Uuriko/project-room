import{createHash}from"node:crypto";const STATES=new Set(["open","claimed","blocked","done"]);export function buildGovernanceBoardTaskEntry({taskId,title,laneId,state="open",revision=1,updatedAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(taskId||"")||typeof title!=="string"||!title.trim()||!/^[A-Za-z0-9._:-]{1,96}$/.test(laneId||"")||!STATES.has(state)||!Number.isInteger(revision)||revision<1||!Number.isFinite(Date.parse(updatedAt)))throw Error("board task entry invalid");let b={kind:"governance-board-task-entry/v1",taskId,title:title.trim(),laneId,state,revision,updatedAt:new Date(updatedAt).toISOString()};return Object.freeze({...b,entryHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
