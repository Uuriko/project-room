import{createHash}from"node:crypto";export function buildGovernanceCostProxyAssessment({budget,measurement}){if(!/^[a-f0-9]{64}$/.test(budget?.budgetHash||"")||!/^([a-f0-9]{64})$/.test(measurement?.measurementHash||"")||measurement.budgetHash!==budget.budgetHash)throw Error("cost proxy invalid");let pollRatio=measurement.polls/budget.maxPolls,computeRatio=measurement.computeUnits/budget.maxComputeUnits,elapsed=Date.parse(measurement.measuredAt)-Date.parse(measurement.windowStartedAt),withinWindow=elapsed<=budget.windowMinutes*6e4,b={kind:"governance-cost-proxy-assessment/v1",budgetHash:budget.budgetHash,measurementHash:measurement.measurementHash,pollRatio,computeRatio,withinWindow,withinBudget:withinWindow&&pollRatio<=1&&computeRatio<=1};return Object.freeze({...b,assessmentHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
