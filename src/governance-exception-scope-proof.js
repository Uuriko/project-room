export function buildGovernanceExceptionScopeProof({lifecycleHash,lane,stage,controlRefs}){if(!/^[a-f0-9]{64}$/.test(lifecycleHash||"")||!lane||!stage||!Array.isArray(controlRefs)||!controlRefs.length||new Set(controlRefs).size!==controlRefs.length)throw Error("scope invalid");return Object.freeze({kind:"governance-exception-scope-proof/v1",lifecycleHash,lane,stage,controlRefs:Object.freeze([...controlRefs].sort())})}
