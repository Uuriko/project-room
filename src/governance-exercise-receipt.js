import{createHash}from"node:crypto";export function buildGovernanceExerciseReceipt({plan,observation,closedAt}){if(!/^[a-f0-9]{64}$/.test(plan?.planHash||"")||!/^[a-f0-9]{64}$/.test(observation?.observationHash||"")||observation.recoveryMinutes>plan.expectedRecoveryMinutes||!Number.isFinite(Date.parse(closedAt))||Date.parse(closedAt)<Date.parse(observation.recoveredAt))throw Error("exercise receipt invalid");let b={kind:"governance-exercise-receipt/v1",planHash:plan.planHash,observationHash:observation.observationHash,closedAt:new Date(closedAt).toISOString(),objectiveMet:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
