export function buildGovernanceBoundaryControlProof({transferHash,controlHashes}){if(!/^[a-f0-9]{64}$/.test(transferHash||"")||controlHashes.length!==3||controlHashes.some(x=>!/^[a-f0-9]{64}$/.test(x))||new Set(controlHashes).size!==3)throw Error("controls invalid");return Object.freeze({kind:"governance-boundary-control-proof/v1",transferHash,controlHashes:Object.freeze([...controlHashes].sort()),complete:true})}
