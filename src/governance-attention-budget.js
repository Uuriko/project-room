import{createHash}from"node:crypto";export function buildGovernanceAttentionBudget({budgetId,maxPolls,maxComputeUnits,windowMinutes}){if(!budgetId||![maxPolls,maxComputeUnits,windowMinutes].every(Number.isInteger)||maxPolls<1||maxComputeUnits<1||windowMinutes<1)throw Error("attention budget invalid");let b={kind:"governance-attention-budget/v1",budgetId,maxPolls,maxComputeUnits,windowMinutes};return Object.freeze({...b,budgetHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
