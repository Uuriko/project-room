import{createHash}from"node:crypto";export function buildGovernanceSloClosure({assessment,closedAt}){if(!assessment?.targetMet||!assessment?.ceilingMet||!/^[a-f0-9]{64}$/.test(assessment.assessmentHash||"")||!Number.isFinite(Date.parse(closedAt)))throw Error("slo closure invalid");let b={kind:"governance-slo-closure/v1",assessmentHash:assessment.assessmentHash,closedAt:new Date(closedAt).toISOString(),closed:true};return Object.freeze({...b,closureHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
