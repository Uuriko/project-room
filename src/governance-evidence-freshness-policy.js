import{createHash}from"node:crypto";export function buildGovernanceEvidenceFreshnessPolicy({evidenceClass,maxAgeSeconds,clockMode}){if(!evidenceClass||!Number.isInteger(maxAgeSeconds)||maxAgeSeconds<1||!["explicit","monotonic"].includes(clockMode))throw Error("policy invalid");let b={kind:"governance-evidence-freshness-policy/v1",evidenceClass,maxAgeSeconds,clockMode};return Object.freeze({...b,policyHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
