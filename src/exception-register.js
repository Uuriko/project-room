import{createHash}from"node:crypto";const list=(v,l)=>{if(!Array.isArray(v)||!v.length||new Set(v).size!==v.length||v.some(x=>typeof x!=="string"||!x))throw Error(`${l} invalid`);return[...v].sort()},freeze=x=>{if(x&&typeof x==="object"){Object.values(x).forEach(freeze);Object.freeze(x)}return x};export function buildExceptionRegister(x){for(const k of["email","phone","name","address"])if(k in x)throw Error("identity PII forbidden");for(const k of["payload","content","body","text"])if(k in x)throw Error("content forbidden");let roles=[x.sponsorRef,x.ownerRef,x.stewardRef,x.approverRef,x.reviewerRef];if(roles.some(v=>typeof v!=="string"||!v)||new Set(roles).size!==5)throw Error("roles must be independent");let start=Date.parse(x.validFrom),end=Date.parse(x.expiresAt);if(!Number.isFinite(start)||!Number.isFinite(end)||end<=start)throw Error("window invalid");let base={sponsorRef:x.sponsorRef,ownerRef:x.ownerRef,stewardRef:x.stewardRef,approverRef:x.approverRef,reviewerRef:x.reviewerRef,exceptionRefs:list(x.exceptionRefs,"exceptions"),conditionRefs:list(x.conditionRefs,"conditions"),evidenceRefs:list(x.evidenceRefs,"evidence"),validFrom:x.validFrom,expiresAt:x.expiresAt};return freeze({...base,registerHash:createHash("sha256").update(JSON.stringify(base)).digest("hex")})}
