import{createHash}from"node:crypto";export function buildGovernanceCiProof({receiptHash,checkCodes,commitHash,passed}){if(![receiptHash,commitHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!checkCodes.length||typeof passed!=="boolean")throw Error("ci proof invalid");checkCodes=Object.freeze([...new Set(checkCodes)].sort());let b={kind:"governance-ci-proof/v1",receiptHash,checkCodes,commitHash,passed};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
