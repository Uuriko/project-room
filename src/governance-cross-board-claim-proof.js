import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceCrossBoardClaimProof({manifest,taskId,sourceBoardId,targetBoardId,sourceStateHash,targetStateHash,claimedAt}){if(!H.test(manifest?.manifestHash||"")||!/^[A-Za-z0-9._:-]{1,96}$/.test(taskId||"")||sourceBoardId===targetBoardId||![sourceStateHash,targetStateHash].every(x=>H.test(x||""))||!manifest.members.some(x=>x.boardId===sourceBoardId&&x.stateHash===sourceStateHash)||!manifest.members.some(x=>x.boardId===targetBoardId&&x.stateHash===targetStateHash)||!Number.isFinite(Date.parse(claimedAt)))throw Error("cross board claim invalid");let b={kind:"governance-cross-board-claim-proof/v1",manifestHash:manifest.manifestHash,taskId,sourceBoardId,targetBoardId,sourceStateHash,targetStateHash,claimedAt:new Date(claimedAt).toISOString()};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
