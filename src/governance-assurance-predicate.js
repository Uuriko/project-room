import{createHash}from"node:crypto";export function buildGovernanceAssurancePredicate({code,subjectHash,operator,expected}){if(!/^[A-Z_]+$/.test(code||"")||!/^[a-f0-9]{64}$/.test(subjectHash||"")||!["eq","gte","includes"].includes(operator)||!["string","number","boolean"].includes(typeof expected))throw Error("predicate invalid");let b={kind:"governance-assurance-predicate/v1",code,subjectHash,operator,expected};return Object.freeze({...b,predicateHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
