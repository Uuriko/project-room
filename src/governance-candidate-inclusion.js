import{createHash}from"node:crypto";export function buildGovernanceCandidateInclusion({train,candidateHash,includedAt}){if(!/^[a-f0-9]{64}$/.test(train?.trainHash||"")||!/^[a-f0-9]{64}$/.test(candidateHash||"")||!Number.isFinite(Date.parse(includedAt))||Date.parse(includedAt)>Date.parse(train.cutoffAt))throw Error("candidate inclusion invalid");let b={kind:"governance-candidate-inclusion/v1",trainHash:train.trainHash,candidateHash,includedAt:new Date(includedAt).toISOString()};return Object.freeze({...b,inclusionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
