import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceMigrationReceipt({mapping,proof,resultHash,migratedAt}){if(!H.test(mapping?.mappingHash||"")||!proof?.compatible||!H.test(proof.proofHash||"")||proof.mappingHash!==mapping.mappingHash||!H.test(resultHash||"")||!Number.isFinite(Date.parse(migratedAt))||Date.parse(migratedAt)<Date.parse(proof.verifiedAt))throw Error("migration receipt invalid");let b={kind:"governance-migration-receipt/v1",mappingHash:mapping.mappingHash,proofHash:proof.proofHash,resultHash,migratedAt:new Date(migratedAt).toISOString(),migrated:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
