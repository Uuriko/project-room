import{createHash}from"node:crypto";export function buildGovernanceProtocolActionRecord({actionCode,taskId,lane,state,reason,observedAt}){if(!["claim","heartbeat","handoff","release","receipt"].includes(actionCode)||!taskId||!lane||!["submitted","working","completed","failed","released"].includes(state)||!reason||!Number.isFinite(Date.parse(observedAt)))throw Error("action invalid");let b={kind:"governance-protocol-action-record/v1",actionCode,taskId,lane,state,reason,observedAt};return Object.freeze({...b,actionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
