import{createHash}from"node:crypto";export function buildGovernanceRetentionPolicy({policyId,minimumDays=30,holdKinds=[]}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(policyId||"")||!Number.isInteger(minimumDays)||minimumDays<1||!Array.isArray(holdKinds)||new Set(holdKinds).size!==holdKinds.length||holdKinds.some(x=>!/^[a-z][a-z0-9._-]{0,63}$/.test(x||"")))throw Error("retention policy invalid");let hs=[...holdKinds].sort(),b={kind:"governance-retention-policy/v1",policyId,minimumDays,holdKinds:hs};b.holdKinds=Object.freeze(hs);return Object.freeze({...b,policyHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
