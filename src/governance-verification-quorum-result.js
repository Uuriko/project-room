export function buildGovernanceVerificationQuorumResult({policy,ballots}){if(new Set(ballots.map(x=>x.verifierProfileHash)).size!==ballots.length||ballots.some(x=>!policy.eligibleProfileHashes.includes(x.verifierProfileHash)))throw Error("ballots invalid");let counts={approve:0,reject:0,abstain:0};ballots.forEach(x=>counts[x.ballotCode]++);let participating=policy.abstentionPolicy==="count"?ballots.length:counts.approve+counts.reject,outcome=participating<policy.requiredCount?"incomplete":counts.approve>=policy.approvalThreshold?"approved":"rejected";return Object.freeze({kind:"governance-verification-quorum-result/v1",policyHash:policy.policyHash,ballots:Object.freeze([...ballots].sort((a,b)=>a.ballotHash.localeCompare(b.ballotHash))),counts:Object.freeze(counts),outcome})}
