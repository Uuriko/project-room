import{createHash}from"node:crypto";export function buildGovernanceSloAssessment({slo,sample}){if(!/^[a-f0-9]{64}$/.test(slo?.sloHash||"")||!/^[a-f0-9]{64}$/.test(sample?.sampleHash||"")||sample.sloHash!==slo.sloHash)throw Error("slo assessment invalid");let i=Math.ceil(slo.percentile/100*sample.values.length)-1,value=sample.values[i],b={kind:"governance-slo-assessment/v1",sloHash:slo.sloHash,sampleHash:sample.sampleHash,percentileValue:value,targetMet:value<=slo.target,ceilingMet:sample.values.at(-1)<=slo.maxValue};return Object.freeze({...b,assessmentHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
