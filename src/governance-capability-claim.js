import{createHash}from"node:crypto";export function buildGovernanceCapabilityClaim({profileHash,implementationHash,evidenceBundleHash,validFrom,expiresAt}){if(![profileHash,implementationHash,evidenceBundleHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!(Date.parse(validFrom)<Date.parse(expiresAt)))throw Error("claim invalid");let b={kind:"governance-capability-claim/v1",profileHash,implementationHash,evidenceBundleHash,validFrom,expiresAt};return Object.freeze({...b,claimHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
