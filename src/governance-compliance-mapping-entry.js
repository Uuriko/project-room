import{createHash}from"node:crypto";export function buildGovernanceComplianceMappingEntry({externalRequirementCode,internalRequirementHashes,mappingMode,version}){if(!/^[A-Z][A-Z0-9_.-]+$/.test(externalRequirementCode||"")||!internalRequirementHashes.length||internalRequirementHashes.some(x=>!/^[a-f0-9]{64}$/.test(x))||!["one-to-one","one-to-many"].includes(mappingMode)||!/^[0-9]+\.[0-9]+\.[0-9]+$/.test(version||""))throw Error("mapping invalid");internalRequirementHashes=Object.freeze([...new Set(internalRequirementHashes)].sort());let b={kind:"governance-compliance-mapping-entry/v1",externalRequirementCode,internalRequirementHashes,mappingMode,version};return Object.freeze({...b,mappingHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
