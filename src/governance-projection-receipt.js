import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;export function buildGovernanceProjectionReceipt({manifest,sourceHeadHash,projectedAt,maxAgeMinutes=60}){if(!HASH.test(manifest?.manifestHash||"")||!HASH.test(sourceHeadHash||"")||!Number.isFinite(Date.parse(manifest.observedAt))||!Number.isFinite(Date.parse(projectedAt))||!Number.isInteger(maxAgeMinutes)||maxAgeMinutes<1)throw Error("projection receipt invalid");let ageMs=Date.parse(projectedAt)-Date.parse(manifest.observedAt);if(ageMs<0)throw Error("projection receipt clock invalid");let b={kind:"governance-projection-receipt/v1",manifestHash:manifest.manifestHash,sourceHeadHash,observedAt:manifest.observedAt,projectedAt:new Date(projectedAt).toISOString(),maxAgeMinutes,fresh:ageMs<=maxAgeMinutes*6e4};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
