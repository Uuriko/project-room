const freeze=x=>{if(x&&typeof x==="object"){Object.values(x).forEach(freeze);Object.freeze(x)}return x};export function buildCrossLaneReachabilityProof({dag,sourceLane,targetLane}){if(!dag?.adjacency||!dag.nodes.includes(sourceLane)||!dag.nodes.includes(targetLane))throw Error("scope invalid");let q=[[sourceLane]],seen=new Set;while(q.length){let p=q.shift(),n=p.at(-1);if(n===targetLane)return freeze({kind:"cross-lane-reachability-proof/v1",sourceLane,targetLane,path:p});if(seen.has(n))continue;seen.add(n);for(let x of dag.adjacency[n])q.push([...p,x])}throw Error("unreachable") }
