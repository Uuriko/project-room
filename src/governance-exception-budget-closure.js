import{createHash}from"node:crypto";export function buildGovernanceExceptionBudgetClosure({assessment,remediationHashes,replacementLedgerHash}){if(assessment.compliant||!remediationHashes.length||![...remediationHashes,replacementLedgerHash].every(x=>/^[a-f0-9]{64}$/.test(x||"")))throw Error("closure invalid");let b={kind:"governance-exception-budget-closure/v1",policyHash:assessment.policyHash,remediationHashes:Object.freeze([...new Set(remediationHashes)].sort()),replacementLedgerHash};return Object.freeze({...b,closureHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
