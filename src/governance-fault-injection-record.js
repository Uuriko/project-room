import{createHash}from"node:crypto";export function buildGovernanceFaultInjectionRecord({plan,faultHash,injectedAt,contained}){if(!/^[a-f0-9]{64}$/.test(plan?.planHash||"")||!/^[a-f0-9]{64}$/.test(faultHash||"")||!Number.isFinite(Date.parse(injectedAt))||contained!==true)throw Error("fault injection invalid");let b={kind:"governance-fault-injection-record/v1",planHash:plan.planHash,faultHash,injectedAt:new Date(injectedAt).toISOString(),contained:true};return Object.freeze({...b,injectionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
