import{createHash}from"node:crypto";export function buildGovernanceReleaseTrainRecord({trainId,departureAt,cutoffAt}){if(!trainId||![departureAt,cutoffAt].every(x=>Number.isFinite(Date.parse(x)))||Date.parse(cutoffAt)>=Date.parse(departureAt))throw Error("release train invalid");let b={kind:"governance-release-train-record/v1",trainId,cutoffAt:new Date(cutoffAt).toISOString(),departureAt:new Date(departureAt).toISOString()};return Object.freeze({...b,trainHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
