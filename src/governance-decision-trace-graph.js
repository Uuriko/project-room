import{createHash}from"node:crypto";export function buildGovernanceDecisionTraceGraph({steps,dependencies}){steps=[...steps].sort((a,b)=>a.stepHash.localeCompare(b.stepHash));if(new Set(steps.map(x=>x.stepHash)).size!==steps.length)throw Error("duplicate");let hs=new Set(steps.map(x=>x.stepHash)),adj=Object.fromEntries(steps.map(x=>[x.stepHash,[]]));for(let [a,b]of dependencies){if(!hs.has(a)||!hs.has(b))throw Error("missing dependency");adj[a].push(b)}let seen=new Set,stack=new Set,visit=n=>{if(stack.has(n))throw Error("cycle");if(seen.has(n))return;stack.add(n);adj[n].forEach(visit);stack.delete(n);seen.add(n)};steps.forEach(x=>visit(x.stepHash));dependencies=[...dependencies].sort();let b={kind:"governance-decision-trace-graph/v1",steps:Object.freeze(steps),dependencies:Object.freeze(dependencies)};return Object.freeze({...b,traceHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
