import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;export function buildGovernanceOwnerConfirmationProof({ownerId,action,scope,requestHash,confirmedAt,expiresAt}){if(!/^[A-Za-z0-9._:@+-]{1,128}$/.test(ownerId||"")||!/^[a-z][a-z0-9._:-]{0,95}$/.test(action||"")||!/^[A-Za-z0-9._:*:-]{1,128}$/.test(scope||"")||!HASH.test(requestHash||"")||!Number.isFinite(Date.parse(confirmedAt))||!Number.isFinite(Date.parse(expiresAt))||Date.parse(expiresAt)<=Date.parse(confirmedAt))throw Error("owner confirmation invalid");let b={kind:"governance-owner-confirmation-proof/v1",ownerId,action,scope,requestHash,confirmedAt:new Date(confirmedAt).toISOString(),expiresAt:new Date(expiresAt).toISOString()};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
