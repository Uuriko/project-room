import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceBoardGenerationRecord({boardId,generation,parentHash=null,stateHash,openedAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(boardId||"")||!Number.isInteger(generation)||generation<1||(generation===1?parentHash!==null:!H.test(parentHash||""))||!H.test(stateHash||"")||!Number.isFinite(Date.parse(openedAt)))throw Error("board generation invalid");let b={kind:"governance-board-generation-record/v1",boardId,generation,parentHash,stateHash,openedAt:new Date(openedAt).toISOString()};return Object.freeze({...b,generationHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
