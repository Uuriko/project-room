import{createHash}from"node:crypto";export function buildGovernanceChangeRecord({oldArtifactHash,newArtifactHash,lane,stage,reasonCode,observedAt}){if(![oldArtifactHash,newArtifactHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||oldArtifactHash===newArtifactHash||!lane||!stage||!reasonCode||!Number.isFinite(Date.parse(observedAt)))throw Error("change invalid");let b={kind:"governance-change-record/v1",oldArtifactHash,newArtifactHash,lane,stage,reasonCode,observedAt};return Object.freeze({...b,changeHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
