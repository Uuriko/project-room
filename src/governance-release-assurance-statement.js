import{createHash}from"node:crypto";export function buildGovernanceReleaseAssuranceStatement({subjectHash,verificationReceiptHash,policyReceiptHash,outcome}){if(![subjectHash,verificationReceiptHash,policyReceiptHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!["approved","rejected"].includes(outcome))throw Error("statement invalid");let b={kind:"governance-release-assurance-statement/v1",subjectHash,verificationReceiptHash,policyReceiptHash,outcome};return Object.freeze({...b,statementHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
