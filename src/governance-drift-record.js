import{createHash}from"node:crypto";export function buildGovernanceDriftRecord({baselineHash,baselineCommitment,observedCommitment,driftClass,lane,stage,observationBucket}){if(![baselineHash,baselineCommitment,observedCommitment].every(x=>/^[a-f0-9]{64}$/.test(x||""))||baselineCommitment===observedCommitment||!driftClass||!lane||!stage||!/^[0-9]{4}-[0-9]{2}$/.test(observationBucket||""))throw Error("drift invalid");let b={kind:"governance-drift-record/v1",baselineHash,baselineCommitment,observedCommitment,driftClass,lane,stage,observationBucket};return Object.freeze({...b,driftHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
