import{createHash}from"node:crypto";export function buildGovernancePortableComplianceProfile({sourceProfileHash,schemeCode,version,mappings}){if(!/^[a-f0-9]{64}$/.test(sourceProfileHash||"")||!schemeCode||!/^[0-9]+\.[0-9]+\.[0-9]+$/.test(version||"")||!mappings.length)throw Error("profile invalid");mappings=[...mappings].sort((a,b)=>a.externalRequirementCode.localeCompare(b.externalRequirementCode));if(new Set(mappings.map(x=>x.externalRequirementCode)).size!==mappings.length)throw Error("duplicate");let b={kind:"governance-portable-compliance-profile/v1",sourceProfileHash,schemeCode,version,mappings:Object.freeze(mappings)};return Object.freeze({...b,profileHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
