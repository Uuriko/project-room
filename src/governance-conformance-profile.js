import{createHash}from"node:crypto";export function buildGovernanceConformanceProfile({name,version,requirements}){if(!name||!/^[0-9]+\.[0-9]+\.[0-9]+$/.test(version||"")||!requirements.length)throw Error("profile invalid");requirements=[...requirements].sort((a,b)=>a.code.localeCompare(b.code));if(new Set(requirements.map(x=>x.code)).size!==requirements.length)throw Error("duplicate");let b={kind:"governance-conformance-profile/v1",name,version,requirements:Object.freeze(requirements)};return Object.freeze({...b,profileHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
