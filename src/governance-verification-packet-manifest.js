import{createHash}from"node:crypto";export function buildGovernanceVerificationPacketManifest(x){for(let k of["headerHash","provenanceHash","evidenceHash","replayHash","auditHash","conformanceHash"])if(!/^[a-f0-9]{64}$/.test(x?.[k]||""))throw Error("manifest invalid");let b={kind:"governance-verification-packet-manifest/v1",...x};return Object.freeze({...b,manifestHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
