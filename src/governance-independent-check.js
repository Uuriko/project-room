import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceIndependentCheck({profile,artifactHash,outcome,checkedAt}){if(!H.test(profile?.profileHash||"")||!H.test(artifactHash||"")||!["pass","fail"].includes(outcome)||!Number.isFinite(Date.parse(checkedAt)))throw Error("independent check invalid");let b={kind:"governance-independent-check/v1",profileHash:profile.profileHash,verifierId:profile.verifierId,affiliation:profile.affiliation,artifactHash,outcome,checkedAt:new Date(checkedAt).toISOString()};return Object.freeze({...b,checkHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
