import{createHash}from"node:crypto";export function buildGovernanceProvenanceEdge(x){for(let k of["sourceNodeHash","targetNodeHash","boundaryLinkHash"])if(!/^[a-f0-9]{64}$/.test(x?.[k]||""))throw Error("edge invalid");if(x.sourceNodeHash===x.targetNodeHash)throw Error("self edge");let b={kind:"governance-provenance-edge/v1",...x};return Object.freeze({...b,edgeHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
