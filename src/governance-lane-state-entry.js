import{createHash}from"node:crypto";const STATES=new Set(["open","held","closed"]);export function buildGovernanceLaneStateEntry({laneId,state="open",activeTaskIds=[],revision=1,updatedAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(laneId||"")||!STATES.has(state)||!Array.isArray(activeTaskIds)||activeTaskIds.some(x=>!/^[A-Za-z0-9._:-]{1,96}$/.test(x||""))||new Set(activeTaskIds).size!==activeTaskIds.length||!Number.isInteger(revision)||revision<1||!Number.isFinite(Date.parse(updatedAt)))throw Error("lane state entry invalid");let b={kind:"governance-lane-state-entry/v1",laneId,state,activeTaskIds:[...activeTaskIds].sort(),revision,updatedAt:new Date(updatedAt).toISOString()};b.activeTaskIds=Object.freeze(b.activeTaskIds);return Object.freeze({...b,entryHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
