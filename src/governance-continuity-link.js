import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceContinuityLink({boardId,fromGenerationHash,toGenerationHash,closingStateHash,openingStateHash,linkedAt}){if(!/^[A-Za-z0-9._:-]{1,96}$/.test(boardId||"")||[fromGenerationHash,toGenerationHash,closingStateHash,openingStateHash].some(x=>!H.test(x||""))||fromGenerationHash===toGenerationHash||closingStateHash!==openingStateHash||!Number.isFinite(Date.parse(linkedAt)))throw Error("continuity link invalid");let b={kind:"governance-continuity-link/v1",boardId,fromGenerationHash,toGenerationHash,stateHash:closingStateHash,linkedAt:new Date(linkedAt).toISOString()};return Object.freeze({...b,linkHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
