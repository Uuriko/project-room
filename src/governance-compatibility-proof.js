import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceCompatibilityProof({mapping,fixtureHashes,verifiedAt}){if(!H.test(mapping?.mappingHash||"")||!Array.isArray(fixtureHashes)||fixtureHashes.length<1||fixtureHashes.some(x=>!H.test(x||""))||new Set(fixtureHashes).size!==fixtureHashes.length||!Number.isFinite(Date.parse(verifiedAt)))throw Error("compatibility proof invalid");let fs=[...fixtureHashes].sort(),b={kind:"governance-compatibility-proof/v1",mappingHash:mapping.mappingHash,fixtureHashes:fs,verifiedAt:new Date(verifiedAt).toISOString(),compatible:true};b.fixtureHashes=Object.freeze(fs);return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
