import{createHash}from"node:crypto";export function buildGovernanceClaimLeaseRecord({actionHash,leaseHours,heartbeatMinutes,startsAt}){if(!/^[a-f0-9]{64}$/.test(actionHash||"")||leaseHours!==24||heartbeatMinutes!==60||!Number.isFinite(Date.parse(startsAt)))throw Error("lease invalid");let b={kind:"governance-claim-lease-record/v1",actionHash,leaseHours,heartbeatMinutes,startsAt};return Object.freeze({...b,leaseHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
