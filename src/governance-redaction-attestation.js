import{createHash}from"node:crypto";export function buildGovernanceRedactionAttestation({sourceHash,projectionHash,ruleHash,removedFieldCounts}){if(![sourceHash,projectionHash,ruleHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||sourceHash===projectionHash||!removedFieldCounts||Object.values(removedFieldCounts).some(x=>!Number.isInteger(x)||x<0))throw Error("attestation invalid");removedFieldCounts=Object.freeze(Object.fromEntries(Object.entries(removedFieldCounts).sort()));let b={kind:"governance-redaction-attestation/v1",sourceHash,projectionHash,ruleHash,removedFieldCounts};return Object.freeze({...b,attestationHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
