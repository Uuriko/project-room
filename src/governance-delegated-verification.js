import{createHash}from"node:crypto";export function buildGovernanceDelegatedVerification({grant,packetHash,executedCheckCodes,resultHashes}){if(!/^[a-f0-9]{64}$/.test(packetHash||"")||executedCheckCodes.length!==resultHashes.length||executedCheckCodes.some(x=>!grant.allowedCheckCodes.includes(x))||resultHashes.some(x=>!/^[a-f0-9]{64}$/.test(x)))throw Error("execution invalid");let pairs=executedCheckCodes.map((code,i)=>({code,resultHash:resultHashes[i]})).sort((a,b)=>a.code.localeCompare(b.code));let b={kind:"governance-delegated-verification/v1",grantHash:grant.grantHash,packetHash,results:Object.freeze(pairs.map(Object.freeze))};return Object.freeze({...b,executionHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
