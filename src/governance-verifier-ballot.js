import{createHash}from"node:crypto";export function buildGovernanceVerifierBallot({verifierProfileHash,packetHash,ballotCode,reasonHash,castBucket}){if(![verifierProfileHash,packetHash,reasonHash].every(x=>/^[a-f0-9]{64}$/.test(x||""))||!["approve","reject","abstain"].includes(ballotCode)||!/^[0-9]{4}-[0-9]{2}$/.test(castBucket||""))throw Error("ballot invalid");let b={kind:"governance-verifier-ballot/v1",verifierProfileHash,packetHash,ballotCode,reasonHash,castBucket};return Object.freeze({...b,ballotHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
