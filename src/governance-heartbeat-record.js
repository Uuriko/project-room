import{createHash}from"node:crypto";export function buildGovernanceHeartbeatRecord({leaseHash,progressCode,observedAt}){if(!/^[a-f0-9]{64}$/.test(leaseHash||"")||!progressCode||!Number.isFinite(Date.parse(observedAt)))throw Error("heartbeat invalid");let b={kind:"governance-heartbeat-record/v1",leaseHash,progressCode,observedAt};return Object.freeze({...b,heartbeatHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
