import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceRetentionAssessment({record,assessedAt}){if(!H.test(record?.recordHash||"")||!Number.isFinite(Date.parse(assessedAt)))throw Error("retention assessment invalid");let age=Date.parse(assessedAt)-Date.parse(record.createdAt);if(age<0)throw Error("retention clock invalid");let periodComplete=age>=record.minimumDays*864e5,eligible=periodComplete&&record.holds.length===0,b={kind:"governance-retention-assessment/v1",recordHash:record.recordHash,assessedAt:new Date(assessedAt).toISOString(),periodComplete,activeHolds:[...record.holds],eligibleForDeletion:eligible};b.activeHolds=Object.freeze(b.activeHolds);return Object.freeze({...b,assessmentHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
