import{createHash}from"node:crypto";export function buildGovernanceIndependentReceipt({proof,releasedAt}){if(!proof?.independent||!/^[a-f0-9]{64}$/.test(proof.proofHash||"")||!Number.isFinite(Date.parse(releasedAt)))throw Error("independent receipt invalid");let b={kind:"governance-independent-receipt/v1",proofHash:proof.proofHash,artifactHash:proof.artifactHash,releasedAt:new Date(releasedAt).toISOString(),verified:true};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
