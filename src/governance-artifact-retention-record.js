import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceArtifactRetentionRecord({artifactId,artifactHash,policy,createdAt,holds=[]}){if(!/^[A-Za-z0-9._:-]{1,128}$/.test(artifactId||"")||!H.test(artifactHash||"")||!H.test(policy?.policyHash||"")||!Number.isFinite(Date.parse(createdAt))||!Array.isArray(holds)||new Set(holds).size!==holds.length||holds.some(x=>!policy.holdKinds.includes(x)))throw Error("artifact retention invalid");let hs=[...holds].sort(),b={kind:"governance-artifact-retention-record/v1",artifactId,artifactHash,policyHash:policy.policyHash,minimumDays:policy.minimumDays,createdAt:new Date(createdAt).toISOString(),holds:hs};b.holds=Object.freeze(hs);return Object.freeze({...b,recordHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
