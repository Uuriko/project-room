import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceMigrationMapping({fromVersion,toVersion,fieldMap}){if(!H.test(fromVersion?.versionHash||"")||!H.test(toVersion?.versionHash||"")||fromVersion.protocol!==toVersion.protocol||toVersion.major<fromVersion.major||!fieldMap||typeof fieldMap!=="object"||Array.isArray(fieldMap)||Object.keys(fieldMap).length<1||Object.values(fieldMap).some(x=>typeof x!=="string"||!x))throw Error("migration mapping invalid");let entries=Object.entries(fieldMap).sort(([a],[b])=>a.localeCompare(b)),b={kind:"governance-migration-mapping/v1",fromVersionHash:fromVersion.versionHash,toVersionHash:toVersion.versionHash,fieldMap:Object.fromEntries(entries)};b.fieldMap=Object.freeze(b.fieldMap);return Object.freeze({...b,mappingHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
