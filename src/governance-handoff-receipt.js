import{createHash}from"node:crypto";export function buildGovernanceHandoffReceipt({actionHash,fromLane,toLane,acceptedAt}){if(!/^[a-f0-9]{64}$/.test(actionHash||"")||!fromLane||!toLane||fromLane===toLane||!Number.isFinite(Date.parse(acceptedAt)))throw Error("handoff invalid");let b={kind:"governance-handoff-receipt/v1",actionHash,fromLane,toLane,acceptedAt};return Object.freeze({...b,handoffHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
