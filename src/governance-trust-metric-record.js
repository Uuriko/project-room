import{createHash}from"node:crypto";const HASH=/^[a-f0-9]{64}$/;export function buildGovernanceTrustMetricRecord({metric,score,scale=100,sourceHash,sampledAt}){if(!/^[a-z][a-z0-9._-]{0,63}$/.test(metric||"")||!Number.isInteger(score)||!Number.isInteger(scale)||scale<1||score<0||score>scale||!HASH.test(sourceHash||"")||!Number.isFinite(Date.parse(sampledAt)))throw Error("trust metric invalid");let b={kind:"governance-trust-metric-record/v1",metric,score,scale,sourceHash,sampledAt:new Date(sampledAt).toISOString(),authority:false};return Object.freeze({...b,recordHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
