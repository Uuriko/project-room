import{createHash}from"node:crypto";export function buildGovernanceRecoveryCheckpoint({prefixManifestHash,nextLane,nextIndex,observedAt}){if(!/^[a-f0-9]{64}$/.test(prefixManifestHash||"")||!nextLane||!Number.isInteger(nextIndex)||nextIndex<0||!Number.isFinite(Date.parse(observedAt)))throw Error("checkpoint invalid");let b={kind:"governance-recovery-checkpoint/v1",prefixManifestHash,nextLane,nextIndex,observedAt};return Object.freeze({...b,checkpointHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
