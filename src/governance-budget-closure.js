import{createHash}from"node:crypto";export function buildGovernanceBudgetClosure({assessment,closedAt}){if(!assessment?.withinBudget||!/^[a-f0-9]{64}$/.test(assessment.assessmentHash||"")||!Number.isFinite(Date.parse(closedAt)))throw Error("budget closure invalid");let b={kind:"governance-budget-closure/v1",assessmentHash:assessment.assessmentHash,closedAt:new Date(closedAt).toISOString(),closed:true};return Object.freeze({...b,closureHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
