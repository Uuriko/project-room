import{createHash}from"node:crypto";const H=/^[a-f0-9]{64}$/;export function buildGovernanceDryRunClaimProof({participantId,checklistHash,claimHash,conflictChecked,performedAt}){if(!/^[A-Za-z0-9._:@+-]{1,128}$/.test(participantId||"")||!H.test(checklistHash||"")||!H.test(claimHash||"")||conflictChecked!==true||!Number.isFinite(Date.parse(performedAt)))throw Error("dry run claim invalid");let b={kind:"governance-dry-run-claim-proof/v1",participantId,checklistHash,claimHash,conflictChecked:true,performedAt:new Date(performedAt).toISOString(),dryRun:true};return Object.freeze({...b,proofHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
