export function buildGovernanceTwoStrikeProof({first,second}){if(first.taskId!==second.taskId||first.strike!==1||second.strike!==2||Date.parse(second.observedAt)-Date.parse(first.observedAt)<4*36e5)throw Error("two strike invalid");return Object.freeze({kind:"governance-two-strike-proof/v1",taskId:first.taskId,firstHash:first.reclaimHash,secondHash:second.reclaimHash,released:true})}
