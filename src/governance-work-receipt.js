import{createHash}from"node:crypto";export function buildGovernanceWorkReceipt({taskId,lane,mergeHash,outcome,observedAt}){if(!taskId||!lane||!/^([a-f0-9]{7,64}|none)$/.test(mergeHash||"")||!["completed","failed"].includes(outcome)||!Number.isFinite(Date.parse(observedAt)))throw Error("receipt invalid");let b={kind:"governance-work-receipt/v1",taskId,lane,mergeHash,outcome,observedAt};return Object.freeze({...b,receiptHash:createHash("sha256").update(JSON.stringify(b)).digest("hex")})}
