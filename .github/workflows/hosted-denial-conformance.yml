name: hosted-denial-conformance

on:
  pull_request:
  push:
    paths:
      - "hosted-denial-conformance/**"
      - ".github/workflows/hosted-denial-conformance.yml"

permissions:
  contents: read

# Cancel superseded PR checks; preserve every main/manual run independently.
concurrency:
  group: ${{ github.workflow }}-pr-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hosted-denial-conformance
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm test
