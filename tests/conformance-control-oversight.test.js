import test from"node:test";import assert from"node:assert/strict";import{buildControlDecision}from"../src/control-decision.js";const c={controlHash:"a".repeat(64),operatorRef:"operator",controlRefs:["access"],safeguardRefs:["limit"],evidenceRefs:["overseen","authorized"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},make=(outcome="active",conditionCodes=[])=>buildControlDecision({control:c,controlHash:c.controlHash,controlRefs:c.controlRefs,safeguardRefs:c.safeguardRefs,evidenceRefs:c.evidenceRefs,operatorRef:"operator",approverRef:"approver",reviewerRef:"reviewer",outcome,conditionCodes,observedAt:"2026-09-17T02:00:00Z"});test("V103 complete authorized scope",()=>{let d=make();for(const k of["controlRefs","safeguardRefs","evidenceRefs"])assert.deepEqual(d[k],[...c[k]].sort())});test("V103 active decision stays in window",()=>assert.equal(make().outcome,"active"));test("V103 non-active decisions carry conditions",()=>{assert.deepEqual(make("conditional",["limit"]).conditionCodes,["limit"]);assert.deepEqual(make("denied",["policy"]).conditionCodes,["policy"])});test("V103 roles remain separate",()=>{let d=make();assert.equal(new Set([d.operatorRef,d.approverRef,d.reviewerRef]).size,3);assert.ok(Object.isFrozen(d))})
