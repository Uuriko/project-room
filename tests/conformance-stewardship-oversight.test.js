import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipDecision}from"../src/stewardship-decision.js";const c={stewardshipHash:"a".repeat(64),stewardRef:"oversight:o",assetRefs:["authorization:read"],dutyRefs:["entitlement:no-share"],evidenceRefs:["oversight:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={contract:c,stewardshipHash:c.stewardshipHash,assetRefs:c.assetRefs,dutyRefs:c.dutyRefs,evidenceRefs:c.evidenceRefs,stewardRef:c.stewardRef,approverRef:"authorization:a",reviewerRef:"entitlement:r",outcome:"conditional",conditionCodes:["oversight:monitor"],observedAt:"2026-09-17T02:00:00Z"};test("V123 complete stewardship decision",()=>{let d=buildStewardshipDecision(x);assert.deepEqual(d.assetRefs,c.assetRefs);assert.deepEqual(d.dutyRefs,c.dutyRefs);assert.deepEqual(d.evidenceRefs,c.evidenceRefs)});test("V123 bounded conditions",()=>{assert.deepEqual(buildStewardshipDecision(x).conditionCodes,["oversight:monitor"]);assert.throws(()=>buildStewardshipDecision({...x,outcome:"active"}),/conditions/)});test("V123 in-window observation",()=>{assert.throws(()=>buildStewardshipDecision({...x,observedAt:c.expiresAt.replace("18T01","19T01")}),/window/)});test("V123 role separation",()=>{assert.equal(new Set([x.stewardRef,x.approverRef,x.reviewerRef]).size,3);assert.throws(()=>buildStewardshipDecision({...x,reviewerRef:x.stewardRef}),/independent/)})
