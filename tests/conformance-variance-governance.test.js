import test from"node:test";import assert from"node:assert/strict";import{buildVarianceContract}from"../src/variance-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",baselineRefs:["qualified:baseline"],deviationRefs:["assured:deviation"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V142 hash-bound variance",()=>{let c=buildVarianceContract(x);assert.match(c.varianceHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V142 preserves scope",()=>{let c=buildVarianceContract(x);assert.deepEqual([c.baselineRefs,c.deviationRefs,c.evidenceRefs],[x.baselineRefs,x.deviationRefs,x.evidenceRefs])});test("V142 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildVarianceContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V142 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildVarianceContract({...x,expiresAt:x.validFrom}),/window/)})
