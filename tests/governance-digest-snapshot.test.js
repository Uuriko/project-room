import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceHealthMetricRecord as health}from"../src/governance-health-metric-record.js";import{buildGovernanceTrustMetricRecord as trust}from"../src/governance-trust-metric-record.js";import{buildGovernanceDigestSnapshot as digest}from"../src/governance-digest-snapshot.js";let sampledAt="2026-09-16T18:00:00Z",h=health({metric:"tasks",value:1,unit:"count",level:"healthy",sourceHash:"a".repeat(64),sampledAt}),t=trust({metric:"confidence",score:80,sourceHash:"b".repeat(64),sampledAt}),windowStartsAt="2026-09-16T17:00:00Z",windowEndsAt="2026-09-16T19:00:00Z";test("595 digest binds health and non-authoritative trust metrics",()=>{let x=digest({healthMetrics:[h],trustMetrics:[t],windowStartsAt,windowEndsAt});assert.equal(x.trust[0].authority,false);assert.ok(Object.isFrozen(x))});test("595 duplicate metrics and reversed windows fail closed",()=>{assert.throws(()=>digest({healthMetrics:[h,h],trustMetrics:[t],windowStartsAt,windowEndsAt}),/duplicate/);assert.throws(()=>digest({healthMetrics:[h],trustMetrics:[t],windowStartsAt:windowEndsAt,windowEndsAt:windowStartsAt}),/invalid/)})
