import test from"node:test";import assert from"node:assert/strict";import{buildAuthorityContract}from"../src/authority-contract.js";const x={grantorRef:"governance:g",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",authorityRefs:["qualified:read"],limitRefs:["assured:no-share"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V118 governed authority is hash-bound",()=>{let c=buildAuthorityContract(x);assert.match(c.authorityHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V118 preserves authority limit evidence bounds",()=>{let c=buildAuthorityContract(x);assert.deepEqual(c.authorityRefs,x.authorityRefs);assert.deepEqual(c.limitRefs,x.limitRefs);assert.deepEqual(c.evidenceRefs,x.evidenceRefs)});test("V118 preserves governed role separation",()=>{assert.throws(()=>buildAuthorityContract({...x,reviewerRef:x.grantorRef}),/independent/);assert.equal(new Set([x.grantorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4)});test("V118 preserves qualification window",()=>{assert.throws(()=>buildAuthorityContract({...x,expiresAt:x.validFrom}),/window/);assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom))})
