import test from"node:test";import assert from"node:assert/strict";import{buildConsentDecision}from"../src/consent-decision.js";const c={consentHash:"a".repeat(64),ownerRef:"o",permissionRefs:["read"],restrictionRefs:["no-share"],evidenceRefs:["e"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={contract:c,consentHash:c.consentHash,permissionRefs:c.permissionRefs,restrictionRefs:c.restrictionRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",approverRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"};test("AB2 immutable consent decision",()=>{let d=buildConsentDecision(x);assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("AB2 binding and complete evaluation",()=>{assert.throws(()=>buildConsentDecision({...x,consentHash:"b".repeat(64)}),/mismatch/);assert.throws(()=>buildConsentDecision({...x,permissionRefs:[]}),/permissions/);assert.throws(()=>buildConsentDecision({...x,evidenceRefs:["z"]}),/evidence/)});test("AB2 outcomes roles and window",()=>{assert.equal(buildConsentDecision({...x,outcome:"conditional",conditionCodes:["limit"]}).outcome,"conditional");assert.throws(()=>buildConsentDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildConsentDecision({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildConsentDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("AB2 privacy",()=>{assert.throws(()=>buildConsentDecision({...x,name:"n"}),/PII/);assert.throws(()=>buildConsentDecision({...x,body:"b"}),/content/)})
