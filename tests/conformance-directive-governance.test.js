import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",executorRef:"qualification:a",reviewerRef:"governance:r",actionRefs:["qualified:request"],constraintRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V186 hash-bound commission",()=>{let c=buildDirectiveContract(x);assert.match(c.directiveHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V186 preserves scope",()=>{let c=buildDirectiveContract(x);assert.deepEqual([c.actionRefs,c.constraintRefs,c.evidenceRefs],[x.actionRefs,x.constraintRefs,x.evidenceRefs])});test("V186 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.executorRef,x.reviewerRef]).size,4);assert.throws(()=>buildDirectiveContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V186 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildDirectiveContract({...x,expiresAt:x.validFrom}),/window/)})
