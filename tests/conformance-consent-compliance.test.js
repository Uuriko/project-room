import test from"node:test";import assert from"node:assert/strict";import{buildConsentException}from"../src/consent-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",permissionRefs:["compliant:read"],restrictionRefs:["validated:no-share"]},x={decision:d,decisionHash:d.decisionHash,permissionRefs:d.permissionRefs,restrictionRefs:d.restrictionRefs,controlRefs:["compliance:review"],evidenceRefs:["validation:proof"],ownerRef:"owner",approverRef:"approver",reviewerRef:"reviewer",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V116 consent scope remains contained",()=>{let e=buildConsentException(x);assert.deepEqual(e.permissionRefs,d.permissionRefs);assert.deepEqual(e.restrictionRefs,d.restrictionRefs)});test("V116 preserves compensating controls and evidence",()=>{let e=buildConsentException(x);assert.deepEqual(e.controlRefs,x.controlRefs);assert.deepEqual(e.evidenceRefs,x.evidenceRefs)});test("V116 preserves bounded expiry",()=>{assert.ok(Date.parse(buildConsentException(x).expiresAt)>Date.parse(x.observedAt));assert.throws(()=>buildConsentException({...x,expiresAt:x.observedAt}),/expiry/)});test("V116 preserves role separation",()=>{assert.equal(new Set([x.ownerRef,x.approverRef,x.reviewerRef]).size,3);assert.throws(()=>buildConsentException({...x,reviewerRef:x.ownerRef}),/independent/)})
