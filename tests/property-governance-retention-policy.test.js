import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceRetentionPolicy as build}from"../src/governance-retention-policy.js";test("662 hold order canonicalizes",()=>assert.deepEqual(build({policyId:"p",holdKinds:["legal","audit"]}),build({policyId:"p",holdKinds:["audit","legal"]})));test("662 duration changes policy identity",()=>assert.notEqual(build({policyId:"p",minimumDays:30}).policyHash,build({policyId:"p",minimumDays:31}).policyHash))
