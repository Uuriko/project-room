import test from"node:test";import assert from"node:assert/strict";import{buildApprovalContract}from"../src/approval-contract.js";import{buildApprovalDecision}from"../src/approval-decision.js";const make=()=>{let c=buildApprovalContract({sponsorRef:"s",ownerRef:"oversight:o",approverRef:"authorization:a",approvalerRef:"entitlement:r",requestRefs:["subject"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildApprovalDecision({contract:c,approvalHash:c.approvalHash,requestRefs:c.requestRefs,criterionRefs:c.criterionRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",approverRef:"authorization:a",approvalerRef:"entitlement:r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V151 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.requestRefs,d.criterionRefs,d.evidenceRefs],[['subject'],['criterion'],['approval']])});test("V151 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V151 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.approverRef,d.approvalerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V151 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
