import test from"node:test";import assert from"node:assert/strict";import{buildConsentResolution}from"../src/consent-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),permissionRefs:["read"],restrictionRefs:["no-share"],controlRefs:["review"],evidenceRefs:["e"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,permissionRefs:e.permissionRefs,restrictionRefs:e.restrictionRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"a",reviewerRef:"b",outcome:"resolved",observedAt:"2026-09-17T02:00:00Z"};test("AB4 immutable resolution",()=>{let r=buildConsentResolution(x);assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("AB4 binding and complete lineage",()=>{assert.throws(()=>buildConsentResolution({...x,decisionHash:"c".repeat(64)}),/mismatch/);assert.throws(()=>buildConsentResolution({...x,restrictionRefs:[]}),/restrictions/);assert.throws(()=>buildConsentResolution({...x,evidenceRefs:["z"]}),/evidence/)});test("AB4 outcome roles and time",()=>{assert.equal(buildConsentResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildConsentResolution({...x,outcome:"open"}),/outcome/);assert.throws(()=>buildConsentResolution({...x,reviewerRef:"a"}),/independent/);assert.throws(()=>buildConsentResolution({...x,outcome:"expired"}),/not expired/)});test("AB4 privacy",()=>{assert.throws(()=>buildConsentResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildConsentResolution({...x,text:"t"}),/content/)})
