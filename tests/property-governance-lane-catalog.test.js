import test from"node:test";import assert from"node:assert/strict";import{governanceLanes}from"../src/governance-lane-catalog.js";test("252 catalog has stable order",()=>assert.deepEqual([...governanceLanes],[...governanceLanes].slice()));test("252 catalog resists mutation",()=>assert.throws(()=>governanceLanes.push("x"),TypeError));test("252 values are unique nonempty strings",()=>{assert.equal(new Set(governanceLanes).size,governanceLanes.length);governanceLanes.forEach(x=>assert.ok(x))});