import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceTrustMetricRecord as build}from"../src/governance-trust-metric-record.js";let base={metric:"confidence",scale:100,sourceHash:"a".repeat(64),sampledAt:"2026-09-16T18:00:00Z"};test("594 every bounded integer score remains non-authoritative",()=>{for(let score=0;score<=100;score++)assert.equal(build({...base,score}).authority,false)});test("594 equivalent instants canonicalize without changing score",()=>assert.deepEqual(build({...base,score:50}),build({...base,score:50,sampledAt:"2026-09-16T11:00:00-07:00"})))
