import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceAuditFinding as finding}from"../src/governance-audit-finding.js";import{buildGovernanceAuditPacket as packet}from"../src/governance-audit-packet.js";import{buildGovernanceAuditExplanation as explain}from"../src/governance-audit-explanation.js";const run=()=>{let a=finding({category:"stale",severity:"high",lane:"policy",stage:"resolution",artifactHash:"a".repeat(64)}),b=finding({category:"privacy",severity:"critical",lane:"control",stage:"contract",artifactHash:"b".repeat(64)}),p=packet({findings:[b,a],snapshotHash:"c".repeat(64),manifestHash:"d".repeat(64),provenanceHash:"e".repeat(64),replayHash:"f".repeat(64)});return{a,b,p,x:explain({findings:p.findings})}};test("340 packet is deterministic immutable and explained",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.equal(a.x.total,2);assert.deepEqual(a.x.codes,["privacy","stale"]);assert.ok(Object.isFrozen(a.p.findings))});test("340 commitment tamper changes packet hash",()=>{let a=run(),p=packet({findings:a.p.findings,snapshotHash:"0".repeat(64),manifestHash:a.p.manifestHash,provenanceHash:a.p.provenanceHash,replayHash:a.p.replayHash});assert.notEqual(p.packetHash,a.p.packetHash)});