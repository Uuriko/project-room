import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceAttentionBudget as b}from"../src/governance-attention-budget.js";test("691 budget canonicalizes",()=>assert.deepEqual(b({budgetId:"x",maxPolls:10,maxComputeUnits:20,windowMinutes:60}),b({budgetId:"x",maxPolls:10,maxComputeUnits:20,windowMinutes:60})));test("691 nonpositive limits reject",()=>assert.throws(()=>b({budgetId:"x",maxPolls:0,maxComputeUnits:20,windowMinutes:60}),/invalid/))
