import test from"node:test";import assert from"node:assert/strict";import{governanceBoundaries}from"../src/governance-boundary-catalog.js";test("259 catalog is nonempty unique and frozen",()=>{assert.ok(governanceBoundaries.length);assert.equal(new Set(governanceBoundaries).size,governanceBoundaries.length);assert.ok(Object.isFrozen(governanceBoundaries))});test("259 catalog excludes private content",()=>assert.equal(JSON.stringify(governanceBoundaries).includes("email"),false));