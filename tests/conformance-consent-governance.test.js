import test from"node:test";import assert from"node:assert/strict";import{buildConsentContract}from"../src/consent-contract.js";const x={grantorRef:"governance:g",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",permissionRefs:["qualified:read"],restrictionRefs:["assured:no-share"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V114 governed consent is hash-bound",()=>{let c=buildConsentContract(x);assert.match(c.consentHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V114 preserves permission restriction evidence bounds",()=>{let c=buildConsentContract(x);assert.deepEqual(c.permissionRefs,x.permissionRefs);assert.deepEqual(c.restrictionRefs,x.restrictionRefs);assert.deepEqual(c.evidenceRefs,x.evidenceRefs)});test("V114 preserves governed role separation",()=>{assert.throws(()=>buildConsentContract({...x,reviewerRef:x.grantorRef}),/independent/);assert.equal(new Set([x.grantorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4)});test("V114 preserves qualification window",()=>{assert.throws(()=>buildConsentContract({...x,expiresAt:x.validFrom}),/window/);assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom))})
