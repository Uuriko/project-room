import test from"node:test";import assert from"node:assert/strict";import{buildObligationContract}from"../src/obligation-contract.js";const x={sponsorRef:"governance:s",obligorRef:"assurance:o",performerRef:"qualification:a",reviewerRef:"governance:r",dutyRefs:["qualified:request"],constraintRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V190 hash-bound commission",()=>{let c=buildObligationContract(x);assert.match(c.obligationHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V190 preserves scope",()=>{let c=buildObligationContract(x);assert.deepEqual([c.dutyRefs,c.constraintRefs,c.evidenceRefs],[x.dutyRefs,x.constraintRefs,x.evidenceRefs])});test("V190 roles",()=>{assert.equal(new Set([x.sponsorRef,x.obligorRef,x.performerRef,x.reviewerRef]).size,4);assert.throws(()=>buildObligationContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V190 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildObligationContract({...x,expiresAt:x.validFrom}),/window/)})
