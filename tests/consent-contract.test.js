import test from"node:test";import assert from"node:assert/strict";import{buildConsentContract}from"../src/consent-contract.js";const x={grantorRef:"g",ownerRef:"o",approverRef:"a",reviewerRef:"r",permissionRefs:["read"],restrictionRefs:["no-share"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("AB1 immutable consent contract",()=>{let c=buildConsentContract(x);assert.match(c.consentHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c));assert.ok(Object.isFrozen(c.permissionRefs))});test("AB1 complete consent scope",()=>{for(const k of["permissionRefs","restrictionRefs","evidenceRefs"])assert.throws(()=>buildConsentContract({...x,[k]:[]}),/invalid/)});test("AB1 roles and window",()=>{assert.throws(()=>buildConsentContract({...x,reviewerRef:"g"}),/independent/);assert.throws(()=>buildConsentContract({...x,expiresAt:x.validFrom}),/window/)});test("AB1 privacy",()=>{assert.throws(()=>buildConsentContract({...x,email:"x"}),/PII/);assert.throws(()=>buildConsentContract({...x,content:"x"}),/content/)})
