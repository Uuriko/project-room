import test from"node:test";import assert from"node:assert/strict";import{buildAuthorityException}from"../src/authority-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",authorityRefs:["compliant:read"],limitRefs:["validated:no-share"]},x={decision:d,decisionHash:d.decisionHash,authorityRefs:d.authorityRefs,limitRefs:d.limitRefs,controlRefs:["compliance:review"],evidenceRefs:["validation:proof"],ownerRef:"owner",approverRef:"approver",reviewerRef:"reviewer",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V120 authority scope remains contained",()=>{let e=buildAuthorityException(x);assert.deepEqual(e.authorityRefs,d.authorityRefs);assert.deepEqual(e.limitRefs,d.limitRefs)});test("V120 preserves compensating controls and evidence",()=>{let e=buildAuthorityException(x);assert.deepEqual(e.controlRefs,x.controlRefs);assert.deepEqual(e.evidenceRefs,x.evidenceRefs)});test("V120 preserves bounded expiry",()=>{assert.ok(Date.parse(buildAuthorityException(x).expiresAt)>Date.parse(x.observedAt));assert.throws(()=>buildAuthorityException({...x,expiresAt:x.observedAt}),/expiry/)});test("V120 preserves role separation",()=>{assert.equal(new Set([x.ownerRef,x.approverRef,x.reviewerRef]).size,3);assert.throws(()=>buildAuthorityException({...x,reviewerRef:x.ownerRef}),/independent/)})
