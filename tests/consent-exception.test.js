import test from"node:test";import assert from"node:assert/strict";import{buildConsentException}from"../src/consent-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",permissionRefs:["read"],restrictionRefs:["no-share"]},x={decision:d,decisionHash:d.decisionHash,permissionRefs:d.permissionRefs,restrictionRefs:d.restrictionRefs,controlRefs:["review"],evidenceRefs:["e"],ownerRef:"o",approverRef:"a",reviewerRef:"r",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("AB3 immutable bounded exception",()=>{let e=buildConsentException(x);assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("AB3 conditional and contained",()=>{assert.throws(()=>buildConsentException({...x,decision:{...d,outcome:"active"}}),/conditional/);assert.throws(()=>buildConsentException({...x,permissionRefs:["write"]}),/scope/)});test("AB3 controls roles and expiry",()=>{assert.throws(()=>buildConsentException({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildConsentException({...x,evidenceRefs:[]}),/evidence/);assert.throws(()=>buildConsentException({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildConsentException({...x,expiresAt:x.observedAt}),/expiry/)});test("AB3 privacy",()=>{assert.throws(()=>buildConsentException({...x,address:"x"}),/PII/);assert.throws(()=>buildConsentException({...x,payload:"x"}),/content/)})
