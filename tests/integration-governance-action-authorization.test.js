import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceActionPolicyRule as rule}from"../src/governance-action-policy-rule.js";import{buildGovernanceOwnerConfirmationProof as proof}from"../src/governance-owner-confirmation-proof.js";import{buildGovernanceAuthorizationDecision as decide}from"../src/governance-authorization-decision.js";import{buildGovernanceActionReceipt as receipt}from"../src/governance-action-receipt.js";const run=()=>{let action="deploy",scope="repo:room",requestHash="a".repeat(64),r=rule({ruleId:"R",action,effect:"confirm-owner",scope}),p=proof({ownerId:"owner",action,scope,requestHash,confirmedAt:"2026-09-16T18:00:00Z",expiresAt:"2026-09-16T19:00:00Z"}),d=decide({rule:r,proof:p,action,scope,requestHash,decidedAt:"2026-09-16T18:30:00Z"}),x=receipt({decision:d,performedAt:"2026-09-16T18:31:00Z",resultHash:"b".repeat(64)});return{r,p,d,x}};test("608 exact owner authorization flows to action receipt",()=>{let x=run();assert.equal(x.d.allowed,true);assert.equal(x.x.decisionHash,x.d.decisionHash)});test("608 authorization flow is deterministic",()=>assert.deepEqual(run(),run()))
