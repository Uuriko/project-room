import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceSloDefinition as b}from"../src/governance-slo-definition.js";let x={sloId:"s",metric:"latency",percentile:95,target:100,maxValue:200,windowMinutes:60};test("711 SLO canonicalizes",()=>assert.deepEqual(b(x),b(x)));test("711 invalid percentile rejects",()=>assert.throws(()=>b({...x,percentile:101}),/invalid/))
