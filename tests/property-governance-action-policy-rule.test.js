import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceActionPolicyRule as build}from"../src/governance-action-policy-rule.js";let base={ruleId:"R",action:"publish",scope:"board:266",revision:1};test("602 each supported effect has a distinct rule identity",()=>{let hashes=["allow","deny","confirm-owner"].map(effect=>build({...base,effect}).ruleHash);assert.equal(new Set(hashes).size,3)});test("602 revision changes separate rule identity",()=>assert.notEqual(build({...base,effect:"deny"}).ruleHash,build({...base,effect:"deny",revision:2}).ruleHash))
