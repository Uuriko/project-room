import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceReplayEnvironment as env}from"../src/governance-replay-environment.js";import{buildGovernanceReplayReceipt as receipt}from"../src/governance-replay-receipt.js";import{buildGovernanceFixtureIntegrity as integrity}from"../src/governance-fixture-integrity.js";const run=()=>{let i=integrity({bytes:'{"a":1}',semantic:{a:1}}),e=env({engine:"node",version:"22",fixtureHash:i.semanticHash,clock:"2026-09-17T03:00:00Z"}),r=receipt({environmentHash:e.environmentHash,snapshotHash:"a".repeat(64),manifestHash:"b".repeat(64),resultHash:"c".repeat(64)});return{i,e,r}};test("323 replay is deterministic and immutable",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.ok(Object.isFrozen(a.r))});test("323 byte and semantic integrity are distinct",()=>{let a=integrity({bytes:"{\n  \"a\": 1\n}",semantic:{a:1}}),b=integrity({bytes:"{\r\n  \"a\": 1\r\n}",semantic:{a:1}});assert.notEqual(a.byteHash,b.byteHash);assert.equal(a.semanticHash,b.semanticHash)});