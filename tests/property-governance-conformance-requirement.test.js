import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceConformanceRequirement as req}from"../src/governance-conformance-requirement.js";import{buildGovernanceConformanceProfile as profile}from"../src/governance-conformance-profile.js";import{buildGovernanceCapabilityClaim as claim}from"../src/governance-capability-claim.js";import{buildGovernanceConformanceAssessment as assess}from"../src/governance-conformance-assessment.js";const run=()=>{let rs=[req({code:"EVIDENCE",version:"1.0.0",evidenceClass:"audit",minimumOutcome:"resolved"}),req({code:"LINEAGE",version:"1.0.0",evidenceClass:"provenance",minimumOutcome:"resolved"})],p=profile({name:"core",version:"1.0.0",requirements:rs.reverse()}),a=assess({profile:p,results:[{code:"EVIDENCE",status:"pass"},{code:"LINEAGE",status:"pass"}]}),c=claim({profileHash:p.profileHash,implementationHash:"a".repeat(64),evidenceBundleHash:"b".repeat(64),validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return{p,a,c}};test("412 profile assesses and claims capability",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.equal(a.a.conformant,true);assert.equal(a.a.counts.pass,2);assert.ok(Object.isFrozen(a.p.requirements))});test("412 downgrade and expired windows fail closed",()=>{let x=run(),a=assess({profile:x.p,results:[{code:"EVIDENCE",status:"fail"}]});assert.equal(a.conformant,false);assert.deepEqual(a.unmet,["EVIDENCE","LINEAGE"]);assert.throws(()=>claim({profileHash:x.p.profileHash,implementationHash:"a".repeat(64),evidenceBundleHash:"b".repeat(64),validFrom:"2026-09-18T01:00:00Z",expiresAt:"2026-09-17T01:00:00Z"}),/claim/)});