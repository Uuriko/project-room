import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceCompatibilityProof as build}from"../src/governance-compatibility-proof.js";let m={mappingHash:"a".repeat(64)},v="2026-09-16T19:00:00Z",a="b".repeat(64),b="c".repeat(64);test("676 fixture order canonicalizes",()=>assert.deepEqual(build({mapping:m,fixtureHashes:[a,b],verifiedAt:v}),build({mapping:m,fixtureHashes:[b,a],verifiedAt:v})));test("676 duplicate fixtures fail closed",()=>assert.throws(()=>build({mapping:m,fixtureHashes:[a,a],verifiedAt:v}),/invalid/))
