import test from"node:test";import assert from"node:assert/strict";import{buildWaiverContract}from"../src/waiver-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",requirementRefs:["qualified:requirement"],controlRefs:["assured:control"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V138 hash-bound waiver",()=>{let c=buildWaiverContract(x);assert.match(c.waiverHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V138 preserves scope",()=>{let c=buildWaiverContract(x);assert.deepEqual([c.requirementRefs,c.controlRefs,c.evidenceRefs],[x.requirementRefs,x.controlRefs,x.evidenceRefs])});test("V138 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildWaiverContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V138 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildWaiverContract({...x,expiresAt:x.validFrom}),/window/)})
