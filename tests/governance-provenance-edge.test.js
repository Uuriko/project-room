import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceProvenanceNode as node}from"../src/governance-provenance-node.js";import{buildGovernanceProvenanceEdge as edge}from"../src/governance-provenance-edge.js";import{buildGovernanceProvenanceGraph as graph}from"../src/governance-provenance-graph.js";import{buildGovernanceProvenanceInclusionProof as proof}from"../src/governance-provenance-inclusion-proof.js";const fixture=()=>{let a=node({lane:"policy",stage:"resolution",artifactHash:"a".repeat(64)}),b=node({lane:"control",stage:"contract",artifactHash:"b".repeat(64)}),e=edge({sourceNodeHash:a.nodeHash,targetNodeHash:b.nodeHash,boundaryLinkHash:"c".repeat(64)}),g=graph({nodes:[a,b],edges:[e]});return{a,b,e,g}};test("303 constructs deterministic immutable provenance",()=>{let x=fixture();assert.match(x.g.graphHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(x.g));assert.deepEqual(graph({nodes:[x.b,x.a],edges:[x.e]}),x.g)});test("303 inclusion is graph-bound and rejects missing",()=>{let x=fixture(),p=proof({graph:x.g,hash:x.a.nodeHash,type:"node"});assert.equal(p.graphHash,x.g.graphHash);assert.throws(()=>proof({graph:x.g,hash:"f".repeat(64),type:"node"}),/missing/)});