import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipException}from"../src/stewardship-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",assetRefs:["compliant:read"],dutyRefs:["validated:no-share"]},x={decision:d,decisionHash:d.decisionHash,assetRefs:d.assetRefs,dutyRefs:d.dutyRefs,controlRefs:["compliance:review"],evidenceRefs:["validation:proof"],stewardRef:"owner",approverRef:"approver",reviewerRef:"reviewer",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V124 stewardship scope remains contained",()=>{let e=buildStewardshipException(x);assert.deepEqual(e.assetRefs,d.assetRefs);assert.deepEqual(e.dutyRefs,d.dutyRefs)});test("V124 preserves compensating controls and evidence",()=>{let e=buildStewardshipException(x);assert.deepEqual(e.controlRefs,x.controlRefs);assert.deepEqual(e.evidenceRefs,x.evidenceRefs)});test("V124 preserves bounded expiry",()=>{assert.ok(Date.parse(buildStewardshipException(x).expiresAt)>Date.parse(x.observedAt));assert.throws(()=>buildStewardshipException({...x,expiresAt:x.observedAt}),/expiry/)});test("V124 preserves role separation",()=>{assert.equal(new Set([x.stewardRef,x.approverRef,x.reviewerRef]).size,3);assert.throws(()=>buildStewardshipException({...x,reviewerRef:x.stewardRef}),/independent/)})
