import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipResolution}from"../src/stewardship-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),assetRefs:["read"],dutyRefs:["no-share"],controlRefs:["review"],evidenceRefs:["e"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,assetRefs:e.assetRefs,dutyRefs:e.dutyRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"a",reviewerRef:"b",outcome:"resolved",observedAt:"2026-09-17T02:00:00Z"};test("SD4 immutable resolution",()=>{let r=buildStewardshipResolution(x);assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("SD4 binding and complete lineage",()=>{assert.throws(()=>buildStewardshipResolution({...x,decisionHash:"c".repeat(64)}),/mismatch/);assert.throws(()=>buildStewardshipResolution({...x,dutyRefs:[]}),/duties/);assert.throws(()=>buildStewardshipResolution({...x,evidenceRefs:["z"]}),/evidence/)});test("SD4 outcome roles and time",()=>{assert.equal(buildStewardshipResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildStewardshipResolution({...x,outcome:"open"}),/outcome/);assert.throws(()=>buildStewardshipResolution({...x,reviewerRef:"a"}),/independent/);assert.throws(()=>buildStewardshipResolution({...x,outcome:"expired"}),/not expired/)});test("SD4 privacy",()=>{assert.throws(()=>buildStewardshipResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildStewardshipResolution({...x,text:"t"}),/content/)})
