import test from"node:test";import assert from"node:assert/strict";import{buildBaselineContract}from"../src/baseline-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",requirementRefs:["qualified:request"],exceptionRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V174 hash-bound commission",()=>{let c=buildBaselineContract(x);assert.match(c.baselineHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V174 preserves scope",()=>{let c=buildBaselineContract(x);assert.deepEqual([c.requirementRefs,c.exceptionRefs,c.evidenceRefs],[x.requirementRefs,x.exceptionRefs,x.evidenceRefs])});test("V174 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildBaselineContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V174 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildBaselineContract({...x,expiresAt:x.validFrom}),/window/)})
