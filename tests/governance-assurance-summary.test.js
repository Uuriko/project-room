import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceAssuranceSummary}from"../src/governance-assurance-summary.js";const input={lanes:Array.from({length:25},(_,i)=>"l"+i),stages:Array(100).fill("s"),edges:Array(24).fill("e"),outcomes:["resolved","discharged","released","recalled","withdrawn","revoked","expired"],negativeBranches:["denied"]};test("289 constructs immutable deterministic report",()=>{let a=buildGovernanceAssuranceSummary(input),b=buildGovernanceAssuranceSummary(input);assert.deepEqual(a,b);assert.ok(Object.isFrozen(a))});