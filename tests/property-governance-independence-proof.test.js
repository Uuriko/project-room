import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceIndependenceProof as b}from"../src/governance-independence-proof.js";let c=(v,a,h)=>({checkHash:h.repeat(64),outcome:"pass",verifierId:v,affiliation:a,artifactHash:"f".repeat(64)}),x=[c("v1","a","1"),c("v2","b","2")];test("686 check order canonicalizes",()=>assert.deepEqual(b({checks:x}),b({checks:x.toReversed()})));test("686 failed check rejects",()=>assert.throws(()=>b({checks:[x[0],{...x[1],outcome:"fail"}]}),/invalid/))
