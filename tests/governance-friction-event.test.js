import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceFrictionEvent as build}from"../src/governance-friction-event.js";let b={eventId:"E1",kind:"retry",workflow:"handoff",sourceHash:"a".repeat(64),occurredAt:"2026-09-16T18:00:00Z"};test("621 friction event is canonical",()=>assert.deepEqual(build(b),build(b)));test("621 unknown kind and bad evidence fail closed",()=>{assert.throws(()=>build({...b,kind:"slow"}),/invalid/);assert.throws(()=>build({...b,sourceHash:"x"}),/invalid/)})
