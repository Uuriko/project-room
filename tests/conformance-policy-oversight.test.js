import test from"node:test";import assert from"node:assert/strict";import{buildPolicyDecision}from"../src/policy-decision.js";const p={policyHash:"a".repeat(64),operatorRef:"operator",policyRefs:["access"],constraintRefs:["limit"],evidenceRefs:["overseen","authorized"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},make=(outcome="active",conditionCodes=[])=>buildPolicyDecision({policy:p,policyHash:p.policyHash,policyRefs:p.policyRefs,constraintRefs:p.constraintRefs,evidenceRefs:p.evidenceRefs,operatorRef:"operator",approverRef:"approver",reviewerRef:"reviewer",outcome,conditionCodes,observedAt:"2026-09-17T02:00:00Z"});test("V99 complete authorized scope",()=>{let d=make();for(const k of["policyRefs","constraintRefs","evidenceRefs"])assert.deepEqual(d[k],[...p[k]].sort())});test("V99 active decision stays in window",()=>assert.equal(make().outcome,"active"));test("V99 non-active decisions carry conditions",()=>{assert.deepEqual(make("conditional",["limit"]).conditionCodes,["limit"]);assert.deepEqual(make("denied",["policy"]).conditionCodes,["policy"])});test("V99 roles remain separate",()=>{let d=make();assert.equal(new Set([d.operatorRef,d.approverRef,d.reviewerRef]).size,3);assert.ok(Object.isFrozen(d))})
