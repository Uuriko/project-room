import test from"node:test";import assert from"node:assert/strict";import{buildCustodyContract}from"../src/custody-contract.js";import{buildCustodyDecision}from"../src/custody-decision.js";import{buildCustodyException}from"../src/custody-exception.js";import{buildCustodyResolution}from"../src/custody-resolution.js";const chain=()=>{let c=buildCustodyContract({transferorRef:"t",custodianRef:"c",approverRef:"a",reviewerRef:"r",assetRefs:["room"],controlRefs:["review"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),d=buildCustodyDecision({contract:c,custodyHash:c.custodyHash,assetRefs:c.assetRefs,controlRefs:c.controlRefs,evidenceRefs:c.evidenceRefs,custodianRef:"c",approverRef:"a",reviewerRef:"r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"});return buildCustodyException({decision:d,decisionHash:d.decisionHash,assetRefs:d.assetRefs,controlRefs:d.controlRefs,safeguardRefs:["escrow"],evidenceRefs:["audit"],custodianRef:"c",approverRef:"a",reviewerRef:"r",observedAt:"2026-09-17T02:00:00Z",expiresAt:"2026-09-18T01:00:00Z"})},args=e=>({exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,assetRefs:e.assetRefs,controlRefs:e.controlRefs,safeguardRefs:e.safeguardRefs,evidenceRefs:e.evidenceRefs,resolverRef:"resolver",reviewerRef:"reviewer",outcome:"resolved",observedAt:"2026-09-17T03:00:00Z"});test("CU4 real-chain immutable resolution",()=>{let r=buildCustodyResolution(args(chain()));assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("CU4 binding and complete lineage",()=>{let e=chain(),x=args(e);assert.throws(()=>buildCustodyResolution({...x,decisionHash:"f".repeat(64)}),/mismatch/);assert.throws(()=>buildCustodyResolution({...x,safeguardRefs:[]}),/safeguards/)});test("CU4 outcome roles and time",()=>{let x=args(chain());assert.equal(buildCustodyResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildCustodyResolution({...x,reviewerRef:"resolver"}),/independent/);assert.throws(()=>buildCustodyResolution({...x,outcome:"expired"}),/not expired/)});test("CU4 privacy",()=>{let x=args(chain());assert.throws(()=>buildCustodyResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildCustodyResolution({...x,text:"t"}),/content/)})
