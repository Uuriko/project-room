import test from"node:test";import assert from"node:assert/strict";import{buildPolicyExceptionRecord}from"../src/policy-exception-record.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",policyRefs:["access"],constraintRefs:["limit"]},make=()=>buildPolicyExceptionRecord({decision:d,decisionHash:d.decisionHash,policyRefs:d.policyRefs,constraintRefs:d.constraintRefs,controlRefs:["monitor"],evidenceRefs:["compliant","validated"],ownerRef:"owner",approverRef:"approver",reviewerRef:"reviewer",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});test("V100 policy scope remains contained",()=>{let e=make();assert.deepEqual(e.policyRefs,d.policyRefs);assert.deepEqual(e.constraintRefs,d.constraintRefs)});test("V100 preserves controls and evidence",()=>{let e=make();assert.deepEqual(e.controlRefs,["monitor"]);assert.deepEqual(e.evidenceRefs,["compliant","validated"])});test("V100 exception remains bounded",()=>{let e=make();assert.ok(Date.parse(e.expiresAt)>Date.parse(e.observedAt))});test("V100 roles remain separate",()=>{let e=make();assert.equal(new Set([e.ownerRef,e.approverRef,e.reviewerRef]).size,3);assert.ok(Object.isFrozen(e))})
