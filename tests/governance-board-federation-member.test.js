import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceBoardFederationMember as build}from"../src/governance-board-federation-member.js";let b={boardId:"266",stateHash:"a".repeat(64),generationHash:"b".repeat(64),observedAt:"2026-09-16T19:00:00Z"};test("651 member is canonical and immutable",()=>{assert.deepEqual(build(b),build(b));assert.ok(Object.isFrozen(build(b)))});test("651 bad state and generation evidence fail closed",()=>{for(let k of["stateHash","generationHash"])assert.throws(()=>build({...b,[k]:"x"}),/invalid/)})
