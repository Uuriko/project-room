import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipContract}from"../src/stewardship-contract.js";const x={sponsorRef:"s",stewardRef:"t",approverRef:"a",reviewerRef:"r",assetRefs:["room"],dutyRefs:["review"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("SD1 immutable stewardship contract",()=>{let c=buildStewardshipContract(x);assert.match(c.stewardshipHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c));assert.ok(Object.isFrozen(c.assetRefs))});test("SD1 complete stewardship scope",()=>{for(const k of["assetRefs","dutyRefs","evidenceRefs"])assert.throws(()=>buildStewardshipContract({...x,[k]:[]}),/invalid/)});test("SD1 roles and window",()=>{assert.throws(()=>buildStewardshipContract({...x,reviewerRef:"s"}),/independent/);assert.throws(()=>buildStewardshipContract({...x,expiresAt:x.validFrom}),/window/)});test("SD1 privacy",()=>{assert.throws(()=>buildStewardshipContract({...x,email:"x"}),/PII/);assert.throws(()=>buildStewardshipContract({...x,content:"x"}),/content/)})
