import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceDataMinimizationRule as rule}from"../src/governance-data-minimization-rule.js";import{buildGovernanceRedactionAttestation as attest}from"../src/governance-redaction-attestation.js";import{buildGovernanceDisclosureSurfaceSummary as summary}from"../src/governance-disclosure-surface-summary.js";import{buildGovernanceMinimizationComplianceProof as proof}from"../src/governance-minimization-compliance-proof.js";const run=()=>{let r=rule({allowedFieldCodes:["hash","lane","stage"],purposeCode:"audit",retentionClass:"short"}),a=attest({sourceHash:"a".repeat(64),projectionHash:"b".repeat(64),ruleHash:r.ruleHash,removedFieldCounts:{identity:2,payload:1}}),s=summary({counts:{hash:1,lane:1}});return{r,a,s,p:proof({rule:r,attestation:a,summary:s})}};test("388 minimization chain is deterministic immutable compliant",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.equal(a.p.compliant,true);assert.ok(Object.isFrozen(a.a.removedFieldCounts))});test("388 private fields and forged counts fail closed",()=>{assert.throws(()=>rule({allowedFieldCodes:["email"],purposeCode:"audit",retentionClass:"short"}),/rule/);let x=run(),s=summary({counts:{hash:9}});assert.throws(()=>proof({rule:x.r,attestation:x.a,summary:s}),/forged/)});