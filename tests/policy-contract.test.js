import test from"node:test";import assert from"node:assert/strict";import{buildPolicyContract}from"../src/policy-contract.js";const x={sponsorRef:"s",operatorRef:"o",stewardRef:"t",approverRef:"a",reviewerRef:"r",policyRefs:["access"],constraintRefs:["limit"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("W1 immutable bounded policy",()=>{let p=buildPolicyContract(x);assert.match(p.policyHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(p))});test("W1 nonempty scope",()=>{for(const k of["policyRefs","constraintRefs","evidenceRefs"])assert.throws(()=>buildPolicyContract({...x,[k]:[]}),/invalid/)});test("W1 roles and window",()=>{assert.throws(()=>buildPolicyContract({...x,reviewerRef:"s"}),/independent/);assert.throws(()=>buildPolicyContract({...x,expiresAt:x.validFrom}),/window/)});test("W1 privacy",()=>{assert.throws(()=>buildPolicyContract({...x,email:"x"}),/PII/);assert.throws(()=>buildPolicyContract({...x,content:"x"}),/content/)})
