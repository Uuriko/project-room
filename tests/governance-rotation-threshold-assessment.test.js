import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceRotationThresholdAssessment as build}from"../src/governance-rotation-threshold-assessment.js";let b={generation:1,entryCount:499,ageHours:167,assessedAt:"2026-09-16T18:00:00Z"};test("613 below thresholds retains board",()=>assert.equal(build(b).rotate,false));test("613 either inclusive threshold rotates",()=>{assert.deepEqual(build({...b,entryCount:500}).reasons,["entry-count"]);assert.deepEqual(build({...b,ageHours:168}).reasons,["age"])})
