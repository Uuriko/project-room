import test from"node:test";import assert from"node:assert/strict";import{buildControlException}from"../src/control-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",controlRefs:["access"],safeguardRefs:["limit"]},make=()=>buildControlException({decision:d,decisionHash:d.decisionHash,controlRefs:d.controlRefs,safeguardRefs:d.safeguardRefs,measureRefs:["monitor"],evidenceRefs:["compliant","validated"],ownerRef:"owner",approverRef:"approver",reviewerRef:"reviewer",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});test("V104 control scope remains contained",()=>{let e=make();assert.deepEqual(e.controlRefs,d.controlRefs);assert.deepEqual(e.safeguardRefs,d.safeguardRefs)});test("V104 preserves measures and evidence",()=>{let e=make();assert.deepEqual(e.measureRefs,["monitor"]);assert.deepEqual(e.evidenceRefs,["compliant","validated"])});test("V104 exception remains bounded",()=>{let e=make();assert.ok(Date.parse(e.expiresAt)>Date.parse(e.observedAt))});test("V104 roles remain separate",()=>{let e=make();assert.equal(new Set([e.ownerRef,e.approverRef,e.reviewerRef]).size,3);assert.ok(Object.isFrozen(e))})
