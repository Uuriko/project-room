import test from"node:test";import assert from"node:assert/strict";import{buildApprovalContract}from"../src/approval-contract.js";import{buildApprovalDecision}from"../src/approval-decision.js";const c=()=>buildApprovalContract({sponsorRef:"s",ownerRef:"o",approverRef:"a",approvalerRef:"r",requestRefs:["request"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),args=c=>({contract:c,approvalHash:c.approvalHash,requestRefs:c.requestRefs,criterionRefs:c.criterionRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",approverRef:"a",approvalerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"});test("AP2 real-contract immutable decision",()=>{let d=buildApprovalDecision(args(c()));assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("AP2 exact evaluation",()=>{let x=args(c());assert.throws(()=>buildApprovalDecision({...x,requestRefs:[]}),/requests/);assert.throws(()=>buildApprovalDecision({...x,criterionRefs:["other"]}),/criteria/)});test("AP2 outcomes roles and window",()=>{let x=args(c());assert.equal(buildApprovalDecision({...x,outcome:"conditional",conditionCodes:["monitor"]}).outcome,"conditional");assert.throws(()=>buildApprovalDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildApprovalDecision({...x,approvalerRef:"o"}),/independent/);assert.throws(()=>buildApprovalDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("AP2 privacy",()=>{let x=args(c());assert.throws(()=>buildApprovalDecision({...x,email:"x"}),/PII/);assert.throws(()=>buildApprovalDecision({...x,content:"x"}),/content/)})
