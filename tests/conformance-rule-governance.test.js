import test from"node:test";import assert from"node:assert/strict";import{buildRuleContract}from"../src/rule-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",requirementRefs:["qualified:request"],limitRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V182 hash-bound commission",()=>{let c=buildRuleContract(x);assert.match(c.ruleHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V182 preserves scope",()=>{let c=buildRuleContract(x);assert.deepEqual([c.requirementRefs,c.limitRefs,c.evidenceRefs],[x.requirementRefs,x.limitRefs,x.evidenceRefs])});test("V182 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildRuleContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V182 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildRuleContract({...x,expiresAt:x.validFrom}),/window/)})
