import test from"node:test";import assert from"node:assert/strict";import{buildAssuranceCaseContract}from"../src/assurance-case-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",assessorRef:"qualification:a",reviewerRef:"governance:r",claimRefs:["qualified:request"],constraintRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V194 hash-bound commission",()=>{let c=buildAssuranceCaseContract(x);assert.match(c.assuranceCaseHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V194 preserves scope",()=>{let c=buildAssuranceCaseContract(x);assert.deepEqual([c.claimRefs,c.constraintRefs,c.evidenceRefs],[x.claimRefs,x.constraintRefs,x.evidenceRefs])});test("V194 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.assessorRef,x.reviewerRef]).size,4);assert.throws(()=>buildAssuranceCaseContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V194 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildAssuranceCaseContract({...x,expiresAt:x.validFrom}),/window/)})
