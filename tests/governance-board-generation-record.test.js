import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceBoardGenerationRecord as build}from"../src/governance-board-generation-record.js";let x={boardId:"266",generation:1,parentHash:null,stateHash:"a".repeat(64),openedAt:"2026-09-16T18:00:00Z"};test("611 genesis generation is canonical",()=>{assert.deepEqual(build(x),build(x));assert.ok(Object.isFrozen(build(x)))});test("611 later generation requires parent",()=>assert.throws(()=>build({...x,generation:2}),/invalid/))
