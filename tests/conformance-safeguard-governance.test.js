import test from"node:test";import assert from"node:assert/strict";import{buildSafeguardContract}from"../src/safeguard-contract.js";const x={sponsorRef:"governance:s",protectorRef:"assurance:o",operatorRef:"qualification:a",reviewerRef:"governance:r",protectionRefs:["qualified:request"],protectTriggerRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V166 hash-bound safeguard",()=>{let c=buildSafeguardContract(x);assert.match(c.safeguardHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V166 preserves scope",()=>{let c=buildSafeguardContract(x);assert.deepEqual([c.protectionRefs,c.protectTriggerRefs,c.evidenceRefs],[x.protectionRefs,x.protectTriggerRefs,x.evidenceRefs])});test("V166 roles",()=>{assert.equal(new Set([x.sponsorRef,x.protectorRef,x.operatorRef,x.reviewerRef]).size,4);assert.throws(()=>buildSafeguardContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V166 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildSafeguardContract({...x,expiresAt:x.validFrom}),/window/)})
