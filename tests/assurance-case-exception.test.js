import test from"node:test";import assert from"node:assert/strict";import{buildAssuranceCaseContract}from"../src/assurance-case-contract.js";import{buildAssuranceCaseDecision}from"../src/assurance-case-decision.js";import{buildAssuranceCaseException}from"../src/assurance-case-exception.js";const chain=()=>{let c=buildAssuranceCaseContract({sponsorRef:"s",ownerRef:"o",assessorRef:"r",reviewerRef:"a",claimRefs:["request"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildAssuranceCaseDecision({contract:c,assuranceCaseHash:c.assuranceCaseHash,claimRefs:c.claimRefs,constraintRefs:c.constraintRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",assessorRef:"r",reviewerRef:"a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})},args=d=>({decision:d,decisionHash:d.decisionHash,claimRefs:d.claimRefs,constraintRefs:d.constraintRefs,controlRefs:["commission"],evidenceRefs:["audit"],ownerRef:"o",assessorRef:"r",reviewerRef:"a",observedAt:"2026-09-17T02:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});test("AC3 real-chain immutable exception",()=>{let e=buildAssuranceCaseException(args(chain()));assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("AC3 conditional contained scope",()=>{let d=chain(),x=args(d);assert.throws(()=>buildAssuranceCaseException(args({...d,outcome:"active"})),/conditional/);assert.throws(()=>buildAssuranceCaseException({...x,constraintRefs:["other"]}),/scope/)});test("AC3 controls roles expiry",()=>{let x=args(chain());assert.throws(()=>buildAssuranceCaseException({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildAssuranceCaseException({...x,assessorRef:"o"}),/independent/);assert.throws(()=>buildAssuranceCaseException({...x,expiresAt:x.observedAt}),/expiry/)});test("AC3 privacy",()=>{let x=args(chain());assert.throws(()=>buildAssuranceCaseException({...x,email:"x"}),/PII/);assert.throws(()=>buildAssuranceCaseException({...x,content:"x"}),/content/)})
