import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceHealthMetricRecord as metric}from"../src/governance-health-metric-record.js";import{buildGovernanceDigestSnapshot as digest}from"../src/governance-digest-snapshot.js";import{buildGovernanceRotationThresholdAssessment as assess}from"../src/governance-rotation-threshold-assessment.js";let at="2026-09-16T18:00:00Z";const run=()=>{let h=metric({metric:"entries",value:500,unit:"count",level:"degraded",sourceHash:"a".repeat(64),sampledAt:at}),d=digest({healthMetrics:[h],trustMetrics:[],windowStartsAt:"2026-09-16T17:00:00Z",windowEndsAt:"2026-09-16T19:00:00Z"}),a=assess({generation:1,entryCount:d.health[0].value,ageHours:1,assessedAt:at});return{d,a}};test("643 digest count reaches rotation threshold",()=>assert.equal(run().a.rotate,true));test("643 digest-to-rotation is deterministic",()=>assert.deepEqual(run(),run()))
