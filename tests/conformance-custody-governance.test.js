import test from"node:test";import assert from"node:assert/strict";import{buildCustodyContract}from"../src/custody-contract.js";const x={transferorRef:"governance:g",custodianRef:"assurance:c",approverRef:"qualification:a",reviewerRef:"governance:r",assetRefs:["qualified:asset"],controlRefs:["assured:control"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V126 governed custody is hash-bound",()=>{let c=buildCustodyContract(x);assert.match(c.custodyHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V126 preserves asset control evidence bounds",()=>{let c=buildCustodyContract(x);assert.deepEqual(c.assetRefs,x.assetRefs);assert.deepEqual(c.controlRefs,x.controlRefs);assert.deepEqual(c.evidenceRefs,x.evidenceRefs)});test("V126 preserves role separation",()=>{assert.throws(()=>buildCustodyContract({...x,reviewerRef:x.transferorRef}),/independent/);assert.equal(new Set([x.transferorRef,x.custodianRef,x.approverRef,x.reviewerRef]).size,4)});test("V126 preserves qualification window",()=>{assert.throws(()=>buildCustodyContract({...x,expiresAt:x.validFrom}),/window/);assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom))})
