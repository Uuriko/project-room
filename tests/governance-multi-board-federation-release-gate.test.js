import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceFederationReceipt as receipt}from"../src/governance-federation-receipt.js";let manifest={manifestHash:"a".repeat(64)},claimProof={proofHash:"b".repeat(64),manifestHash:manifest.manifestHash,targetBoardId:"b",claimedAt:"2026-09-16T19:00:00Z"};test("660 release accepts target-board receipt",()=>assert.equal(receipt({manifest,claimProof,acceptedBy:"b",acceptedAt:"2026-09-16T19:01:00Z"}).accepted,true));test("660 release rejects non-target acceptance",()=>assert.throws(()=>receipt({manifest,claimProof,acceptedBy:"a",acceptedAt:"2026-09-16T19:01:00Z"}),/invalid/))
