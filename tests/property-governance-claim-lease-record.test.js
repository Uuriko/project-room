import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceProtocolActionRecord as action}from"../src/governance-protocol-action-record.js";import{buildGovernanceClaimLeaseRecord as lease}from"../src/governance-claim-lease-record.js";import{buildGovernanceHeartbeatRecord as heartbeat}from"../src/governance-heartbeat-record.js";import{buildGovernanceHandoffReceipt as handoff}from"../src/governance-handoff-receipt.js";const run=()=>{let a=action({actionCode:"claim",taskId:"T1",lane:"instinct",state:"working",reason:"build",observedAt:"2026-09-17T01:00:00Z"}),l=lease({actionHash:a.actionHash,leaseHours:24,heartbeatMinutes:60,startsAt:a.observedAt}),h=heartbeat({leaseHash:l.leaseHash,progressCode:"tests-green",observedAt:"2026-09-17T02:00:00Z"}),r=handoff({actionHash:a.actionHash,fromLane:"instinct",toLane:"quill",acceptedAt:"2026-09-17T03:00:00Z"});return{a,l,h,r}};test("554 claim heartbeat handoff protocol is deterministic",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.equal(a.l.leaseHours,24);assert.equal(a.l.heartbeatMinutes,60);assert.ok(Object.isFrozen(a.r))});test("554 invalid state and self handoff fail closed",()=>{let x=run();assert.throws(()=>action({actionCode:"claim",taskId:"T",lane:"x",state:"done",reason:"x",observedAt:"2026-09-17T01:00:00Z"}),/action/);assert.throws(()=>handoff({actionHash:x.a.actionHash,fromLane:"x",toLane:"x",acceptedAt:"2026-09-17T03:00:00Z"}),/handoff/)});