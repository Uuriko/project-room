import test from"node:test";import assert from"node:assert/strict";import{buildDelegatedControlContract}from"../src/delegated-control-contract.js";import{buildDelegatedControlDecision}from"../src/delegated-control-decision.js";const make=()=>{let c=buildDelegatedControlContract({delegatorRef:"s",delegateRef:"oversight:o",controllerRef:"entitlement:r",reviewerRef:"authorization:a",controlRefs:["subject"],limitRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildDelegatedControlDecision({contract:c,delegatedControlHash:c.delegatedControlHash,controlRefs:c.controlRefs,limitRefs:c.limitRefs,evidenceRefs:c.evidenceRefs,delegateRef:"oversight:o",controllerRef:"entitlement:r",reviewerRef:"authorization:a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V163 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.controlRefs,d.limitRefs,d.evidenceRefs],[['subject'],['criterion'],['commission']])});test("V163 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V163 timing and roles",()=>{let d=make();assert.equal(new Set([d.delegateRef,d.controllerRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V163 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
