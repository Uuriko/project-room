import test from"node:test";import assert from"node:assert/strict";import{buildGateContract}from"../src/gate-contract.js";const x={sponsorRef:"governance:s",gatekeeperRef:"assurance:o",evaluatorRef:"qualification:a",reviewerRef:"governance:r",criterionRefs:["qualified:request"],evidenceRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V170 hash-bound gate",()=>{let c=buildGateContract(x);assert.match(c.gateHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V170 preserves scope",()=>{let c=buildGateContract(x);assert.deepEqual([c.criterionRefs,c.evidenceRefs],[x.criterionRefs,x.evidenceRefs])});test("V170 roles",()=>{assert.equal(new Set([x.sponsorRef,x.gatekeeperRef,x.evaluatorRef,x.reviewerRef]).size,4);assert.throws(()=>buildGateContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V170 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildGateContract({...x,expiresAt:x.validFrom}),/window/)})
