import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceArchiveInclusionProof as b}from"../src/governance-archive-inclusion-proof.js";let m={manifestHash:"a".repeat(64),artifactHashes:["b".repeat(64)]};test("733 exact index proves inclusion",()=>assert.equal(b({manifest:m,artifactHash:m.artifactHashes[0],index:0}).index,0));test("733 wrong index rejects",()=>assert.throws(()=>b({manifest:m,artifactHash:m.artifactHashes[0],index:1}),/invalid/))
