import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceBoardFederationMember as build}from"../src/governance-board-federation-member.js";let b={boardId:"266",stateHash:"a".repeat(64),generationHash:"b".repeat(64),observedAt:"2026-09-16T19:00:00Z"};test("652 board, state and generation changes separate member identity",()=>{let x=build(b);for(let p of[{boardId:"267"},{stateHash:"c".repeat(64)},{generationHash:"c".repeat(64)}])assert.notEqual(x.memberHash,build({...b,...p}).memberHash)});test("652 equivalent instants canonicalize",()=>assert.deepEqual(build(b),build({...b,observedAt:"2026-09-16T12:00:00-07:00"})))
