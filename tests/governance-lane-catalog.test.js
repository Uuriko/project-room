import test from"node:test";import assert from"node:assert/strict";import{governanceLanes}from"../src/governance-lane-catalog.js";test("251 catalog is nonempty unique and frozen",()=>{assert.ok(governanceLanes.length);assert.equal(new Set(governanceLanes).size,governanceLanes.length);assert.ok(Object.isFrozen(governanceLanes))});test("251 catalog excludes private content",()=>assert.equal(JSON.stringify(governanceLanes).includes("email"),false));