import test from"node:test";import assert from"node:assert/strict";import{buildEscalationContract}from"../src/escalation-contract.js";const x={sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",triggerRefs:["threshold"],routeRefs:["governance"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("ES1 immutable governed escalation",()=>{let c=buildEscalationContract(x);assert.match(c.escalationHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c));assert.ok(Object.isFrozen(c.triggerRefs))});test("ES1 complete trigger route scope",()=>{for(const k of["triggerRefs","routeRefs","evidenceRefs"])assert.throws(()=>buildEscalationContract({...x,[k]:[]}),/invalid/)});test("ES1 roles and window",()=>{assert.throws(()=>buildEscalationContract({...x,reviewerRef:"s"}),/independent/);assert.throws(()=>buildEscalationContract({...x,expiresAt:x.validFrom}),/window/)});test("ES1 excludes attention content",()=>{assert.throws(()=>buildEscalationContract({...x,attention:"x"}),/attention/);assert.throws(()=>buildEscalationContract({...x,content:"x"}),/content/)})
