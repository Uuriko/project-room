import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";const x={sponsorRef:"s",ownerRef:"o",executorRef:"a",reviewerRef:"r",actionRefs:["request"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("DT1 immutable commission",()=>{let c=buildDirectiveContract(x);assert.match(c.directiveHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("DT1 complete scope",()=>{for(const k of["actionRefs","constraintRefs","evidenceRefs"])assert.throws(()=>buildDirectiveContract({...x,[k]:[]}),/invalid/)});test("DT1 roles window",()=>{assert.throws(()=>buildDirectiveContract({...x,ownerRef:"s"}),/independent/);assert.throws(()=>buildDirectiveContract({...x,expiresAt:x.validFrom}),/window/)});test("DT1 privacy",()=>{assert.throws(()=>buildDirectiveContract({...x,email:"x"}),/PII/);assert.throws(()=>buildDirectiveContract({...x,content:"x"}),/content/)})
