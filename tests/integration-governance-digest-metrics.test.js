import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceHealthMetricRecord as health}from"../src/governance-health-metric-record.js";import{buildGovernanceTrustMetricRecord as trust}from"../src/governance-trust-metric-record.js";import{buildGovernanceDigestSnapshot as digest}from"../src/governance-digest-snapshot.js";import{buildGovernanceDigestPublicationReceipt as publish}from"../src/governance-digest-publication-receipt.js";const run=()=>{let sampledAt="2026-09-16T18:00:00Z",h=health({metric:"open_tasks",value:3,unit:"count",level:"healthy",sourceHash:"a".repeat(64),sampledAt}),t=trust({metric:"handoff",score:90,sourceHash:"b".repeat(64),sampledAt}),d=digest({healthMetrics:[h],trustMetrics:[t],windowStartsAt:"2026-09-16T17:00:00Z",windowEndsAt:"2026-09-16T19:00:00Z"}),p=publish({snapshotHash:d.snapshotHash,channel:"issue:266",sourceHeadHash:"c".repeat(64),publishedAt:"2026-09-16T19:01:00Z"});return{h,t,d,p}};test("598 metric digest publishes end to end",()=>{let x=run();assert.equal(x.p.snapshotHash,x.d.snapshotHash);assert.equal(x.t.authority,false);assert.equal(x.p.authority,false)});test("598 end-to-end publication is deterministic",()=>assert.deepEqual(run(),run()))
