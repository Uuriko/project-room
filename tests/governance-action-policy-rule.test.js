import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceActionPolicyRule as build}from"../src/governance-action-policy-rule.js";let input={ruleId:"R1",action:"deploy",effect:"confirm-owner",scope:"repo:project-room",revision:1};test("601 action policy rules are deterministic and immutable",()=>{assert.deepEqual(build(input),build(input));assert.ok(Object.isFrozen(build(input)))});test("601 unknown effects and malformed actions fail closed",()=>{assert.throws(()=>build({...input,effect:"trust"}),/invalid/);assert.throws(()=>build({...input,action:"Deploy now"}),/invalid/)})
