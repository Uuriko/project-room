import test from"node:test";import assert from"node:assert/strict";import{buildAssignmentContract}from"../src/assignment-contract.js";const x={assignerRef:"governance:s",assigneeRef:"assurance:o",reviewerRef:"qualification:a",assignmenterRef:"governance:r",dutyRefs:["qualified:request"],limitRefs:["assured:criterion"],evidenceRefs:["governed:assignment"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V154 hash-bound assignment",()=>{let c=buildAssignmentContract(x);assert.match(c.assignmentHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V154 preserves scope",()=>{let c=buildAssignmentContract(x);assert.deepEqual([c.dutyRefs,c.limitRefs,c.evidenceRefs],[x.dutyRefs,x.limitRefs,x.evidenceRefs])});test("V154 roles",()=>{assert.equal(new Set([x.assignerRef,x.assigneeRef,x.reviewerRef,x.assignmenterRef]).size,4);assert.throws(()=>buildAssignmentContract({...x,assignmenterRef:x.sponsorRef}),/independent/)});test("V154 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildAssignmentContract({...x,expiresAt:x.validFrom}),/window/)})
