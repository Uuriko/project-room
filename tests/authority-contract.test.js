import test from"node:test";import assert from"node:assert/strict";import{buildAuthorityContract}from"../src/authority-contract.js";const x={grantorRef:"g",ownerRef:"o",approverRef:"a",reviewerRef:"r",authorityRefs:["read"],limitRefs:["no-share"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("AC1 immutable authority contract",()=>{let c=buildAuthorityContract(x);assert.match(c.authorityHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c));assert.ok(Object.isFrozen(c.authorityRefs))});test("AC1 complete authority scope",()=>{for(const k of["authorityRefs","limitRefs","evidenceRefs"])assert.throws(()=>buildAuthorityContract({...x,[k]:[]}),/invalid/)});test("AC1 roles and window",()=>{assert.throws(()=>buildAuthorityContract({...x,reviewerRef:"g"}),/independent/);assert.throws(()=>buildAuthorityContract({...x,expiresAt:x.validFrom}),/window/)});test("AC1 privacy",()=>{assert.throws(()=>buildAuthorityContract({...x,email:"x"}),/PII/);assert.throws(()=>buildAuthorityContract({...x,content:"x"}),/content/)})
