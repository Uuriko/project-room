import test from"node:test";import assert from"node:assert/strict";import{buildEscalationContract}from"../src/escalation-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",triggerRefs:["qualified:threshold"],routeRefs:["assured:governance"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V130 governed escalation is hash-bound",()=>{let c=buildEscalationContract(x);assert.match(c.escalationHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V130 preserves trigger route evidence",()=>{let c=buildEscalationContract(x);assert.deepEqual([c.triggerRefs,c.routeRefs,c.evidenceRefs],[x.triggerRefs,x.routeRefs,x.evidenceRefs])});test("V130 preserves roles and window",()=>{assert.throws(()=>buildEscalationContract({...x,reviewerRef:x.sponsorRef}),/independent/);assert.throws(()=>buildEscalationContract({...x,expiresAt:x.validFrom}),/window/)});test("V130 excludes room attention semantics",()=>{assert.throws(()=>buildEscalationContract({...x,attention:"urgent"}),/attention/);assert.throws(()=>buildEscalationContract({...x,workLoop:"x"}),/attention/)})
