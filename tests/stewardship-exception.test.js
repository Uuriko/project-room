import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipException}from"../src/stewardship-exception.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",assetRefs:["read"],dutyRefs:["no-share"]},x={decision:d,decisionHash:d.decisionHash,assetRefs:d.assetRefs,dutyRefs:d.dutyRefs,controlRefs:["review"],evidenceRefs:["e"],stewardRef:"o",approverRef:"a",reviewerRef:"r",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("SD3 immutable bounded exception",()=>{let e=buildStewardshipException(x);assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("SD3 conditional and contained",()=>{assert.throws(()=>buildStewardshipException({...x,decision:{...d,outcome:"active"}}),/conditional/);assert.throws(()=>buildStewardshipException({...x,assetRefs:["write"]}),/scope/)});test("SD3 controls roles and expiry",()=>{assert.throws(()=>buildStewardshipException({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildStewardshipException({...x,evidenceRefs:[]}),/evidence/);assert.throws(()=>buildStewardshipException({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildStewardshipException({...x,expiresAt:x.observedAt}),/expiry/)});test("SD3 privacy",()=>{assert.throws(()=>buildStewardshipException({...x,address:"x"}),/PII/);assert.throws(()=>buildStewardshipException({...x,payload:"x"}),/content/)})
