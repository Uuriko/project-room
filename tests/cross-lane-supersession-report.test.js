import test from"node:test";import assert from"node:assert/strict";import{buildCrossLaneSupersessionReport}from"../src/cross-lane-supersession-report.js";const input={records:[{priorHash:"a".repeat(64),replacementHash:"b".repeat(64)}],manifestHashes:["b".repeat(64)]};test("287 constructs immutable deterministic report",()=>{let a=buildCrossLaneSupersessionReport(input),b=buildCrossLaneSupersessionReport(input);assert.deepEqual(a,b);assert.ok(Object.isFrozen(a))});