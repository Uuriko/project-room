import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";import{buildDirectiveDecision}from"../src/directive-decision.js";const c=()=>buildDirectiveContract({sponsorRef:"s",ownerRef:"o",executorRef:"a",reviewerRef:"r",actionRefs:["request"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),args=c=>({contract:c,directiveHash:c.directiveHash,actionRefs:c.actionRefs,constraintRefs:c.constraintRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",executorRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"});test("DT2 real-contract immutable decision",()=>{let d=buildDirectiveDecision(args(c()));assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("DT2 exact evaluation",()=>{let x=args(c());assert.throws(()=>buildDirectiveDecision({...x,actionRefs:[]}),/objectives/);assert.throws(()=>buildDirectiveDecision({...x,constraintRefs:["other"]}),/constraints/)});test("DT2 outcomes roles and window",()=>{let x=args(c());assert.equal(buildDirectiveDecision({...x,outcome:"conditional",conditionCodes:["monitor"]}).outcome,"conditional");assert.throws(()=>buildDirectiveDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildDirectiveDecision({...x,executorRef:"o"}),/independent/);assert.throws(()=>buildDirectiveDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("DT2 privacy",()=>{let x=args(c());assert.throws(()=>buildDirectiveDecision({...x,email:"x"}),/PII/);assert.throws(()=>buildDirectiveDecision({...x,content:"x"}),/content/)})
