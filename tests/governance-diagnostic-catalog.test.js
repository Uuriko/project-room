import test from"node:test";import assert from"node:assert/strict";import{governanceDiagnostics}from"../src/governance-diagnostic-catalog.js";test("257 catalog is nonempty unique and frozen",()=>{assert.ok(governanceDiagnostics.length);assert.equal(new Set(governanceDiagnostics).size,governanceDiagnostics.length);assert.ok(Object.isFrozen(governanceDiagnostics))});test("257 catalog excludes private content",()=>assert.equal(JSON.stringify(governanceDiagnostics).includes("email"),false));