import test from"node:test";import assert from"node:assert/strict";import{buildVarianceContract}from"../src/variance-contract.js";import{buildVarianceDecision}from"../src/variance-decision.js";const make=()=>{let c=buildVarianceContract({sponsorRef:"s",ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",baselineRefs:["baseline"],deviationRefs:["deviation"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildVarianceDecision({contract:c,varianceHash:c.varianceHash,baselineRefs:c.baselineRefs,deviationRefs:c.deviationRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V143 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.baselineRefs,d.deviationRefs,d.evidenceRefs],[['baseline'],['deviation'],['approval']])});test("V143 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V143 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.approverRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V143 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
