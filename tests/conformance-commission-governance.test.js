import test from"node:test";import assert from"node:assert/strict";import{buildCommissionContract}from"../src/commission-contract.js";const x={sponsorRef:"governance:s",commissionerRef:"assurance:o",executorRef:"qualification:a",reviewerRef:"governance:r",objectiveRefs:["qualified:request"],constraintRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V158 hash-bound commission",()=>{let c=buildCommissionContract(x);assert.match(c.commissionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V158 preserves scope",()=>{let c=buildCommissionContract(x);assert.deepEqual([c.objectiveRefs,c.constraintRefs,c.evidenceRefs],[x.objectiveRefs,x.constraintRefs,x.evidenceRefs])});test("V158 roles",()=>{assert.equal(new Set([x.sponsorRef,x.commissionerRef,x.executorRef,x.reviewerRef]).size,4);assert.throws(()=>buildCommissionContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V158 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildCommissionContract({...x,expiresAt:x.validFrom}),/window/)})
