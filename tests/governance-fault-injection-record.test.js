import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceFaultInjectionRecord as b}from"../src/governance-fault-injection-record.js";let x={plan:{planHash:"a".repeat(64)},faultHash:"b".repeat(64),injectedAt:"2026-09-16T19:00:00Z",contained:true};test("703 contained fault records",()=>assert.equal(b(x).contained,true));test("703 uncontained fault rejects",()=>assert.throws(()=>b({...x,contained:false}),/invalid/))
