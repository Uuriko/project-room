import test from"node:test";import assert from"node:assert/strict";import{buildStandardContract}from"../src/standard-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",requirementRefs:["qualified:request"],controlRefs:["assured:criterion"],evidenceRefs:["governed:commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V178 hash-bound commission",()=>{let c=buildStandardContract(x);assert.match(c.standardHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V178 preserves scope",()=>{let c=buildStandardContract(x);assert.deepEqual([c.requirementRefs,c.controlRefs,c.evidenceRefs],[x.requirementRefs,x.controlRefs,x.evidenceRefs])});test("V178 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildStandardContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V178 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildStandardContract({...x,expiresAt:x.validFrom}),/window/)})
