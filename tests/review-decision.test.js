import test from"node:test";import assert from"node:assert/strict";import{buildReviewContract}from"../src/review-contract.js";import{buildReviewDecision}from"../src/review-decision.js";const c=()=>buildReviewContract({sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",subjectRefs:["subject"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),args=c=>({contract:c,reviewHash:c.reviewHash,subjectRefs:c.subjectRefs,criterionRefs:c.criterionRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",approverRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"});test("RV2 real-contract immutable decision",()=>{let d=buildReviewDecision(args(c()));assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("RV2 exact evaluation",()=>{let x=args(c());assert.throws(()=>buildReviewDecision({...x,subjectRefs:[]}),/subjects/);assert.throws(()=>buildReviewDecision({...x,criterionRefs:["other"]}),/criteria/)});test("RV2 outcomes roles and window",()=>{let x=args(c());assert.equal(buildReviewDecision({...x,outcome:"conditional",conditionCodes:["monitor"]}).outcome,"conditional");assert.throws(()=>buildReviewDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildReviewDecision({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildReviewDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("RV2 privacy",()=>{let x=args(c());assert.throws(()=>buildReviewDecision({...x,email:"x"}),/PII/);assert.throws(()=>buildReviewDecision({...x,content:"x"}),/content/)})
