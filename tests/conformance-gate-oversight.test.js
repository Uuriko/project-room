import test from"node:test";import assert from"node:assert/strict";import{buildGateContract}from"../src/gate-contract.js";import{buildGateDecision}from"../src/gate-decision.js";const make=()=>{let c=buildGateContract({sponsorRef:"s",gatekeeperRef:"oversight:o",evaluatorRef:"entitlement:r",reviewerRef:"authorization:a",criterionRefs:["subject"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildGateDecision({contract:c,gateHash:c.gateHash,criterionRefs:c.criterionRefs,evidenceRefs:c.evidenceRefs,gatekeeperRef:"oversight:o",evaluatorRef:"entitlement:r",reviewerRef:"authorization:a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V171 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.criterionRefs,d.evidenceRefs],[['subject'],['commission']])});test("V171 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V171 timing and roles",()=>{let d=make();assert.equal(new Set([d.gatekeeperRef,d.evaluatorRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V171 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
