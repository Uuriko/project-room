import test from"node:test";import assert from"node:assert/strict";import{buildApprovalContract}from"../src/approval-contract.js";const x={sponsorRef:"s",ownerRef:"o",approverRef:"a",approvalerRef:"r",requestRefs:["request"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("AP1 immutable approval",()=>{let c=buildApprovalContract(x);assert.match(c.approvalHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("AP1 complete scope",()=>{for(const k of["requestRefs","criterionRefs","evidenceRefs"])assert.throws(()=>buildApprovalContract({...x,[k]:[]}),/invalid/)});test("AP1 roles window",()=>{assert.throws(()=>buildApprovalContract({...x,approvalerRef:"s"}),/independent/);assert.throws(()=>buildApprovalContract({...x,expiresAt:x.validFrom}),/window/)});test("AP1 privacy",()=>{assert.throws(()=>buildApprovalContract({...x,email:"x"}),/PII/);assert.throws(()=>buildApprovalContract({...x,content:"x"}),/content/)})
