import test from"node:test";import assert from"node:assert/strict";import{buildWaiverContract}from"../src/waiver-contract.js";import{buildWaiverDecision}from"../src/waiver-decision.js";const make=()=>{let c=buildWaiverContract({sponsorRef:"s",ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",requirementRefs:["requirement"],controlRefs:["compensating"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildWaiverDecision({contract:c,waiverHash:c.waiverHash,requirementRefs:c.requirementRefs,controlRefs:c.controlRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V139 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.requirementRefs,d.controlRefs,d.evidenceRefs],[['requirement'],['compensating'],['approval']])});test("V139 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V139 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.approverRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V139 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
