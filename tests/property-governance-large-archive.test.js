import test from"node:test";import assert from"node:assert/strict";import{createHash}from"node:crypto";import{buildGovernanceArchiveManifest as b}from"../src/governance-archive-manifest.js";let hashes=Array.from({length:1000},(_,i)=>createHash("sha256").update(String(i)).digest("hex"));test("746 thousand-artifact archive canonicalizes",()=>assert.equal(b({archiveId:"large",artifactHashes:hashes.toReversed(),createdAt:"2026-09-16T19:00:00Z"}).artifactHashes.length,1000));test("746 large archive immutable",()=>assert.ok(Object.isFrozen(b({archiveId:"large",artifactHashes:hashes,createdAt:"2026-09-16T19:00:00Z"}))))
