import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";import{buildDirectiveDecision}from"../src/directive-decision.js";const make=()=>{let c=buildDirectiveContract({sponsorRef:"s",ownerRef:"oversight:o",executorRef:"entitlement:r",reviewerRef:"authorization:a",actionRefs:["subject"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildDirectiveDecision({contract:c,directiveHash:c.directiveHash,actionRefs:c.actionRefs,constraintRefs:c.constraintRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",executorRef:"entitlement:r",reviewerRef:"authorization:a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V187 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.actionRefs,d.constraintRefs,d.evidenceRefs],[['subject'],['criterion'],['commission']])});test("V187 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V187 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.executorRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V187 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
