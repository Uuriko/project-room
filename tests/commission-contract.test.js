import test from"node:test";import assert from"node:assert/strict";import{buildCommissionContract}from"../src/commission-contract.js";const x={sponsorRef:"s",commissionerRef:"o",executorRef:"a",reviewerRef:"r",objectiveRefs:["request"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("CM1 immutable commission",()=>{let c=buildCommissionContract(x);assert.match(c.commissionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("CM1 complete scope",()=>{for(const k of["objectiveRefs","constraintRefs","evidenceRefs"])assert.throws(()=>buildCommissionContract({...x,[k]:[]}),/invalid/)});test("CM1 roles window",()=>{assert.throws(()=>buildCommissionContract({...x,commissionerRef:"s"}),/independent/);assert.throws(()=>buildCommissionContract({...x,expiresAt:x.validFrom}),/window/)});test("CM1 privacy",()=>{assert.throws(()=>buildCommissionContract({...x,email:"x"}),/PII/);assert.throws(()=>buildCommissionContract({...x,content:"x"}),/content/)})
