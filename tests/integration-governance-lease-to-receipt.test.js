import test from"node:test";import{createHash}from"node:crypto";import assert from"node:assert/strict";import{buildGovernanceDecayPolicy as policy}from"../src/governance-decay-policy.js";import{buildGovernanceExpiryAssessment as assess}from"../src/governance-expiry-assessment.js";import{buildGovernanceReclaimRecord as reclaim}from"../src/governance-reclaim-record.js";import{buildGovernanceTwoStrikeProof as proof}from"../src/governance-two-strike-proof.js";const run=()=>{let p=policy({leaseHours:24,heartbeatMinutes:60,strikeGraceHours:4}),a=assess({policy:p,startsAt:"2026-09-15T18:00:00Z",lastHeartbeatAt:"2026-09-15T18:00:00Z",observedAt:"2026-09-16T19:00:00Z"}),expiredLeaseHash=createHash("sha256").update(JSON.stringify(a)).digest("hex"),x=reclaim({taskId:"T",expiredLeaseHash,strike:1,observedAt:"2026-09-16T19:00:00Z"}),y=reclaim({taskId:"T",expiredLeaseHash,strike:2,observedAt:"2026-09-16T23:00:00Z"});return proof({first:x,second:y})};test("642 expired lease reaches release receipt",()=>assert.equal(run().released,true));test("642 lease receipt flow is deterministic",()=>assert.deepEqual(run(),run()))
