import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceTasks1To749FullStack as b}from"../src/governance-tasks1-749-full-stack.js";let x={protocolHash:"a".repeat(64),stateHash:"b".repeat(64),assuranceHash:"c".repeat(64),archiveHash:"d".repeat(64)};test("750 completion gate accepts all layers",()=>{let s=b(x);assert.equal(s.complete,true);assert.ok(Object.isFrozen(s))});test("750 completion gate rejects absent evidence",()=>assert.throws(()=>b({...x,assuranceHash:"x"}),/invalid/))
