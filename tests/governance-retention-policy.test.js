import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceRetentionPolicy as build}from"../src/governance-retention-policy.js";test("661 policy canonicalizes holds",()=>assert.deepEqual(build({policyId:"p",holdKinds:["audit","legal"]}).holdKinds,["audit","legal"]));test("661 zero duration and duplicate holds fail closed",()=>{assert.throws(()=>build({policyId:"p",minimumDays:0}),/invalid/);assert.throws(()=>build({policyId:"p",holdKinds:["audit","audit"]}),/invalid/)})
