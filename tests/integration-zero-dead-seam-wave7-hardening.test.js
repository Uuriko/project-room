import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceEnrollmentChecklist as enroll}from"../src/governance-enrollment-checklist.js";import{buildGovernanceRotationThresholdAssessment as rotate}from"../src/governance-rotation-threshold-assessment.js";test("648 wave7 seams expose live constructors",()=>{assert.equal(typeof enroll,"function");assert.equal(typeof rotate,"function")});test("648 wave7 seams execute without placeholder values",()=>{let x=enroll({participantId:"p",completed:["read-policy","claim-dry-run","receipt-dry-run"],completedAt:"2026-09-16T18:00:00Z"});assert.equal(x.ready,true);assert.equal(rotate({generation:1,entryCount:500,ageHours:0,assessedAt:"2026-09-16T18:00:00Z"}).rotate,true)})
