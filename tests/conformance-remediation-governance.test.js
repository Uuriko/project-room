import test from"node:test";import assert from"node:assert/strict";import{buildRemediationContract}from"../src/remediation-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",findingRefs:["qualified:gap"],actionRefs:["assured:fix"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V134 hash-bound remediation",()=>{let c=buildRemediationContract(x);assert.match(c.remediationHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V134 preserves scope",()=>{let c=buildRemediationContract(x);assert.deepEqual([c.findingRefs,c.actionRefs,c.evidenceRefs],[x.findingRefs,x.actionRefs,x.evidenceRefs])});test("V134 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildRemediationContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V134 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildRemediationContract({...x,expiresAt:x.validFrom}),/window/)})
