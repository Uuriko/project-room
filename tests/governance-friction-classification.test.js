import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceFrictionClassification as build}from"../src/governance-friction-classification.js";test("623 correction classifies as quality severity two",()=>assert.deepEqual(Object.fromEntries(Object.entries(build({event:{eventHash:"a".repeat(64),frictionKind:"correction"}})).filter(([k])=>["category","severity"].includes(k))),{category:"quality",severity:2}));test("623 unknown kinds fail closed",()=>assert.throws(()=>build({event:{eventHash:"a".repeat(64),frictionKind:"other"}}),/invalid/))
