import test from"node:test";import assert from"node:assert/strict";import{buildReviewContract}from"../src/review-contract.js";const x={sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",subjectRefs:["subject"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("RV1 immutable review",()=>{let c=buildReviewContract(x);assert.match(c.reviewHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("RV1 complete scope",()=>{for(const k of["subjectRefs","criterionRefs","evidenceRefs"])assert.throws(()=>buildReviewContract({...x,[k]:[]}),/invalid/)});test("RV1 roles window",()=>{assert.throws(()=>buildReviewContract({...x,reviewerRef:"s"}),/independent/);assert.throws(()=>buildReviewContract({...x,expiresAt:x.validFrom}),/window/)});test("RV1 privacy",()=>{assert.throws(()=>buildReviewContract({...x,email:"x"}),/PII/);assert.throws(()=>buildReviewContract({...x,content:"x"}),/content/)})
