import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";import{buildDirectiveDecision}from"../src/directive-decision.js";import{buildDirectiveException}from"../src/directive-exception.js";import{buildDirectiveResolution}from"../src/directive-resolution.js";const chain=()=>{let c=buildDirectiveContract({sponsorRef:"s",ownerRef:"o",executorRef:"r",reviewerRef:"a",actionRefs:["subject"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),d=buildDirectiveDecision({contract:c,directiveHash:c.directiveHash,actionRefs:c.actionRefs,constraintRefs:c.constraintRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",executorRef:"r",reviewerRef:"a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"});return buildDirectiveException({decision:d,decisionHash:d.decisionHash,actionRefs:d.actionRefs,constraintRefs:d.constraintRefs,controlRefs:["commission"],evidenceRefs:["audit"],ownerRef:"o",executorRef:"r",reviewerRef:"a",observedAt:"2026-09-17T02:00:00Z",expiresAt:"2026-09-18T01:00:00Z"})},args=e=>({exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,actionRefs:e.actionRefs,constraintRefs:e.constraintRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"resolver",reviewerRef:"commissioner",outcome:"resolved",observedAt:"2026-09-17T03:00:00Z"});test("V189 complete hash-bound commission audit",()=>{let r=buildDirectiveResolution(args(chain()));assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("V189 rejects tamper and incomplete chain",()=>{let x=args(chain());assert.throws(()=>buildDirectiveResolution({...x,decisionHash:"f".repeat(64)}),/mismatch/);assert.throws(()=>buildDirectiveResolution({...x,controlRefs:[]}),/controls/)});test("V189 rejects roles and premature expiry",()=>{let x=args(chain());assert.equal(buildDirectiveResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildDirectiveResolution({...x,reviewerRef:"resolver"}),/independent/);assert.throws(()=>buildDirectiveResolution({...x,outcome:"expired"}),/not expired/)});test("V189 privacy",()=>{let x=args(chain());assert.throws(()=>buildDirectiveResolution({...x,email:"x"}),/PII/);assert.throws(()=>buildDirectiveResolution({...x,content:"x"}),/content/)})
