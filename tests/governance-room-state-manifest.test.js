import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceBoardTaskEntry as task}from"../src/governance-board-task-entry.js";import{buildGovernanceLaneStateEntry as lane}from"../src/governance-lane-state-entry.js";import{buildGovernanceRoomStateManifest as manifest}from"../src/governance-room-state-manifest.js";let at="2026-09-16T18:00:00Z",l=lane({laneId:"a",activeTaskIds:["T"],updatedAt:at}),t=task({taskId:"T",title:"Task",laneId:"a",updatedAt:at});test("585 manifests bind canonical task and lane state",()=>{let x=manifest({boardEntries:[t],laneEntries:[l],observedAt:at});assert.equal(x.tasks[0].entryHash,t.entryHash);assert.equal(x.lanes[0].entryHash,l.entryHash);assert.ok(Object.isFrozen(x))});test("585 duplicate identities and orphan tasks fail closed",()=>{assert.throws(()=>manifest({boardEntries:[t,t],laneEntries:[l],observedAt:at}),/invalid/);assert.throws(()=>manifest({boardEntries:[{...t,laneId:"b"}],laneEntries:[l],observedAt:at}),/orphan/)})
