import test from"node:test";import assert from"node:assert/strict";import{buildControlResolution}from"../src/control-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),controlRefs:["access"],safeguardRefs:["limit"],measureRefs:["monitor"],evidenceRefs:["audit","verified"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},make=(outcome="resolved",observedAt="2026-09-17T02:00:00Z")=>buildControlResolution({exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,controlRefs:e.controlRefs,safeguardRefs:e.safeguardRefs,measureRefs:e.measureRefs,evidenceRefs:e.evidenceRefs,resolverRef:"resolver",reviewerRef:"reviewer",outcome,observedAt});test("V105 is hash-bound and auditable",()=>{let r=make();assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.equal(r.exceptionHash,e.exceptionHash);assert.equal(r.decisionHash,e.decisionHash)});test("V105 preserves complete verified lineage",()=>{let r=make();for(const k of["controlRefs","safeguardRefs","measureRefs","evidenceRefs"])assert.deepEqual(r[k],[...e[k]].sort())});test("V105 separates resolver and reviewer",()=>{let r=make();assert.notEqual(r.resolverRef,r.reviewerRef);assert.ok(Object.isFrozen(r))});test("V105 enforces expired timing",()=>{assert.throws(()=>make("expired"),/not expired/);assert.equal(make("expired",e.expiresAt).outcome,"expired")})
