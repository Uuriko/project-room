import test from"node:test";import assert from"node:assert/strict";import{buildRuleContract}from"../src/rule-contract.js";const x={sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",requirementRefs:["request"],limitRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("RC1 immutable commission",()=>{let c=buildRuleContract(x);assert.match(c.ruleHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("RC1 complete scope",()=>{for(const k of["requirementRefs","limitRefs","evidenceRefs"])assert.throws(()=>buildRuleContract({...x,[k]:[]}),/invalid/)});test("RC1 roles window",()=>{assert.throws(()=>buildRuleContract({...x,ownerRef:"s"}),/independent/);assert.throws(()=>buildRuleContract({...x,expiresAt:x.validFrom}),/window/)});test("RC1 privacy",()=>{assert.throws(()=>buildRuleContract({...x,email:"x"}),/PII/);assert.throws(()=>buildRuleContract({...x,content:"x"}),/content/)})
