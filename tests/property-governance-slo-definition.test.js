import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceSloDefinition as b}from"../src/governance-slo-definition.js";let x={sloId:"s",metric:"latency",percentile:95,target:100,maxValue:200,windowMinutes:60};test("712 limits separate identity",()=>assert.notEqual(b(x).sloHash,b({...x,target:101}).sloHash));test("712 ceiling below target rejects",()=>assert.throws(()=>b({...x,maxValue:99}),/invalid/))
