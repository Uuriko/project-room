import test from"node:test";import assert from"node:assert/strict";import{buildReviewContract}from"../src/review-contract.js";import{buildReviewDecision}from"../src/review-decision.js";import{buildReviewException}from"../src/review-exception.js";const chain=()=>{let c=buildReviewContract({sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",subjectRefs:["subject"],criterionRefs:["criterion"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildReviewDecision({contract:c,reviewHash:c.reviewHash,subjectRefs:c.subjectRefs,criterionRefs:c.criterionRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",approverRef:"a",reviewerRef:"r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})},args=d=>({decision:d,decisionHash:d.decisionHash,subjectRefs:d.subjectRefs,criterionRefs:d.criterionRefs,controlRefs:["review"],evidenceRefs:["audit"],ownerRef:"o",approverRef:"a",reviewerRef:"r",observedAt:"2026-09-17T02:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});test("RV3 real-chain immutable exception",()=>{let e=buildReviewException(args(chain()));assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("RV3 conditional contained scope",()=>{let d=chain(),x=args(d);assert.throws(()=>buildReviewException(args({...d,outcome:"active"})),/conditional/);assert.throws(()=>buildReviewException({...x,criterionRefs:["other"]}),/scope/)});test("RV3 controls roles expiry",()=>{let x=args(chain());assert.throws(()=>buildReviewException({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildReviewException({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildReviewException({...x,expiresAt:x.observedAt}),/expiry/)});test("RV3 privacy",()=>{let x=args(chain());assert.throws(()=>buildReviewException({...x,email:"x"}),/PII/);assert.throws(()=>buildReviewException({...x,content:"x"}),/content/)})
