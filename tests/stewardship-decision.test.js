import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipDecision}from"../src/stewardship-decision.js";const c={stewardshipHash:"a".repeat(64),stewardRef:"o",assetRefs:["read"],dutyRefs:["no-share"],evidenceRefs:["e"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={contract:c,stewardshipHash:c.stewardshipHash,assetRefs:c.assetRefs,dutyRefs:c.dutyRefs,evidenceRefs:c.evidenceRefs,stewardRef:"o",approverRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"};test("SD2 immutable stewardship decision",()=>{let d=buildStewardshipDecision(x);assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("SD2 binding and complete evaluation",()=>{assert.throws(()=>buildStewardshipDecision({...x,stewardshipHash:"b".repeat(64)}),/mismatch/);assert.throws(()=>buildStewardshipDecision({...x,assetRefs:[]}),/assets/);assert.throws(()=>buildStewardshipDecision({...x,evidenceRefs:["z"]}),/evidence/)});test("SD2 outcomes roles and window",()=>{assert.equal(buildStewardshipDecision({...x,outcome:"conditional",conditionCodes:["limit"]}).outcome,"conditional");assert.throws(()=>buildStewardshipDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildStewardshipDecision({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildStewardshipDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("SD2 privacy",()=>{assert.throws(()=>buildStewardshipDecision({...x,name:"n"}),/PII/);assert.throws(()=>buildStewardshipDecision({...x,body:"b"}),/content/)})
