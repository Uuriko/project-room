import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceVerifierProfile as b}from"../src/governance-verifier-profile.js";let x={verifierId:"v",affiliation:"a"};test("682 order canonicalizes",()=>assert.deepEqual(b({...x,capabilities:["a","b"]}),b({...x,capabilities:["b","a"]})));test("682 affiliation separates",()=>assert.notEqual(b({...x,capabilities:[]}).profileHash,b({...x,affiliation:"b",capabilities:[]}).profileHash))
