import test from"node:test";import assert from"node:assert/strict";import{buildVarianceContract}from"../src/variance-contract.js";const x={sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",baselineRefs:["baseline"],deviationRefs:["deviation"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("VA1 immutable variance",()=>{let c=buildVarianceContract(x);assert.match(c.varianceHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("VA1 complete scope",()=>{for(const k of["baselineRefs","deviationRefs","evidenceRefs"])assert.throws(()=>buildVarianceContract({...x,[k]:[]}),/invalid/)});test("VA1 roles window",()=>{assert.throws(()=>buildVarianceContract({...x,reviewerRef:"s"}),/independent/);assert.throws(()=>buildVarianceContract({...x,expiresAt:x.validFrom}),/window/)});test("VA1 privacy",()=>{assert.throws(()=>buildVarianceContract({...x,email:"x"}),/PII/);assert.throws(()=>buildVarianceContract({...x,content:"x"}),/content/)})
