import test from"node:test";import assert from"node:assert/strict";import{buildAuthorityResolution}from"../src/authority-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),authorityRefs:["read"],limitRefs:["no-share"],controlRefs:["review"],evidenceRefs:["e"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,authorityRefs:e.authorityRefs,limitRefs:e.limitRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"a",reviewerRef:"b",outcome:"resolved",observedAt:"2026-09-17T02:00:00Z"};test("AC4 immutable resolution",()=>{let r=buildAuthorityResolution(x);assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("AC4 binding and complete lineage",()=>{assert.throws(()=>buildAuthorityResolution({...x,decisionHash:"c".repeat(64)}),/mismatch/);assert.throws(()=>buildAuthorityResolution({...x,limitRefs:[]}),/limits/);assert.throws(()=>buildAuthorityResolution({...x,evidenceRefs:["z"]}),/evidence/)});test("AC4 outcome roles and time",()=>{assert.equal(buildAuthorityResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildAuthorityResolution({...x,outcome:"open"}),/outcome/);assert.throws(()=>buildAuthorityResolution({...x,reviewerRef:"a"}),/independent/);assert.throws(()=>buildAuthorityResolution({...x,outcome:"expired"}),/not expired/)});test("AC4 privacy",()=>{assert.throws(()=>buildAuthorityResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildAuthorityResolution({...x,text:"t"}),/content/)})
