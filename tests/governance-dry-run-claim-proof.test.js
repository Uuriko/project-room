import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceDryRunClaimProof as build}from"../src/governance-dry-run-claim-proof.js";let b={participantId:"p",checklistHash:"a".repeat(64),claimHash:"b".repeat(64),conflictChecked:true,performedAt:"2026-09-16T18:00:00Z"};test("633 claim proof is explicitly dry run",()=>assert.equal(build(b).dryRun,true));test("633 unchecked conflict and bad hashes fail closed",()=>{assert.throws(()=>build({...b,conflictChecked:false}),/invalid/);assert.throws(()=>build({...b,claimHash:"x"}),/invalid/)})
