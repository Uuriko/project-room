import test from"node:test";import assert from"node:assert/strict";import{buildPolicyDecision}from"../src/policy-decision.js";const p={policyHash:"a".repeat(64),operatorRef:"o",policyRefs:["access"],constraintRefs:["limit"],evidenceRefs:["e"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={policy:p,policyHash:p.policyHash,policyRefs:p.policyRefs,constraintRefs:p.constraintRefs,evidenceRefs:p.evidenceRefs,operatorRef:"o",approverRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"};test("W2 immutable decision",()=>{let d=buildPolicyDecision(x);assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("W2 complete evaluation",()=>{assert.throws(()=>buildPolicyDecision({...x,policyRefs:[]}),/policies/);assert.throws(()=>buildPolicyDecision({...x,evidenceRefs:["z"]}),/evidence/)});test("W2 outcomes roles and window",()=>{assert.equal(buildPolicyDecision({...x,outcome:"conditional",conditionCodes:["limit"]}).outcome,"conditional");assert.throws(()=>buildPolicyDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildPolicyDecision({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildPolicyDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("W2 privacy",()=>{assert.throws(()=>buildPolicyDecision({...x,name:"n"}),/PII/);assert.throws(()=>buildPolicyDecision({...x,body:"b"}),/content/)})
