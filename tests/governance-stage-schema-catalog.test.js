import test from"node:test";import assert from"node:assert/strict";import{governanceStages}from"../src/governance-stage-schema-catalog.js";test("253 catalog is nonempty unique and frozen",()=>{assert.ok(governanceStages.length);assert.equal(new Set(governanceStages).size,governanceStages.length);assert.ok(Object.isFrozen(governanceStages))});test("253 catalog excludes private content",()=>assert.equal(JSON.stringify(governanceStages).includes("email"),false));