import test from"node:test";import assert from"node:assert/strict";import{buildReviewContract}from"../src/review-contract.js";const x={sponsorRef:"governance:s",ownerRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",subjectRefs:["qualified:subject"],criterionRefs:["assured:criterion"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V146 hash-bound review",()=>{let c=buildReviewContract(x);assert.match(c.reviewHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V146 preserves scope",()=>{let c=buildReviewContract(x);assert.deepEqual([c.subjectRefs,c.criterionRefs,c.evidenceRefs],[x.subjectRefs,x.criterionRefs,x.evidenceRefs])});test("V146 roles",()=>{assert.equal(new Set([x.sponsorRef,x.ownerRef,x.approverRef,x.reviewerRef]).size,4);assert.throws(()=>buildReviewContract({...x,reviewerRef:x.sponsorRef}),/independent/)});test("V146 window",()=>{assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom));assert.throws(()=>buildReviewContract({...x,expiresAt:x.validFrom}),/window/)})
