import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceSloAssessment as b}from"../src/governance-slo-assessment.js";let s={sloHash:"a".repeat(64),percentile:50,target:2,maxValue:4},m={sampleHash:"b".repeat(64),sloHash:s.sloHash,values:[1,2,4]};test("715 percentile assesses",()=>assert.equal(b({slo:s,sample:m}).targetMet,true));test("715 ceiling assesses",()=>assert.equal(b({slo:{...s,maxValue:3},sample:m}).ceilingMet,false))
