import test from"node:test";import assert from"node:assert/strict";import{buildRuleContract}from"../src/rule-contract.js";import{buildRuleDecision}from"../src/rule-decision.js";const make=()=>{let c=buildRuleContract({sponsorRef:"s",ownerRef:"oversight:o",approverRef:"entitlement:r",reviewerRef:"authorization:a",requirementRefs:["subject"],limitRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildRuleDecision({contract:c,ruleHash:c.ruleHash,requirementRefs:c.requirementRefs,limitRefs:c.limitRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",approverRef:"entitlement:r",reviewerRef:"authorization:a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V183 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.requirementRefs,d.limitRefs,d.evidenceRefs],[['subject'],['criterion'],['commission']])});test("V183 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V183 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.approverRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V183 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
