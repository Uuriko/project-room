import test from"node:test";import assert from"node:assert/strict";import{buildRuleContract}from"../src/rule-contract.js";import{buildRuleDecision}from"../src/rule-decision.js";const c=()=>buildRuleContract({sponsorRef:"s",ownerRef:"o",approverRef:"a",reviewerRef:"r",requirementRefs:["request"],limitRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"}),args=c=>({contract:c,ruleHash:c.ruleHash,requirementRefs:c.requirementRefs,limitRefs:c.limitRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",approverRef:"a",reviewerRef:"r",outcome:"active",conditionCodes:[],observedAt:"2026-09-17T02:00:00Z"});test("RC2 real-contract immutable decision",()=>{let d=buildRuleDecision(args(c()));assert.match(d.decisionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(d))});test("RC2 exact evaluation",()=>{let x=args(c());assert.throws(()=>buildRuleDecision({...x,requirementRefs:[]}),/objectives/);assert.throws(()=>buildRuleDecision({...x,limitRefs:["other"]}),/constraints/)});test("RC2 outcomes roles and window",()=>{let x=args(c());assert.equal(buildRuleDecision({...x,outcome:"conditional",conditionCodes:["monitor"]}).outcome,"conditional");assert.throws(()=>buildRuleDecision({...x,outcome:"denied"}),/conditions/);assert.throws(()=>buildRuleDecision({...x,approverRef:"o"}),/independent/);assert.throws(()=>buildRuleDecision({...x,observedAt:"2026-09-19T00:00:00Z"}),/window/)});test("RC2 privacy",()=>{let x=args(c());assert.throws(()=>buildRuleDecision({...x,email:"x"}),/PII/);assert.throws(()=>buildRuleDecision({...x,content:"x"}),/content/)})
