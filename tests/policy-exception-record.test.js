import test from"node:test";import assert from"node:assert/strict";import{buildPolicyExceptionRecord}from"../src/policy-exception-record.js";const d={decisionHash:"a".repeat(64),outcome:"conditional",policyRefs:["access"],constraintRefs:["limit"]},x={decision:d,decisionHash:d.decisionHash,policyRefs:d.policyRefs,constraintRefs:d.constraintRefs,controlRefs:["monitor"],evidenceRefs:["e"],ownerRef:"o",approverRef:"a",reviewerRef:"r",observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("W3 immutable bounded exception",()=>{let e=buildPolicyExceptionRecord(x);assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("W3 conditional and contained scope",()=>{assert.throws(()=>buildPolicyExceptionRecord({...x,decision:{...d,outcome:"active"}}),/conditional/);assert.throws(()=>buildPolicyExceptionRecord({...x,policyRefs:["other"]}),/scope/)});test("W3 controls roles and expiry",()=>{assert.throws(()=>buildPolicyExceptionRecord({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildPolicyExceptionRecord({...x,evidenceRefs:[]}),/evidence/);assert.throws(()=>buildPolicyExceptionRecord({...x,reviewerRef:"o"}),/independent/);assert.throws(()=>buildPolicyExceptionRecord({...x,expiresAt:x.observedAt}),/expiry/)});test("W3 privacy",()=>{assert.throws(()=>buildPolicyExceptionRecord({...x,address:"x"}),/PII/);assert.throws(()=>buildPolicyExceptionRecord({...x,payload:"x"}),/content/)})
