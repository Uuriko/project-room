import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceHealthMetricRecord as health}from"../src/governance-health-metric-record.js";import{buildGovernanceTrustMetricRecord as trust}from"../src/governance-trust-metric-record.js";import{buildGovernanceDigestSnapshot as build}from"../src/governance-digest-snapshot.js";let sampledAt="2026-09-16T18:00:00Z",hs=["b","a"].map(metric=>health({metric,value:1,unit:"count",level:"healthy",sourceHash:"a".repeat(64),sampledAt})),ts=["y","x"].map(metric=>trust({metric,score:1,sourceHash:"b".repeat(64),sampledAt})),w={windowStartsAt:"2026-09-16T17:00:00Z",windowEndsAt:"2026-09-16T19:00:00Z"};test("596 metric permutations yield one digest identity",()=>assert.deepEqual(build({...w,healthMetrics:hs,trustMetrics:ts}),build({...w,healthMetrics:hs.toReversed(),trustMetrics:ts.toReversed()})));test("596 window changes separate digest identity",()=>{let a=build({...w,healthMetrics:hs,trustMetrics:ts}),b=build({...w,windowEndsAt:"2026-09-16T20:00:00Z",healthMetrics:hs,trustMetrics:ts});assert.notEqual(a.snapshotHash,b.snapshotHash)})
