import test from"node:test";import assert from"node:assert/strict";import{buildPolicyResolution}from"../src/policy-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),policyRefs:["access"],constraintRefs:["limit"],controlRefs:["monitor"],evidenceRefs:["e"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,policyRefs:e.policyRefs,constraintRefs:e.constraintRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"a",reviewerRef:"b",outcome:"resolved",observedAt:"2026-09-17T02:00:00Z"};test("W4 immutable resolution",()=>{let r=buildPolicyResolution(x);assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("W4 binding and complete lineage",()=>{assert.throws(()=>buildPolicyResolution({...x,decisionHash:"c".repeat(64)}),/mismatch/);assert.throws(()=>buildPolicyResolution({...x,constraintRefs:[]}),/constraints/);assert.throws(()=>buildPolicyResolution({...x,evidenceRefs:["z"]}),/evidence/)});test("W4 outcome roles and time",()=>{assert.equal(buildPolicyResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildPolicyResolution({...x,outcome:"open"}),/outcome/);assert.throws(()=>buildPolicyResolution({...x,reviewerRef:"a"}),/independent/);assert.throws(()=>buildPolicyResolution({...x,outcome:"expired"}),/not expired/)});test("W4 privacy",()=>{assert.throws(()=>buildPolicyResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildPolicyResolution({...x,text:"t"}),/content/)})
