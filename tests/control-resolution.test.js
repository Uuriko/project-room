import test from"node:test";import assert from"node:assert/strict";import{buildControlResolution}from"../src/control-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),controlRefs:["access"],safeguardRefs:["limit"],measureRefs:["monitor"],evidenceRefs:["e"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},x={exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,controlRefs:e.controlRefs,safeguardRefs:e.safeguardRefs,measureRefs:e.measureRefs,evidenceRefs:e.evidenceRefs,resolverRef:"a",reviewerRef:"b",outcome:"resolved",observedAt:"2026-09-17T02:00:00Z"};test("Y4 immutable resolution",()=>{let r=buildControlResolution(x);assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(r))});test("Y4 binding and complete lineage",()=>{assert.throws(()=>buildControlResolution({...x,decisionHash:"c".repeat(64)}),/mismatch/);assert.throws(()=>buildControlResolution({...x,safeguardRefs:[]}),/safeguards/);assert.throws(()=>buildControlResolution({...x,evidenceRefs:["z"]}),/evidence/)});test("Y4 outcome roles and time",()=>{assert.equal(buildControlResolution({...x,outcome:"revoked"}).outcome,"revoked");assert.throws(()=>buildControlResolution({...x,outcome:"open"}),/outcome/);assert.throws(()=>buildControlResolution({...x,reviewerRef:"a"}),/independent/);assert.throws(()=>buildControlResolution({...x,outcome:"expired"}),/not expired/)});test("Y4 privacy",()=>{assert.throws(()=>buildControlResolution({...x,phone:"p"}),/PII/);assert.throws(()=>buildControlResolution({...x,text:"t"}),/content/)})
