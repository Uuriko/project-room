import test from"node:test";import assert from"node:assert/strict";import{buildStewardshipContract}from"../src/stewardship-contract.js";const x={sponsorRef:"governance:g",stewardRef:"assurance:o",approverRef:"qualification:a",reviewerRef:"governance:r",assetRefs:["qualified:read"],dutyRefs:["assured:no-share"],evidenceRefs:["governed:approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"};test("V122 governed stewardship is hash-bound",()=>{let c=buildStewardshipContract(x);assert.match(c.stewardshipHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(c))});test("V122 preserves asset duty evidence bounds",()=>{let c=buildStewardshipContract(x);assert.deepEqual(c.assetRefs,x.assetRefs);assert.deepEqual(c.dutyRefs,x.dutyRefs);assert.deepEqual(c.evidenceRefs,x.evidenceRefs)});test("V122 preserves governed role separation",()=>{assert.throws(()=>buildStewardshipContract({...x,reviewerRef:x.sponsorRef}),/independent/);assert.equal(new Set([x.sponsorRef,x.stewardRef,x.approverRef,x.reviewerRef]).size,4)});test("V122 preserves qualification window",()=>{assert.throws(()=>buildStewardshipContract({...x,expiresAt:x.validFrom}),/window/);assert.ok(Date.parse(x.expiresAt)>Date.parse(x.validFrom))})
