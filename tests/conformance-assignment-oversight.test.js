import test from"node:test";import assert from"node:assert/strict";import{buildAssignmentContract}from"../src/assignment-contract.js";import{buildAssignmentDecision}from"../src/assignment-decision.js";const make=()=>{let c=buildAssignmentContract({assignerRef:"s",assigneeRef:"oversight:o",reviewerRef:"authorization:a",assignmenterRef:"entitlement:r",dutyRefs:["subject"],limitRefs:["criterion"],evidenceRefs:["assignment"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildAssignmentDecision({contract:c,assignmentHash:c.assignmentHash,dutyRefs:c.dutyRefs,limitRefs:c.limitRefs,evidenceRefs:c.evidenceRefs,assigneeRef:"oversight:o",reviewerRef:"authorization:a",assignmenterRef:"entitlement:r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V155 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.dutyRefs,d.limitRefs,d.evidenceRefs],[['subject'],['criterion'],['assignment']])});test("V155 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V155 timing and roles",()=>{let d=make();assert.equal(new Set([d.assigneeRef,d.reviewerRef,d.assignmenterRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V155 preserves bounded decision",()=>{let d=make();assert.ok(Object.isFrozen(d));assert.match(d.decisionHash,/^[a-f0-9]{64}$/)})
