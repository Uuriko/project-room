import test from"node:test";import assert from"node:assert/strict";import{buildEscalationContract}from"../src/escalation-contract.js";import{buildEscalationDecision}from"../src/escalation-decision.js";const make=()=>{let c=buildEscalationContract({sponsorRef:"s",ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",triggerRefs:["threshold"],routeRefs:["governance"],evidenceRefs:["approval"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildEscalationDecision({contract:c,escalationHash:c.escalationHash,triggerRefs:c.triggerRefs,routeRefs:c.routeRefs,evidenceRefs:c.evidenceRefs,ownerRef:"oversight:o",approverRef:"authorization:a",reviewerRef:"entitlement:r",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})};test("V131 real-chain decision completeness",()=>{let d=make();assert.deepEqual([d.triggerRefs,d.routeRefs,d.evidenceRefs],[['threshold'],['governance'],['approval']])});test("V131 bounded conditions",()=>{assert.deepEqual(make().conditionCodes,["monitor"])});test("V131 timing and roles",()=>{let d=make();assert.equal(new Set([d.ownerRef,d.approverRef,d.reviewerRef]).size,3);assert.equal(d.observedAt,"2026-09-17T02:00:00Z")});test("V131 excludes attention semantics",()=>{let d=make();assert.equal("attention" in d,false);assert.equal("workLoop" in d,false)})
