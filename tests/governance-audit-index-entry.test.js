import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceAuditIndexEntry as entry}from"../src/governance-audit-index-entry.js";import{buildGovernanceAuditIndex as index}from"../src/governance-audit-index.js";import{buildGovernanceAuditQueryPlan as plan}from"../src/governance-audit-query-plan.js";import{buildGovernanceAuditQueryResult as result}from"../src/governance-audit-query-result.js";const run=()=>{let a=entry({packetHash:"a".repeat(64),categoryCounts:{stale:1},maximumSeverity:"high",timeBucket:"2026-09"}),b=entry({packetHash:"b".repeat(64),categoryCounts:{privacy:1},maximumSeverity:"critical",timeBucket:"2026-10"}),i=index({entries:[b,a]}),p=plan({category:"privacy",minimumSeverity:"high",fromBucket:"2026-09",toBucket:"2026-10"});return{i,p,r:result({index:i,plan:p})}};test("421 index query is canonical content free",()=>{let a=run(),b=run();assert.deepEqual(a,b);assert.deepEqual(a.r.packetHashes,["b".repeat(64)]);assert.equal(a.r.count,1);assert.ok(Object.isFrozen(a.i.entries))});test("421 adversarial predicates fail closed",()=>{assert.throws(()=>plan({category:"privacy OR 1=1",fromBucket:"2026-09",toBucket:"2026-10"}),/query/);assert.throws(()=>plan({fromBucket:"2026-11",toBucket:"2026-10"}),/query/)});