import test from"node:test";import assert from"node:assert/strict";import{buildDirectiveContract}from"../src/directive-contract.js";import{buildDirectiveDecision}from"../src/directive-decision.js";import{buildDirectiveException}from"../src/directive-exception.js";const chain=()=>{let c=buildDirectiveContract({sponsorRef:"s",ownerRef:"o",executorRef:"r",reviewerRef:"a",actionRefs:["request"],constraintRefs:["criterion"],evidenceRefs:["commission"],validFrom:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});return buildDirectiveDecision({contract:c,directiveHash:c.directiveHash,actionRefs:c.actionRefs,constraintRefs:c.constraintRefs,evidenceRefs:c.evidenceRefs,ownerRef:"o",executorRef:"r",reviewerRef:"a",outcome:"conditional",conditionCodes:["monitor"],observedAt:"2026-09-17T02:00:00Z"})},args=d=>({decision:d,decisionHash:d.decisionHash,actionRefs:d.actionRefs,constraintRefs:d.constraintRefs,controlRefs:["commission"],evidenceRefs:["audit"],ownerRef:"o",executorRef:"r",reviewerRef:"a",observedAt:"2026-09-17T02:00:00Z",expiresAt:"2026-09-18T01:00:00Z"});test("DT3 real-chain immutable exception",()=>{let e=buildDirectiveException(args(chain()));assert.match(e.exceptionHash,/^[a-f0-9]{64}$/);assert.ok(Object.isFrozen(e))});test("DT3 conditional contained scope",()=>{let d=chain(),x=args(d);assert.throws(()=>buildDirectiveException(args({...d,outcome:"active"})),/conditional/);assert.throws(()=>buildDirectiveException({...x,constraintRefs:["other"]}),/scope/)});test("DT3 controls roles expiry",()=>{let x=args(chain());assert.throws(()=>buildDirectiveException({...x,controlRefs:[]}),/controls/);assert.throws(()=>buildDirectiveException({...x,executorRef:"o"}),/independent/);assert.throws(()=>buildDirectiveException({...x,expiresAt:x.observedAt}),/expiry/)});test("DT3 privacy",()=>{let x=args(chain());assert.throws(()=>buildDirectiveException({...x,email:"x"}),/PII/);assert.throws(()=>buildDirectiveException({...x,content:"x"}),/content/)})
