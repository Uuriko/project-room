import test from"node:test";import assert from"node:assert/strict";import{buildGovernanceRestorationReceipt as b}from"../src/governance-restoration-receipt.js";let p={planHash:"a".repeat(64),artifactHash:"b".repeat(64),plannedAt:"2026-09-16T19:00:00Z"};test("737 exact artifact restores",()=>assert.equal(b({plan:p,restoredHash:p.artifactHash,restoredAt:p.plannedAt}).restored,true));test("737 altered artifact rejects",()=>assert.throws(()=>b({plan:p,restoredHash:"c".repeat(64),restoredAt:p.plannedAt}),/invalid/))
