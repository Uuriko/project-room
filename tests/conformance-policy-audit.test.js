import test from"node:test";import assert from"node:assert/strict";import{buildPolicyResolution}from"../src/policy-resolution.js";const e={exceptionHash:"a".repeat(64),decisionHash:"b".repeat(64),policyRefs:["access"],constraintRefs:["limit"],controlRefs:["monitor"],evidenceRefs:["audit","verified"],observedAt:"2026-09-17T01:00:00Z",expiresAt:"2026-09-18T01:00:00Z"},make=(outcome="resolved",observedAt="2026-09-17T02:00:00Z")=>buildPolicyResolution({exception:e,exceptionHash:e.exceptionHash,decisionHash:e.decisionHash,policyRefs:e.policyRefs,constraintRefs:e.constraintRefs,controlRefs:e.controlRefs,evidenceRefs:e.evidenceRefs,resolverRef:"resolver",reviewerRef:"reviewer",outcome,observedAt});test("V101 is hash-bound and auditable",()=>{let r=make();assert.match(r.resolutionHash,/^[a-f0-9]{64}$/);assert.equal(r.exceptionHash,e.exceptionHash);assert.equal(r.decisionHash,e.decisionHash)});test("V101 preserves complete verified lineage",()=>{let r=make();for(const k of["policyRefs","constraintRefs","controlRefs","evidenceRefs"])assert.deepEqual(r[k],[...e[k]].sort())});test("V101 separates resolver and reviewer",()=>{let r=make();assert.notEqual(r.resolverRef,r.reviewerRef);assert.ok(Object.isFrozen(r))});test("V101 enforces expired timing",()=>{assert.throws(()=>make("expired"),/not expired/);assert.equal(make("expired",e.expiresAt).outcome,"expired")})
