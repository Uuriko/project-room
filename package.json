{
  "name": "project-room",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Executable v0 contract for a shared human and agent project room",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check src/events.js && node --check src/seed.js && node --check src/app.js && node --check server.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
