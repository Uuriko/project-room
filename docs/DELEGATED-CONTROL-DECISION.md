# Delegated-control decision

`buildDelegatedControlDecision` binds exact control, limit and evidence evaluation to a delegated-control contract. It enforces independent delegate, controller and reviewer roles, outcome conditions and in-window observation. All identifiers are camelCase; content and identity PII are rejected.
