# Remediation resolution

`buildRemediationResolution` creates an immutable resolution bound to remediation exception and decision hashes. It requires complete finding, action, control and evidence lineage, separate resolver and reviewer, valid time, and resolved, expired or revoked outcome. It imports no older remediation modules and has no external effects.
