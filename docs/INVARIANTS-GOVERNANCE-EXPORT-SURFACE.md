# Task 241: invariants governance export surface

Compatibility reconstruction hardens export/schema seams, deterministic JSON replay, stale and cycle rejection, domain separation, immutable manifests, and release gating.
