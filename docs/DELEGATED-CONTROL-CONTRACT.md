# Delegated-control contract

`buildDelegatedControlContract` creates immutable hash-bound metadata over controls, limits and evidence. It requires independent delegator, delegate, controller and reviewer roles plus a valid window. Content and identity PII are rejected.
