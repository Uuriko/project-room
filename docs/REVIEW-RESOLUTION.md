# Review resolution

`buildReviewResolution` creates an immutable resolution bound to review exception and decision hashes. It requires complete subject, criterion, control and evidence lineage, separate resolver and reviewer, valid time, and resolved, expired or revoked outcome. It imports no prefixed review modules and has no external effects.
