# Change-control portfolio lineage invariants

Task 206 drives real Waiver, Variance, Review, Approval and Assignment builders. Revoked waiver and expired variance terminal hashes remain distinct through every descendant, so later stages cannot reproduce the approved path. Skipped, reordered and stale approval handoffs likewise produce distinct immutable, role-separated terminals. Native outcome vocabulary is preserved.
