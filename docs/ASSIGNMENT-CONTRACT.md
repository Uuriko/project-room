# Assignment contract

`buildAssignmentContract` creates immutable hash-bound assignment metadata over duties, limits and evidence. It requires independent assigner, assignee, reviewer and acceptor roles plus a valid window. Content and identity PII are rejected; no external effects occur.
