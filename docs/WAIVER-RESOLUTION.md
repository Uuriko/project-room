# Waiver resolution

`buildWaiverResolution` creates an immutable resolution bound to waiver exception and decision hashes. It requires complete requirement, control, safeguard and evidence lineage, separate resolver and reviewer, valid time, and resolved, expired or revoked outcome. Content and identity PII are rejected; no external effects occur.
