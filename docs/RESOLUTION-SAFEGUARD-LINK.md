# Resolution safeguard link

`buildResolutionSafeguardLink` binds one real upstream terminal hash as a deterministic downstream safeguard reference. The immutable domain-separated record names source and target lane/stage, safeguard class, activation and strict expiry. It rejects malformed lineage, self-links, invalid windows, unknown fields, PII and content without embedding source payloads.
