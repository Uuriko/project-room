# Waiver exception

`buildWaiverException` creates an immutable exception bound to a conditional waiver decision. Requirement and control scopes stay contained; compensating safeguards and evidence are required. Roles are separate and expiry is bounded. Content and identity PII are rejected; no external effects occur.
