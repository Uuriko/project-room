# Safeguard exception

`buildSafeguardException` binds a conditional safeguard decision to contained protections, compensating controls, established safeguard references and evidence. It preserves protector, operator and reviewer separation and bounded expiry. It does not define `safeguarderRef`.
