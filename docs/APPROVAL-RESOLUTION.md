# Approval resolution

`buildApprovalResolution` creates an immutable resolution bound to approval exception and decision hashes. It requires complete subject, criterion, control and evidence lineage, separate resolver and approvaler, valid time, and resolved, expired or revoked outcome. It imports no prefixed approval modules and has no external effects.
