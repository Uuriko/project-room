# Delegated-control exception

`buildDelegatedControlException` binds a conditional delegated-control decision to contained controls plus compensating controls, safeguards and evidence. It preserves delegate, controller and reviewer separation and bounded expiry. All identifiers are camelCase.
