# Assignment decision

`buildAssignmentDecision` binds exact duty, limit and evidence evaluation to an assignment contract. It enforces independent assignee, acceptor and reviewer roles, outcome conditions and in-window observation. Content and identity PII are rejected.
