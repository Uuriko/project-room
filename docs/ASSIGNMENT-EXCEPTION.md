# Assignment exception

`buildAssignmentException` binds a conditional assignment decision to contained duties, compensating controls and evidence. It preserves assignment role separation and bounded expiry. Content and identity PII are rejected.
