# Commission decision

`buildCommissionDecision` binds exact objective, constraint and evidence evaluation to a commission contract. It enforces independent commissioner, executor and reviewer roles, outcome conditions and in-window observation. Content and identity PII are rejected.
