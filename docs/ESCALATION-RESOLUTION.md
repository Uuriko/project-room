# Escalation resolution

`buildEscalationResolution` creates an immutable resolution bound to escalation exception and decision hashes. It requires complete trigger, route, control and evidence lineage, separate resolver and reviewer, valid time, and resolved, expired or revoked outcome. Room attention, UI and work-loop behavior remain excluded; no external effects occur.
