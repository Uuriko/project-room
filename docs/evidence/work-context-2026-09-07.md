# Selected-task context: local evidence

Recorded 2026-09-07 (America/Los_Angeles). Local candidate based on b6ebfe4.
No live service, hosted CI, human study or provider execution was used.

## Automated checks

- 361 core/API checks passed, including six selected-context checks and executable
  assignment/read/write examples. Repeated after adding read-only reopen coverage.
- All 76 browser checks passed on the first full run for this slice (128.5 seconds).
- All seven local Cloudflare checks passed (17.3 seconds). The HTTP check now
  includes bearer and account-bound selected reads, exact linked source, unchanged
  Room state and stale-binding rejection. Known synthetic self-signed TLS probe
  diagnostics appeared; certificate verification was not disabled.
- Fifteen exact public assets packaged; no client/browser asset added. Production
  entrypoint bundled locally, 201,966 bytes. No dependency or schema changes here.
- Reviewed current desktop first contribution and narrow 200%-text catch-up
  screenshots. No new UI was introduced; these are regression observations.

Focused coverage includes a single store read transaction; detached allowlisted
records; no unrelated work/messages, private reminders, histories or cursor;
default-excluded/exact opt-in source; expiry on one evaluation clock; current
roles and permission-aware hints; correction/new receipt/exact review/human gate;
retained PASS during reopened work; stale work versus unrelated activity; revoked
credentials; Room/account binding; strict query/CLI arguments; cancellable one-GET
client; malformed selected identity/source; and a fresh read-only store opening.

Read-only review found a pre-existing encoded-colon Room-ID mismatch. The shared
handler now decodes bounded path components once and applies the existing ID
validator. A real client round-trip covers a colon Room and work ID. Unknown or
invalid IDs do not expand the domain. Full suites passed with this routing change.

Screenshots retained in ignored local test output (later runs may replace them):

| Screenshot | Bytes | SHA256 |
| --- | ---: | --- |
| `test-results/first-use-desktop-guest.png` | 86,631 | `556b24d748c10d898d34b9270316c654714347f48430c85f83954ac2ef7bc8b4` |
| `test-results/calm-return-mobile-large-text-controls.png` | 42,348 | `c259e6a9882b5ba9a65faef58d02b94b9ce80559fb98d55e23b0d2dfbc769050` |

## Fresh actual-agent exercise

Two new agents had no prior conversation history. Both received only their own
private fixture configuration path, exact task ID, public interface guides and
explicit local-exercise authority. They used selected context, not orientation,
full snapshot, implementation, tests, seed code or other role configurations.
The reviewer established its rubric before the new completion. No pass was prescribed.
The participants are separate fresh agent contexts within the same test framework,
not evidence of organizational independence or cross-vendor interoperability.

The task was an original proposed cancellation checklist, not implemented Compute
behavior. Seeded prehistory deliberately contained a bad draft, failed review and
released fictional scope, plus unrelated work and a similarly named source.
Seed member names were reused by actual participants, so identity alone cannot
distinguish test setup from actual agent work. The exact boundary is:

- Seed high-water sequence **14**, selected work revision **6**, state blocked.
- Seed receipt `eba85367-47f9-4ae2-9530-3cf0e66b1efe`, version
  `sha256:4e5d70123f7823aa8f6be63e3efb94266cb56591971a1719c6cc3b00a0a6085e`.
- Seeded completion/review are not counted as actual agent contribution or review.

The fresh producer identified the needed correction, released claim, reviewer
and human decision maker from the selected view. It reacquired the exact fictional
scope, resolved the understood blocker, started, authored an original 570-word
document and submitted the actual immutable artifact. Producer, independent
reviewer and root each retrieved/checked its bytes over CA-verified loopback HTTPS.
The [preserved original artifact](work-context-actual-agent-proposal-2026-09-07.md)
is byte-identical: **4,124 bytes**, version
`sha256:8983ba5ed89e6373e07a275f69089a091b2954741bc90f714629fd7516fec544`.

Only the following post-seed actions occurred:

| Sequence | Actor | Action | Event ID |
| ---: | --- | --- | --- |
| 15 | Actual producer | Acquire fictional scope | `e48a18cd-e3f4-418b-a601-8ea821b0d09a` |
| 16 | Actual producer | Resolve understood blocker | `7a239b3c-af56-42e2-81a3-5b3223b97b60` |
| 17 | Actual producer | Start | `cc6c80e7-fd0b-4dbb-950e-fdd4c0e305f2` |
| 18 | Actual producer | Submit original artifact | `eb2c5707-1186-42b1-9834-9df94f411f39` |
| 19 | Actual independent reviewer | Record actual pass | `0f07d0be-3f3e-48a1-a099-039d2625469a` |
| 20 | Root using synthetic operator | Release test scope | `977fb9c9-eb2f-4a60-a529-663c172c6f92` |

The reviewer found all three requested cases, each with display, human next action
and observable evidence. The proposal separates request acknowledgement from
confirmed stop, execution from final accounting, and late results from approval.
This verdict checks a proposed document, not implemented cancellation behavior.

Root independently verified final sequence **20**, work revision **12**, exact
completion/review hash references, claim **released**, unrelated work still
proposed, and owner/producer/reviewer cursors all **0**. Next actor remains the
synthetic human operator, action **decide**, with **decision null**. No approval
was manufactured. The fictional reservation never granted provider access.

Observed friction: both agents required approved loopback sandbox access. The
producer asked for the fixture CA field name; root supplied `artifactCaFile` only,
not task guidance. The trusted participant prompt explicitly authorized the local
artifact helper despite the task's broader prohibition on external publication.
Both agents found the selected source/roles/current finding sufficient here.

The fixture process exited normally. Exact listener checks confirmed both local
ports closed; all three plaintext role configuration files and the TLS private
key were absent. Synthetic SQLite data, public test certificate and artifacts
remain in `/var/folders/h3/r7zqdttd19v3xzb_q69dqkzc0000gn/T/project-room-real-agent-lxUhoR`.
The original Markdown is also preserved in this repository. Loopback evidence
URLs are no longer served. No claim of SIGKILL/startup-failure cleanup is made.

## Deterministic size comparison

Measured before actual participation, at the same Room sequence **14**, with the
same selected source explicitly included. No mutations occurred during measurement.

| Read path | HTTP requests | Response-body bytes | Serialized client-return bytes |
| --- | ---: | ---: | ---: |
| Prior guide: `orient()` then `snapshot()` | 2 | 26,236 (13,118 each) | 16,482 if both values are fully presented |
| `workContext(id, { includeSource: true })` | 1 | 5,109 | 5,109 |

The selected response contains the correct source ID and no distractor sentinel.
This measures serialized bytes and request count in one small fixture, not actual
model tokens, latency, cost or cross-vendor performance. A caller could previously
filter the full snapshot before presenting it; that would reduce model context
but not the two whole-room transfers in the prior guide. There is no measured
human preference, retention, referral lift or population-level adoption evidence.

## Reproduction and boundaries

Run the ordinary full suites described in package scripts. For new independent
agent work, an operator may explicitly start `node scripts/real-agent-fixture.mjs
serve-context`. Supply separate agents only their own generated config path,
the work ID and the interface guides. `artifactCaFile` names the test CA;
`artifactOrigin` is the only permitted evidence origin. The helper `publish
PRODUCER-CONFIG DRAFT.md` serves exact bytes only on that loopback origin. It does
not generate a correction, prescribe a reviewer verdict or grant external authority.
Record fresh findings and IDs instead of copying this run's results. Stop normally
and verify sensitive-file/listener cleanup after every exercise.
