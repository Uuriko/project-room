# Variance decision

`buildVarianceDecision` binds exact baseline, deviation and evidence evaluation to a variance contract. It enforces separate owner, approver and reviewer, outcome conditions and in-window observation. Content and identity PII are rejected; no external effects occur.
