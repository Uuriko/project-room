# Approval exception

`buildApprovalException` creates an immutable exception bound to a conditional approval decision. Subject and criterion scopes stay contained; compensating controls and evidence are required. Roles are separate and expiry is bounded. It imports no prefixed approval modules; no external effects occur.
