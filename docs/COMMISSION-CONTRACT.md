# Commission contract

`buildCommissionContract` creates immutable hash-bound metadata over objectives, constraints and evidence. It requires independent sponsor, commissioner, executor and reviewer roles plus a valid window. Content and identity PII are rejected.
