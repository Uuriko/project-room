# Variance resolution

`buildVarianceResolution` creates an immutable resolution bound to variance exception and decision hashes. It requires complete baseline, deviation, control and evidence lineage, separate resolver and reviewer, valid time, and resolved, expired or revoked outcome. Content and identity PII are rejected; no external effects occur.
