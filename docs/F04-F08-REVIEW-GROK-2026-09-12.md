# F04 / F08 independent review — identity-scope `24d0027`

2026-09-12. Read-only replay of Codex's released identity-scope checkout.
NEW canonical file only. No identity-scope, integration, frozen MCP, or audit
edits. Not a merge or deploy.

Tree: `/Users/johnpotter/src/project-room-identity-scope` HEAD `24d0027`, clean.
Packets from `docs/PROJECT-ROOM-PRODUCT-BUILDOUT-2026-09-12.md` §15.

## F04 — relink/revocation correctness

Acceptance: narrow reconnect never widens privileges; active paths recheck access.

Codex checkpoint `IDENTITY-RELINK-SCOPE-2026-09-12.md` (commit `45beb1c` in the
stack). Source hashes still match that checkpoint:

```
b32711d93ae3c9f328cf696194314f526446738b83ea0928a2fddbd433167cbb  server/agent-identities.mjs
b643a9c1490a46f5352e0d668de9e85d60117a512af684aeba216390f30beb2a  tests/agent-identity-scope.test.js
```

Independent replay just now:

```
cd /Users/johnpotter/src/project-room-identity-scope
node --test tests/agent-identity-scope.test.js tests/conversation.test.js tests/presence.test.js
# 24/24 pass, 236ms
```

Observed on the shipped `AgentIdentities.link()` path:

| Case | Result |
|---|---|
| Relink `[accept_work, complete_work]` → `[accept_work]` | member + response + authenticate all `[accept_work]`; `relinked: true`; revision +1 |
| Explicit broaden to a different pair | stored scope is the requested pair, not the union of history |
| Invalid / human-only grants | throw; identity stays disconnected; secret does not authenticate |
| Independently reactivated unlinked member, then narrow relink | permissions become the requested narrower set |
| Duplicate link | `identity_already_linked`; permissions unchanged |
| Limited admin / demoted admin / insert failure | fail closed; insertion failure rolls back reactivation |

**Holds on this isolated store.** Canonical `/Users/johnpotter/src/project-room`
`server/agent-identities.mjs` was not compared for a merge; origin/main `ff7365e`
still has the original defect until this branch is integrated.

**Not closed**

- Credential rotation, global revocation, expiry/audience, multi-connection
  relink races (Codex remaining list).
- HTTP/UI enrollment paths were not re-driven here (only the identity module
  tests plus conversation/presence).
- Canonical and integration trees do not yet contain this repair.

## F08 — truthful state contract (People rail)

Acceptance: Online / Running / Ready / Accepted cannot contradict evidence.

Codex checkpoint `PEOPLE-RAIL-TRUTH-2026-09-12.md`. Replay of
`tests/conversation.test.js` People-rail case:

- `memberOnLine` / `memberStatus` return **work titles**, not Online/Away.
  Codex with in-progress work → `"Build the first executable Room slice"`.
  Human verifier of completed work → that work's title. Instinct with no own
  open work → `"Agent"` (kind label), not a fabricated presence.
- Receipt without terminal work → chip `"Result posted"` (work still
  `independentVerificationRequired` / `ownerDecisionRequired`).
- Same receipt after those flags cleared + `evidenceVersion` → `"Done"`.
- Inactive member → no chip.

`memberOnLine` source at `src/conversation.js:112` is waiting/own work titles
only; `now` is unused. `memberDoneChip` uses `terminalWork(done) ? "Done" :
"Result posted"`.

Presence leftovers: scan of `src/` and `server/*.mjs` found no `memberPresence`,
`presenceLabel`, `presence-dot`, or `inferred-online`. Real `/api/rooms/:roomId/presence`
still exists and is session-worker/SSE based (`tests/presence.test.js` 2/2);
idle members are absent; anonymous 401.

`tests/room-roster.test.js` 8/8 also passed (roster copy, no token leakage).

Chromium People-rail desktop/mobile journey was **not** re-run here. Codex
recorded one pass; this review does not claim physical-device or AT evidence.

**Not closed**

- Function name `memberOnLine` still sounds like presence; it is a work line.
- Real `/presence` is a different API; the rail no longer pretends it is Online.
- Canonical `src/conversation.js` / `src/app.js` are dirty WIP and were not
  edited; F08 lives only on identity-scope until integration.
- Broader status words Running/Ready/Accepted outside the People rail were not
  audited in this pass.

## Combined

F04 and F08 are **locally repaired on identity-scope `24d0027`**. They are **not**
on canonical `d963e4b`, not on origin/main `ff7365e`, and not on the dirty
integration recovery lane. Do not treat this replay as a release.

Grok stays off identity-scope writes. Next Grok packet still needs Codex
assignment (proposed G9 file-draft recovery review after that lane checkpoints).
