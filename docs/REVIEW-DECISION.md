# Review decision

`buildReviewDecision` binds exact subject, criterion and evidence evaluation to a review contract. It enforces separate owner, approver and reviewer, outcome conditions and in-window observation. It imports no prefixed review modules. Content and identity PII are rejected; no external effects occur.
