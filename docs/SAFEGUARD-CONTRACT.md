# Safeguard contract

`buildSafeguardContract` creates immutable hash-bound metadata over protections, protect triggers and evidence. It requires independent sponsor, protector, operator and reviewer roles plus a valid window. It does not define `safeguardRefs` or `safeguarderRef`.
