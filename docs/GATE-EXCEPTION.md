# Gate exception

`buildGateException` binds a conditional gate decision to contained criteria, compensating controls and evidence. It preserves gatekeeper, evaluator and reviewer separation and bounded expiry.
