# Request attention checkpoint

September 8, 2026. Local candidate on `codex/unified-local-20260907`, over
135d824. Not committed, published or deployed. Existing human-journey/copy WIP is
preserved. Overall product goal remains incomplete.

## Implemented

- Explicit observer v3 in a new private directory. V1 stdout and default v2
  work/instruction pull retain their formats and behavior; no migration/reset.
- Open incoming requests, outgoing answers/declines, and open outgoing requests
  whose recipient has inactive Room membership. No presence/model-running claim.
- Metadata-only request notices with a scoped `room_read_request` pointer.
  No request bodies, cancellation reasons or credentials enter the observer.
- A clarification changes the exact notice ID. Old acknowledgement cannot dismiss
  its replacement. Closed answers do not reappear after unrelated chat or later
  membership changes. Cancellation clears current attention, not request history.
- Two fresh authenticated, history-anchored observations before read/ack output.
  No work acceptance, answer, human read marker, approval or model execution.
- Explicit snapshot support marker shared by Node and Worker. Unsupported or
  malformed support refuses before reconciliation; a first unsupported response
  creates no inbox. Existing saved notices remain recoverable.
- A 1,001-condition v3 capacity covers 500 work items, 500 requests and instructions.
  Batches stay at 20. Persisted signatures, metadata, identity, read pointers and
  duplicate notice IDs are validated on reopening.
- CLI `pull/ack ... --requests`; MCP operator environment
  `ROOM_AGENT_ATTENTION_VERSION=3` alongside its new attention directory. Models
  cannot choose the directory/version. Tool counts remain 24 default, 26 optional.

Setup and exact limitations: [Pull attention](CURRENT-ATTENTION.md).

## Qualification

- Full core/API/package suite: 534 passed on final source.
- Full browser suite: 168 passed on fresh local services.
- Local Workers: 12 runtime checks and one browser/SSE/restart check passed.
  The Worker HTTP test explicitly checks the new request-support marker.
- Focused request/CLI/MCP/package run: 16 passed; the final complete core rerun
  also covers malformed first-response refusal without creating an inbox.
- 19 exact browser assets built; no new public asset, dependency or Room schema.
- Isolated cold-package test copies only allowlisted candidate runtime files into
  a temporary test repository, creates a synthetic commit, packages 65 files and
  opens/reopens v3 with no checkout dependencies. This is not a release artifact
  or a commit to the real project branch. Exact historical packaging remains tested.
- Desktop/touch journey captures were regenerated by the full browser run; touch
  exact-result review was inspected. No UI changes in this observer slice.

The local Worker browser still emits the known self-signed local TLS diagnostics;
its assertions pass. This is not hosted TLS qualification. Synthetic persistence
is retained by those existing tests in their reported temporary directories, most
recently `project-room-cf-store-31Tuds` and `project-room-cf-browser-8Qus98` under
the current macOS temporary root. No user data was deleted.

All new agent transport checks use scripted protocol clients, not autonomous
model reasoning. They prove local interoperability, not native Claude/Grok/Instinct
acceptance, human retention, cross-device notification or production readiness.

## Next

1. Run the approved bounded native-host request/clarification/answer and independent
   review exercise with separate synthetic identities. Capture actual tool calls
   and immutable room results; leave the human decision pending. Preserve exact
   unknown-outcome input instead of reissuing altered requests.
2. Preserve the existing preview on port64985. Its long-running process predates
   the new support marker; use a separate fresh fixture for v3 testing. Do not
   silently restart it or alter saved user host configuration.
3. Qualify a current schema12 release/fallback and hosted recovery before any
   publication. The old runtime975f5e5 does not contain this uncommitted work.
   New cold test artifacts do not change that release status.

No push, deployment, live migration, provider change, payment, outreach, recurring
automation or other-product changes occurred.
