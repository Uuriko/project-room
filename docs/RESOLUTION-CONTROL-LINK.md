# Resolution control link

`buildResolutionControlLink` binds one real upstream terminal hash as a deterministic downstream compensating control reference. The immutable, domain-separated record names source and target lane/stage, control class and observation time. It rejects malformed lineage, self-links, invalid times, unknown fields, PII and content without embedding source payloads.
