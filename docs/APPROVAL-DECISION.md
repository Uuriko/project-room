# Approval decision

`buildApprovalDecision` binds exact request, criterion and evidence evaluation to an approval contract. It enforces separate owner, approver and reviewer, outcome conditions and in-window observation. It imports no prefixed approval modules. Content and identity PII are rejected; no external effects occur.
