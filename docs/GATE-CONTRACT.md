# Gate contract

`buildGateContract` creates immutable hash-bound gate criteria and evidence metadata. It requires independent sponsor, gatekeeper, evaluator and reviewer roles plus a valid window. It does not import or redefine execution/dispatch gates or `gateRefs`.
