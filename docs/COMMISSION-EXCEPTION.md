# Commission exception

`buildCommissionException` binds a conditional commission decision to contained objectives, compensating controls, safeguards and evidence. It preserves commissioner, executor and reviewer separation and bounded expiry. Content and identity PII are rejected.
