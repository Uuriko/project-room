# Gate decision

`buildGateDecision` binds exact criteria and evidence to a gate contract. It enforces gatekeeper, evaluator and reviewer separation, outcomes, conditions and in-window observation. It does not redefine execution/dispatch gates or `gateRefs`.
