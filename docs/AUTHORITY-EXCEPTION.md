# Authority exception

`buildAuthorityException` creates an immutable exception bound to a conditional authority decision. Authority and limit scopes must remain contained; compensating controls and evidence are required. Owner, approver and reviewer must be separate, and expiry must follow observation. Content and identity PII are rejected; no external effects occur.
