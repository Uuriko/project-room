# Rule contract

`buildRuleContract` creates immutable hash-bound metadata over requirements, limits and evidence. It requires independent sponsor, owner, approver and reviewer roles plus a valid window. It does not redefine established policy `ruleRefs`.
