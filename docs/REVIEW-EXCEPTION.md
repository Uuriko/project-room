# Review exception

`buildReviewException` creates an immutable exception bound to a conditional review decision. Subject and criterion scopes stay contained; compensating controls and evidence are required. Roles are separate and expiry is bounded. It imports no prefixed review modules; no external effects occur.
