# Waiver decision

`buildWaiverDecision` binds exact requirement, control and evidence evaluation to a waiver contract. It enforces separate owner, approver and reviewer, outcome conditions and in-window observation. Content and identity PII are rejected; no external effects occur.
