# Waiver contract

`buildWaiverContract` creates an immutable hash-bound scope with nonempty requirement, control and evidence references. Sponsor, owner, approver and reviewer are separate, and expiry follows valid-from time. Content and identity PII are rejected; no external effects occur.
