# Consent exception

`buildConsentException` creates an immutable exception bound to a conditional consent decision. Permission and restriction scopes must remain contained; compensating controls and evidence are required. Owner, approver and reviewer must be separate, and expiry must follow observation. Content and identity PII are rejected; no external effects occur.
