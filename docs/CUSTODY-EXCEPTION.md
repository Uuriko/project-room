# Custody exception

`buildCustodyException` creates an immutable exception bound to a conditional custody decision. Asset and control scopes stay contained; safeguards and evidence are required. Custodian, approver and reviewer must be separate, and expiry must follow observation. Content and identity PII are rejected; no external effects occur.
