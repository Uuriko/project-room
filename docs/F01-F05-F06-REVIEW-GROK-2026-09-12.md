# F01 / F05 / F06 independent review — identity-scope `24d0027`

2026-09-12. Read-only replay. NEW canonical file only. No identity-scope,
integration, frozen MCP, or audit writes. Not a merge, restore, or deploy.

Tree: `/Users/johnpotter/src/project-room-identity-scope` HEAD `24d0027`, clean.

```
cd /Users/johnpotter/src/project-room-identity-scope
node --test tests/thread-auth-boundary.test.js tests/diagnostic-boundaries.test.js tests/room-export.test.js
# 11/11 pass, 0 fail/skip, 1662ms
```

## F01 — shared read/auth boundary

Acceptance: thread/search/export aliases reject wrong credential modes consistently.

Codex checkpoint `THREAD-AUTH-BOUNDARY-2026-09-12.md`. This replay drove the two
new regressions in `tests/thread-auth-boundary.test.js` (not the older
message-thread suite):

| Case | Observed |
|---|---|
| Room key in `room_session` cookie on `/messages/:item/thread` | 401 `unauthenticated` |
| Alternating event/thread reads until quota | thread 429 `rate_limited` (shared limiter) |

That matches the two defects Codex reproduced (cookie 200, limiter bypass 200).
They are closed **on this isolated HTTP stack**.

**Not closed**

- Account-session, guest-agent, revoked, and expiry matrices (Codex remaining).
- Search/export aliases were not in this two-test file. Export was covered
  under F05 (`/export` still 401 anonymous). Search not re-driven here.
- Canonical `server/http.mjs` is the frozen MCP preview patch, not this
  thread-auth repair. Combining them needs Codex's integration merge.
- Process-local rate limits are not a distributed quota.

## F05 — authority-aware restore (HTTP guard only)

Acceptance: invites/revocations/cleanup survive verified restore without
resurrection. **This replay does not claim that.** It only re-checks the
immediate HTTP disable.

Codex checkpoint `ONLINE-IMPORT-SAFETY-2026-09-12.md`. Observed:

| Case | Observed |
|---|---|
| Owner POST `/import` of unchanged export | 409 `recovery_requires_maintenance` |
| Duplicate-event payload | 409, no parse/write |
| Malformed body | 409 (stable maintenance code, not a parse 400) |
| Large export | GET `/export` still available; import still 409 |
| Stale export after revocation | 409; revoked access stays unusable; sentinel that HTTP does not call `importEvents` |
| Cursors / projection checkpoints | preserved after rejected import |
| Anonymous import | 401 |

**HTTP replacement is refused.** `RoomStore.importEvents` remains an internal
legacy helper. Grok G3 still showed that a stale *in-process* import resurrects
`member.active` without `identity_links`. Do not use the helper around this 409.

A qualified restore (fresh DB, authority witness, credential rotation, cutover)
is still open. Grok will not implement it unless Codex assigns a file that is
not identity-scope `store.mjs`.

## F06 — bounded/redacted diagnostics

Acceptance: global bounds and no user-ID leakage in route templates.

Codex checkpoint `DIAGNOSTIC-BOUNDARIES-2026-09-12.md`. Observed:

- Lowercase IDs and unknown path text are redacted / collapsed.
- Distinct-room retention is bounded; recently written rooms are kept.

**Not closed**

- Volatile in-process log, not durable audit.
- Invented room IDs can evict other rooms' diagnostics (intentional cap).
- Allowlist can lag new routes (`:unknown` until reviewed).
- No heap-byte proof.

## Ship status

F01/F05-guard/F06 hold on identity-scope `24d0027` only. Canonical MCP preview
stays `ship: false`. No persist join. No deploy. Codex still owns integration
file-draft recovery (`app.js` / `client.js` / `conversation.js`).
