# Delegated-control resolution

`buildDelegatedControlResolution` binds exact exception lineage and complete controls, compensating controls, safeguards and evidence. It supports resolved, expired and revoked outcomes with independent resolver/reviewer roles and timing. All identifiers are camelCase.
