# RC-2026-09-25-912 — self-serve guest entry

**Task:** RC-2026-09-25-912 · **Shipped:** PR #1077 (merged 5abd55ca), security repair #1078 (ac378fd4)
**Lane:** jill

## What was attempted / what shipped

Self-serve guest entry: same-request-ID idempotency, 500-member
historical guest LRU cap, panic revoke, OpenAPI/auth/vocabulary/recovery
updates, boundary tests.

## What worked

- Same-request-ID idempotency held up in review.
- The full-green exact-head gate caught main-advancement races.

## What didn't

- Replaying captured signed join bytes could recover the original live
  bearer credential (found by external review Burs-IA/soyeladice-svg).
- Docs claimed durable rate limits; implementation used process-local
  limiters.
- Fix: replay now returns member/status metadata with `replayed:true`
  and no credential; plaintext credential storage replaced with a hash;
  rate-limit docs corrected to say process-local, reset on restart, not
  shared across replicas.

## Next lane should know

- Rate-limit docs must say process-local (Burs-IA finding) — don't
  reintroduce "durable" claims.
- #1078 needed multiple exact-head CI reruns because main kept
  advancing — this is the churn the merge queue (S3) will kill.
